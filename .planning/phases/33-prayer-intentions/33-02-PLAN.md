---
phase: 33-prayer-intentions
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - frontend/src/pages/prayer/PrayerList.tsx
  - frontend/src/pages/prayer/PrayerIntentionPanel.tsx
  - frontend/src/pages/prayer/components/TodaysFocus.tsx
  - frontend/src/pages/prayer/components/StatusBadge.tsx
autonomous: true
requirements:
  - PRAY-01
  - PRAY-02
  - PRAY-03

must_haves:
  truths:
    - "Prayer list page shows a table of all user's prayer intentions with columns: title, contact name, status badge, created date, description preview"
    - "Active intentions appear first, then answered, then archived, each group sorted by newest"
    - "Status filter dropdown filters the list by active/answered/archived"
    - "Clicking Add Prayer opens a slide-in panel from the right with title, contact picker, and status"
    - "Editing an intention opens the same panel pre-filled with existing data"
    - "Marking as answered shows an optional note prompt dialog before confirming"
    - "Today's Focus section shows curated active intentions above the table with warm amber styling"
    - "Status badges use green for active, blue for answered, gray for archived"
    - "Warm amber chapel aesthetic: bg-amber-50/30 page background, serif headings, generous padding"
  artifacts:
    - path: "frontend/src/pages/prayer/PrayerList.tsx"
      provides: "Main prayer page with table, status filter, Today's Focus, and slide-in panel integration"
      contains: "PrayerList"
    - path: "frontend/src/pages/prayer/PrayerIntentionPanel.tsx"
      provides: "Slide-in Sheet panel for creating and editing prayer intentions"
      contains: "PrayerIntentionPanel"
    - path: "frontend/src/pages/prayer/components/TodaysFocus.tsx"
      provides: "Today's Focus section with daily prayer rotation cards"
      contains: "TodaysFocus"
    - path: "frontend/src/pages/prayer/components/StatusBadge.tsx"
      provides: "Amber-themed status badges for prayer intentions"
      contains: "StatusBadge"
  key_links:
    - from: "frontend/src/pages/prayer/PrayerList.tsx"
      to: "frontend/src/hooks/usePrayers.ts"
      via: "React Query hooks"
      pattern: "usePrayers|useTodaysFocus|useCreatePrayer|useUpdatePrayer|useDeletePrayer"
    - from: "frontend/src/pages/prayer/PrayerIntentionPanel.tsx"
      to: "frontend/src/hooks/usePrayers.ts"
      via: "create/update mutations"
      pattern: "useCreatePrayer|useUpdatePrayer"
    - from: "frontend/src/pages/prayer/PrayerList.tsx"
      to: "frontend/src/pages/prayer/PrayerIntentionPanel.tsx"
      via: "Sheet open state"
      pattern: "PrayerIntentionPanel"
---

<objective>
Build the main Prayer Intentions page with table list view, status filter, Today's Focus section, and slide-in create/edit panel with warm amber "chapel" aesthetic.

Purpose: Give missionaries a dedicated, calm, prayerful page to view, manage, and interact with their prayer intentions daily. The warm amber styling differentiates this page from the standard dashboard feel.

Output: Fully functional Prayer page at /prayer with table, filtering, CRUD via slide-in panel, Today's Focus cards, and chapel aesthetic.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-prayer-intentions/33-CONTEXT.md
@.planning/phases/33-prayer-intentions/33-RESEARCH.md
@.planning/phases/33-prayer-intentions/33-01-SUMMARY.md
@frontend/src/pages/tasks/TaskList.tsx
@frontend/src/pages/donations/DonationDetail.tsx
@frontend/src/pages/donations/DonationList.tsx
@frontend/src/hooks/usePrayers.ts
@frontend/src/api/prayers.ts
@frontend/src/hooks/useContacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Today's Focus section and StatusBadge components</name>
  <files>
    frontend/src/pages/prayer/components/TodaysFocus.tsx
    frontend/src/pages/prayer/components/StatusBadge.tsx
  </files>
  <action>
    **1. Create `frontend/src/pages/prayer/components/StatusBadge.tsx`:**
    Reusable status badge component for prayer intentions using the amber palette:
    - active: `bg-green-100 text-green-800 border-green-200 dark:bg-green-950/50 dark:text-green-400 dark:border-green-800`
    - answered: `bg-blue-100 text-blue-800 border-blue-200 dark:bg-blue-950/50 dark:text-blue-400 dark:border-blue-800`
    - archived: `bg-gray-100 text-gray-600 border-gray-200 dark:bg-gray-800/50 dark:text-gray-400 dark:border-gray-700`
    Use shadcn Badge component with `variant="outline"` base and custom className overlay.
    Export `StatusBadge({ status }: { status: PrayerIntentionStatus })` returning a Badge with label text (capitalize first letter).

    **2. Create `frontend/src/pages/prayer/components/TodaysFocus.tsx`:**
    Today's Focus section displaying curated daily prayer intentions:
    - Use `useTodaysFocus()` hook to fetch today's focus intentions
    - Render as a warm amber section above the table:
      - Container: `bg-amber-50/30 dark:bg-amber-950/20 border border-amber-100 dark:border-amber-900/50 rounded-xl p-6`
      - Heading: `font-serif text-xl text-amber-900 dark:text-amber-100` with Heart icon (sparingly)
      - Each intention as a Card: `bg-white dark:bg-card border-amber-100 dark:border-amber-800 shadow-sm`
        - Card shows: title (font-serif), contact name, StatusBadge
        - Cards arranged in a responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    - Loading state: Skeleton cards with amber tint
    - Empty state: Soft message "No active prayer intentions yet" with amber tones
    - Show "Enter Focus Mode" button (styled secondary, triggers parent callback `onStartFocusMode`)
    - Accept `onStartFocusMode` callback prop for Focus Mode integration (Plan 03)
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Verify TypeScript compilation passes</manual>
  </verify>
  <done>
    StatusBadge renders correctly for all 3 statuses with proper colors and dark mode support.
    TodaysFocus displays curated intentions in warm amber cards with loading/empty states.
    Both components export cleanly and pass TypeScript checks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prayer List page with table, status filter, slide-in panel, and answered note dialog</name>
  <files>
    frontend/src/pages/prayer/PrayerList.tsx
    frontend/src/pages/prayer/PrayerIntentionPanel.tsx
  </files>
  <action>
    **1. Create `frontend/src/pages/prayer/PrayerIntentionPanel.tsx`:**
    Slide-in Sheet panel for creating and editing prayer intentions. Follow DonationDetailPanel pattern:
    - Props: `open: boolean`, `onClose: () => void`, `intention?: PrayerIntention` (undefined = create mode), `lockedContactId?: string` (from contact detail tab)
    - Use shadcn Sheet component: `<Sheet open={open} onOpenChange={(isOpen) => !isOpen && onClose()}>` with `<SheetContent side="right" className="w-full sm:max-w-lg overflow-y-auto">`
    - Form fields:
      - Title: `<Input>` required, placeholder "What to pray for..."
      - Contact picker: Use `useSearchContacts` hook from useContacts.ts. Input field with dropdown showing search results. When `lockedContactId` is provided, show contact name as read-only text (not editable).
      - Status: `<Select>` dropdown with Active/Answered/Archived options
    - Description field intentionally omitted per user decision (title is primary)
    - On submit: call `useCreatePrayer()` or `useUpdatePrayer()` mutation, call `onClose()` on success
    - Delete button shown in edit mode, calls `useDeletePrayer()` with confirmation dialog
    - Warm amber styling on SheetHeader: serif title, amber text tones

    **2. Replace stub `frontend/src/pages/prayer/PrayerList.tsx`:**
    Full prayer list page with chapel aesthetic. Structure:

    **Page layout:**
    - Page background: `bg-amber-50/30 dark:bg-amber-950/10 min-h-screen`
    - Container with generous padding: `p-6 md:p-8 max-w-7xl mx-auto`
    - Header: `font-serif text-2xl text-amber-900 dark:text-amber-100` title "Prayer Intentions"
    - Subtitle: `text-amber-700 dark:text-amber-300 leading-relaxed` with a brief warm description

    **Today's Focus section:**
    - Render `<TodaysFocus onStartFocusMode={() => setFocusModeOpen(true)} />` at top
    - Pass state setter for Focus Mode (will be wired in Plan 03; for now, store the state but don't render the modal)

    **Controls bar:**
    - "Add Prayer" button (amber primary style) that opens PrayerIntentionPanel in create mode
    - Status filter: `<Select>` dropdown with options: All, Active, Answered, Archived
    - Use local state for status filter, pass as query param to `usePrayers({ status: filterValue })`
    - Search input for title/contact name search

    **Table (DataTable pattern not required -- use a simple HTML table for the warm aesthetic):**
    - Use a simple responsive table with amber-tinted borders
    - Table styling: `bg-white dark:bg-card border border-amber-100 dark:border-amber-800 rounded-xl shadow-sm`
    - Columns: Title, Contact, Status (StatusBadge), Created (formatLocalDate), Description (truncated to ~60 chars)
    - Row click opens PrayerIntentionPanel in edit mode with the selected intention
    - Inline status dropdown in each row: Use a small `<Select>` in the status cell that calls `useUpdatePrayer()` on change for quick status transitions
    - When status changes to "answered" via inline dropdown: Show a Dialog (shadcn AlertDialog or Dialog) with optional textarea for note/description. If user fills it, PATCH the intention with updated description. If user skips, just update status.
    - Empty state: Warm amber message "No prayer intentions yet. Add your first prayer intention to get started."

    **Pagination:**
    - Use standard pagination from API response (count, next, previous)
    - Simple Previous/Next buttons

    **State management:**
    - `selectedIntention` state for edit mode
    - `panelOpen` state for slide-in visibility
    - `focusModeOpen` state (prepared for Plan 03)
    - Status filter as simple useState (not nuqs -- user decision says "status filter only, no full filter bar")

    **Dark mode:** Use dark: variants throughout. amber-50/30 -> amber-950/10, amber-100 -> amber-800, etc.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Navigate to /prayer, verify table renders with data, filters work, slide-in panel opens for create/edit</manual>
  </verify>
  <done>
    Prayer List page renders with warm amber chapel aesthetic.
    Table shows intentions with all required columns and inline status change.
    Status filter dropdown filters by active/answered/archived.
    Slide-in panel creates and edits intentions with contact picker.
    Answered status change triggers optional note dialog.
    Today's Focus section shows curated daily intentions above the table.
    Page is fully responsive and supports dark mode.
  </done>
</task>

</tasks>

<verification>
1. Navigate to /prayer -- page loads with warm amber background and serif headings
2. Today's Focus section shows curated intentions (or empty state)
3. Table renders intentions with all 5 columns
4. Status filter works -- selecting "Active" shows only active intentions
5. Click "Add Prayer" -- slide-in panel opens from right with title, contact picker, status
6. Fill form and submit -- new intention appears in table
7. Click a row -- edit panel opens with pre-filled data
8. Change status to "answered" via inline dropdown -- note dialog appears
9. Dark mode renders correctly with amber dark variants
10. `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
- Prayer page at /prayer renders with warm chapel aesthetic
- Full CRUD via slide-in panel (create, edit, delete)
- Status filter works correctly
- Inline status change with answered note prompt
- Today's Focus section shows daily rotation
- Dark mode support throughout
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-prayer-intentions/33-02-SUMMARY.md`
</output>
