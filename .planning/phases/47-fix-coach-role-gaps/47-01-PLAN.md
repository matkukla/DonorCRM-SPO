---
phase: 47-fix-coach-role-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conftest.py
  - apps/imports/tests/test_spo_views.py
  - apps/imports/tests/test_spo_csv_fixture_mapping.py
  - apps/insights/tests/test_user_drilldown.py
autonomous: true
requirements:
  - ROLE-01

must_haves:
  truths:
    - "All four files use role='missionary' (not role='staff') for non-coach test users"
    - "Full test suite passes with zero role='staff' failures in these files"
    - "Insight user-drilldown role assertion checks 'missionary', not 'staff'"
  artifacts:
    - path: "conftest.py"
      provides: "authenticated_client fixture with correct role"
      contains: "role='missionary'"
    - path: "apps/imports/tests/test_spo_views.py"
      provides: "_make_staff() helper with correct role"
      contains: "role='missionary'"
    - path: "apps/imports/tests/test_spo_csv_fixture_mapping.py"
      provides: "_make_staff_owner() helper with correct role"
      contains: "role='missionary'"
    - path: "apps/insights/tests/test_user_drilldown.py"
      provides: "assertEqual for user role uses 'missionary'"
      contains: "'missionary'"
  key_links:
    - from: "conftest.py"
      to: "UserFactory"
      via: "role='missionary'"
      pattern: "role='missionary'"
    - from: "apps/insights/tests/test_user_drilldown.py"
      to: "user role assertion"
      via: "assertEqual"
      pattern: "assertEqual.*role.*missionary"
---

<objective>
Replace four stale `role='staff'` strings (and one `role='staff'` assertion) left over from the Phase 43 rename migration. These cause `IntegrityError` or silent permission mismatches on any test touching those fixtures, since the `staff` UserRole value was deleted in migration 0005.

Purpose: Make the test suite reflect the current role vocabulary so all tests pass without fixture errors.
Output: Four files corrected; test suite green for all affected test modules.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key facts extracted from source inspection. No codebase exploration needed. -->

From apps/users/models.py (UserRole choices — Phase 43 renamed staff→missionary):
```python
class UserRole(models.TextChoices):
    ADMIN = 'admin', 'Admin'
    FINANCE = 'finance', 'Finance'
    MISSIONARY = 'missionary', 'Missionary'   # was 'staff' before Phase 43
    READ_ONLY = 'read_only', 'Read Only'
    SUPERVISOR = 'supervisor', 'Supervisor'   # was 'mission_supervisor'
    COACH = 'coach', 'Coach'
```

Files with stale strings (confirmed by grep):
- conftest.py:25           → `user_factory(role='staff')`                → change to `role='missionary'`
- test_spo_views.py:74     → `role='staff'` in `_make_staff()`           → change to `role='missionary'`
- test_spo_csv_fixture_mapping.py:40 → `role='staff'` in `_make_staff_owner()` → change to `role='missionary'`
- test_user_drilldown.py:171 → `self.assertEqual(...['role'], 'staff')`  → change assertion value to `'missionary'`

NOTE: Django's built-in `is_staff` boolean is unrelated — do NOT touch any `is_staff` boolean field references.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale role='staff' strings and fix role assertion</name>
  <files>conftest.py, apps/imports/tests/test_spo_views.py, apps/imports/tests/test_spo_csv_fixture_mapping.py, apps/insights/tests/test_user_drilldown.py</files>
  <behavior>
    - conftest.py: authenticated_client fixture creates user with role='missionary', not role='staff'
    - test_spo_views.py: _make_staff() helper creates user with role='missionary'
    - test_spo_csv_fixture_mapping.py: _make_staff_owner() helper creates user with role='missionary'
    - test_user_drilldown.py: assertEqual on user['role'] passes when value is 'missionary'
  </behavior>
  <action>
    Make these four targeted edits — nothing else:

    1. conftest.py line 25: change `role='staff'` to `role='missionary'`

    2. apps/imports/tests/test_spo_views.py line 74: change `role='staff'` to `role='missionary'`

    3. apps/imports/tests/test_spo_csv_fixture_mapping.py line 40: change `role='staff'` to `role='missionary'`
       (The surrounding `first_name='Owner', last_name='Staff'` strings are display names and must be left unchanged.)

    4. apps/insights/tests/test_user_drilldown.py line 171: change `'staff'` to `'missionary'` in the assertEqual assertion value only.

    Do NOT change any `is_staff` boolean references, Django admin `is_staff` fields, or any string that is not a UserRole value.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python -m pytest conftest.py apps/imports/tests/test_spo_views.py apps/imports/tests/test_spo_csv_fixture_mapping.py apps/insights/tests/test_user_drilldown.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All four files use role='missionary'; the four test modules pass with zero errors related to stale role strings.</done>
</task>

<task type="auto">
  <name>Task 2: Run broader test suite to confirm no regressions</name>
  <files></files>
  <action>
    Run the broader test suite covering contacts, users, imports, and insights to confirm no regressions from the string replacements.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python -m pytest apps/contacts/tests/ apps/users/tests/ apps/imports/tests/ apps/insights/tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in the four app directories pass. Zero failures, zero errors.</done>
</task>

</tasks>

<verification>
- grep -r "role='staff'" conftest.py apps/imports/tests/ apps/insights/tests/ returns zero results
- python -m pytest conftest.py apps/imports/tests/test_spo_views.py apps/imports/tests/test_spo_csv_fixture_mapping.py apps/insights/tests/test_user_drilldown.py -x -q passes
- No is_staff boolean references were touched
</verification>

<success_criteria>
- Four stale role='staff' strings replaced with role='missionary'
- One stale 'staff' assertion value replaced with 'missionary'
- Broader test suite (contacts, users, imports, insights) green
</success_criteria>

<output>
After completion, create `.planning/phases/47-fix-coach-role-gaps/47-01-SUMMARY.md`
</output>
