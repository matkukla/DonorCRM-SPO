---
phase: 38-ui-polish-list-page-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/Header.tsx
  - frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx
  - frontend/src/pages/admin/analytics/components/UserDrilldownPanel.tsx
  - frontend/src/pages/prayer/PrayerIntentionPanel.tsx
  - frontend/src/pages/journals/components/EventTimelineDrawer.tsx
  - apps/contacts/models.py
  - frontend/src/pages/contacts/ContactList.tsx
  - frontend/src/pages/contacts/ContactForm.tsx
  - frontend/src/pages/contacts/ContactDetail.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Every modal/panel in the app opens as a centered Dialog (no side-sliding Sheets remain)"
    - "All Dialogs have a dark semi-transparent backdrop overlay"
    - "Dialogs dismiss via backdrop click, close button, or Escape key"
    - "Large-content Dialogs have max height ~80vh with internal scrolling"
    - "The contacts page shows 'Potential Donor' everywhere 'Prospect' previously appeared"
    - "Filter dropdowns, badges, and form selects all say 'Potential Donor' not 'Prospect'"
  artifacts:
    - path: "frontend/src/components/layout/Header.tsx"
      provides: "Mobile nav as centered Dialog"
      contains: "Dialog"
    - path: "frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx"
      provides: "Funnel drilldown as centered Dialog"
      contains: "DialogContent"
    - path: "frontend/src/pages/admin/analytics/components/UserDrilldownPanel.tsx"
      provides: "User drilldown as centered Dialog"
      contains: "DialogContent"
    - path: "frontend/src/pages/prayer/PrayerIntentionPanel.tsx"
      provides: "Prayer intention panel as centered Dialog"
      contains: "DialogContent"
    - path: "frontend/src/pages/journals/components/EventTimelineDrawer.tsx"
      provides: "Event timeline as centered Dialog"
      contains: "DialogContent"
    - path: "apps/contacts/models.py"
      provides: "TextChoices display string 'Potential Donor'"
      contains: "Potential Donor"
    - path: "frontend/src/pages/contacts/ContactList.tsx"
      provides: "Status label 'Potential Donor'"
      contains: "Potential Donor"
  key_links:
    - from: "apps/contacts/models.py"
      to: "frontend/src/pages/contacts/ContactList.tsx"
      via: "Backend display label matches frontend label"
      pattern: "Potential Donor"
    - from: "frontend/src/components/ui/dialog.tsx"
      to: "frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx"
      via: "Dialog import replacing Sheet"
      pattern: "from.*dialog"
---

<objective>
Convert all remaining Sheet (side-sliding) components to centered Dialog components, and rename "Prospect" to "Potential Donor" across both backend and frontend.

Purpose: Ensure visual consistency — all modal overlays center on screen with backdrop, and the contact status label matches the user-facing terminology.
Output: 5 Sheet-to-Dialog conversions + backend/frontend label rename with Django migration.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui-polish-list-page-cleanup/38-CONTEXT.md

<interfaces>
<!-- Sheet and Dialog component APIs for conversion -->

From frontend/src/components/ui/sheet.tsx (being replaced):
```typescript
export { Sheet, SheetPortal, SheetOverlay, SheetTrigger, SheetClose, SheetContent, SheetHeader, SheetTitle, SheetDescription }
// SheetContent props: side?: "top" | "bottom" | "left" | "right"
```

From frontend/src/components/ui/dialog.tsx (replacement):
```typescript
export { Dialog, DialogPortal, DialogOverlay, DialogTrigger, DialogClose, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription }
// DialogContent: centered, max-w-lg by default, supports className override
```

Sheet-to-Dialog mapping:
- Sheet → Dialog
- SheetContent → DialogContent (remove `side` prop, add className for width/scroll)
- SheetHeader → DialogHeader
- SheetTitle → DialogTitle
- SheetDescription → DialogDescription

From apps/contacts/models.py:
```python
class ContactStatus(models.TextChoices):
    PROSPECT = 'prospect', 'Prospect'  # Change display to 'Potential Donor'
    ASKED = 'asked', 'Asked'
    DONOR = 'donor', 'Donor'
    LAPSED = 'lapsed', 'Lapsed'
    DECLINED = 'declined', 'Declined'
```

Frontend files with "Prospect" label:
- ContactList.tsx: statusLabels (line 32), filter dropdown (line 264)
- ContactForm.tsx: statusLabels (line 20)
- ContactDetail.tsx: statusLabels (line 43)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 5 remaining Sheet components to centered Dialog</name>
  <files>
    frontend/src/components/layout/Header.tsx
    frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx
    frontend/src/pages/admin/analytics/components/UserDrilldownPanel.tsx
    frontend/src/pages/prayer/PrayerIntentionPanel.tsx
    frontend/src/pages/journals/components/EventTimelineDrawer.tsx
  </files>
  <action>
For each of the 5 files, apply the same conversion pattern:

1. **Replace imports**: Change `from "@/components/ui/sheet"` to `from "@/components/ui/dialog"`. Map component names:
   - `Sheet` → `Dialog`
   - `SheetContent` → `DialogContent`
   - `SheetHeader` → `DialogHeader`
   - `SheetTitle` → `DialogTitle`
   - `SheetDescription` → `DialogDescription`
   - `SheetClose` → `DialogClose` (if used)

2. **Replace JSX**:
   - `<Sheet ...>` → `<Dialog ...>`
   - `<SheetContent side="..." className="...">` → `<DialogContent className="...">` (remove `side` prop entirely)
   - `SheetHeader` → `DialogHeader`, `SheetTitle` → `DialogTitle`, etc.

3. **Add appropriate sizing** to each DialogContent based on content complexity:

   **Header.tsx** (mobile nav):
   - The mobile sidebar Sheet slides from left. Convert to: `<DialogContent className="max-w-xs p-0">` — narrow dialog containing the Sidebar component. Keep the `<Sidebar onNavClick={() => setMobileMenuOpen(false)} />` inside.

   **FunnelDrilldownPanel.tsx** (contacts table):
   - `<DialogContent className="max-w-2xl max-h-[80vh] overflow-y-auto">` — wide enough for table, scrollable.

   **UserDrilldownPanel.tsx** (user stats + contacts table):
   - `<DialogContent className="max-w-3xl max-h-[80vh] overflow-y-auto">` — wider for cards + table layout.

   **PrayerIntentionPanel.tsx** (prayer intention form/display):
   - `<DialogContent className="max-w-lg max-h-[80vh] overflow-y-auto">` — standard dialog width.

   **EventTimelineDrawer.tsx** (infinite scroll timeline):
   - `<DialogContent className="max-w-2xl max-h-[80vh] overflow-y-auto">` — scrollable timeline.

4. All DialogContent components already include backdrop overlay and close button (built into the dialog.tsx component), so no additional work needed for backdrop/close/escape behavior.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - No Sheet imports remain in Header.tsx, FunnelDrilldownPanel.tsx, UserDrilldownPanel.tsx, PrayerIntentionPanel.tsx, EventTimelineDrawer.tsx
    - All 5 components use Dialog/DialogContent instead
    - Each DialogContent has appropriate max-width and max-h-[80vh] with overflow-y-auto for large content
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename "Prospect" to "Potential Donor" in backend and frontend</name>
  <files>
    apps/contacts/models.py
    frontend/src/pages/contacts/ContactList.tsx
    frontend/src/pages/contacts/ContactForm.tsx
    frontend/src/pages/contacts/ContactDetail.tsx
  </files>
  <action>
1. In `apps/contacts/models.py`, update the `ContactStatus` TextChoices display string:
   ```python
   PROSPECT = 'prospect', 'Potential Donor'
   ```
   The stored value stays `'prospect'` — only the human-readable label changes.

2. Run `python manage.py makemigrations contacts` to generate the migration (Django tracks TextChoices label changes in migrations).

3. Run `python manage.py migrate` to apply.

4. In `frontend/src/pages/contacts/ContactList.tsx`:
   - Change `prospect: "Prospect"` to `prospect: "Potential Donor"` in `statusLabels` (line ~32)
   - Change `prospect: "Prospect"` to `prospect: "Potential Donor"` in the inline filter labels object (line ~264, inside the status filter dropdown)
   - The badge variant mapping (`prospect: "secondary"`) stays unchanged — only the text label changes

5. In `frontend/src/pages/contacts/ContactForm.tsx`:
   - Change `prospect: "Prospect"` to `prospect: "Potential Donor"` in `statusLabels` (line ~20)

6. In `frontend/src/pages/contacts/ContactDetail.tsx`:
   - Change `prospect: "Prospect"` to `prospect: "Potential Donor"` in `statusLabels` (line ~43)

Do NOT change:
- The stored value `'prospect'` in the database
- The TypeScript type `ContactStatus = "prospect" | ...` (the value stays 'prospect')
- Badge variant (`"secondary"` for prospect)
- Any test files that use `status='prospect'` (those use the stored value, not the display label)
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python manage.py check 2>&1 | head -5 && grep -rn "Prospect" frontend/src/pages/contacts/ 2>/dev/null; echo "---"; grep -n "Potential Donor" frontend/src/pages/contacts/*.tsx</automated>
  </verify>
  <done>
    - ContactStatus.PROSPECT display label is 'Potential Donor' in Django model
    - Migration created and applied
    - All 3 frontend contact page files show "Potential Donor" not "Prospect" in statusLabels
    - Badge variant remains "secondary" for prospect status
    - No remaining "Prospect" display labels in frontend contact pages (grep confirms)
  </done>
</task>

</tasks>

<verification>
1. `python manage.py check` passes
2. `npx tsc --noEmit` passes
3. `grep -r "from.*sheet" frontend/src/ --include="*.tsx"` returns ONLY the sheet.tsx component file itself and Header.tsx SHOULD NOT appear
4. `grep -rn "Prospect" frontend/src/pages/contacts/` returns NO matches (only "Potential Donor")
5. All Sheet consumers now use Dialog components
</verification>

<success_criteria>
- Zero Sheet imports remain outside of the sheet.tsx component definition itself (search: `grep -r "from.*ui/sheet" frontend/src/ --include="*.tsx"` should return only Header.tsx for mobile nav — WAIT, Header should also be converted. So only sheet.tsx itself should import from radix)
- All 5 converted components render as centered Dialogs with backdrop overlay
- "Prospect" label is replaced by "Potential Donor" in all user-facing text on contacts pages
- Django migration exists for ContactStatus label change
- TypeScript and Python checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui-polish-list-page-cleanup/38-02-SUMMARY.md`
</output>
