---
phase: 05-grid-interactions-decision-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/progress.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Toast notifications display on user actions"
    - "Select dropdowns work for form fields"
    - "Progress bar renders with percentage fill"
    - "Checkboxes toggle between checked/unchecked"
  artifacts:
    - path: "frontend/src/components/ui/sonner.tsx"
      provides: "Toast notification provider and styled Toaster"
      contains: "Toaster"
    - path: "frontend/src/components/ui/select.tsx"
      provides: "Accessible select dropdown components"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue"]
    - path: "frontend/src/components/ui/progress.tsx"
      provides: "Accessible progress bar component"
      exports: ["Progress"]
    - path: "frontend/src/components/ui/checkbox.tsx"
      provides: "Accessible checkbox component"
      exports: ["Checkbox"]
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/ui/sonner.tsx"
      via: "Toaster import and render"
      pattern: "import.*Toaster.*from.*sonner"
---

<objective>
Install shadcn/ui component dependencies and add UI primitives needed for Phase 5 interactions.

Purpose: These components are prerequisites for decision dialogs (Select), progress tracking (Progress), next steps (Checkbox), and feedback (Sonner toast).

Output: Four new UI components ready for use, toast provider integrated into app root.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-grid-interactions-decision-ui/05-RESEARCH.md

# Existing UI components follow this pattern
@frontend/src/components/ui/badge.tsx
@frontend/src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix primitives and Sonner</name>
  <files>frontend/package.json</files>
  <action>
Install the required npm packages:
```bash
cd frontend && npm install sonner @radix-ui/react-select @radix-ui/react-progress @radix-ui/react-checkbox
```

These packages are:
- sonner: Toast notification library (recommended by shadcn/ui)
- @radix-ui/react-select: Accessible dropdown select primitive
- @radix-ui/react-progress: Accessible progress bar primitive
- @radix-ui/react-checkbox: Accessible checkbox primitive

Verify package.json has all four dependencies added.
  </action>
  <verify>`cd frontend && npm ls sonner @radix-ui/react-select @radix-ui/react-progress @radix-ui/react-checkbox` shows all packages installed</verify>
  <done>All four packages in package.json dependencies and installed in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create shadcn/ui components and integrate Toaster</name>
  <files>
frontend/src/components/ui/sonner.tsx
frontend/src/components/ui/select.tsx
frontend/src/components/ui/progress.tsx
frontend/src/components/ui/checkbox.tsx
frontend/src/App.tsx
  </files>
  <action>
Create the four UI components following shadcn/ui patterns. Use the exact implementations from RESEARCH.md section "Code Examples":

1. **sonner.tsx** - Toaster wrapper with shadcn/ui theme styling:
   - Import `Toaster as Sonner` from "sonner"
   - Apply theme="light" and shadcn/ui class names
   - Export as `Toaster`

2. **select.tsx** - Full Select component with all primitives:
   - Import all from @radix-ui/react-select
   - Create: Select, SelectGroup, SelectValue, SelectTrigger, SelectContent, SelectItem
   - Use cn() from @/lib/utils for className merging
   - Include ChevronDown icon in trigger, Check icon in item indicator

3. **progress.tsx** - Progress bar with indicator:
   - Import from @radix-ui/react-progress
   - Root with overflow-hidden rounded-full bg-secondary
   - Indicator with translateX transform based on value prop

4. **checkbox.tsx** - Checkbox with check indicator:
   - Import from @radix-ui/react-checkbox
   - Root with border-primary styling
   - Indicator with Check icon from lucide-react

5. **App.tsx** - Add Toaster to app root:
   - Import `{ Toaster }` from "@/components/ui/sonner"
   - Render `<Toaster position="bottom-right" />` after Router closing tag
   - This enables `toast()` calls anywhere in the app

All components must:
- Use forwardRef for ref forwarding
- Use cn() from @/lib/utils
- Follow existing component patterns in the codebase (see badge.tsx, dialog.tsx)
- Include "use client" directive at top (Next.js pattern used in this codebase)
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd frontend && npx tsc --noEmit
```
  </verify>
  <done>
- sonner.tsx exports Toaster
- select.tsx exports Select, SelectTrigger, SelectContent, SelectItem, SelectValue
- progress.tsx exports Progress
- checkbox.tsx exports Checkbox
- App.tsx renders Toaster at bottom-right
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm ls sonner @radix-ui/react-select @radix-ui/react-progress @radix-ui/react-checkbox` - all packages listed
2. `cd frontend && npx tsc --noEmit` - no TypeScript errors
3. Files exist and export correct components:
   - `grep -l "export.*Toaster" frontend/src/components/ui/sonner.tsx`
   - `grep -l "export.*Select" frontend/src/components/ui/select.tsx`
   - `grep -l "export.*Progress" frontend/src/components/ui/progress.tsx`
   - `grep -l "export.*Checkbox" frontend/src/components/ui/checkbox.tsx`
4. App.tsx includes Toaster: `grep "Toaster" frontend/src/App.tsx`
</verification>

<success_criteria>
- Four new UI component files created following shadcn/ui patterns
- All npm dependencies installed
- TypeScript compiles without errors
- Toaster integrated into App.tsx for global toast access
</success_criteria>

<output>
After completion, create `.planning/phases/05-grid-interactions-decision-ui/05-01-SUMMARY.md`
</output>
