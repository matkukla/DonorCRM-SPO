---
phase: 18-interactive-visualizations-drill-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/insights/services.py
  - apps/insights/serializers.py
  - apps/insights/views.py
  - apps/insights/urls.py
  - apps/insights/tests/test_stage_contacts.py
  - frontend/src/api/insights.ts
  - frontend/src/hooks/useInsights.ts
  - frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx
  - frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can click a funnel stage segment and see a list of contacts in that stage"
    - "Drill-down panel slides in from the right without navigating away from the dashboard"
    - "Contact list in drill-down panel loads from backend endpoint filtered by stage parameter"
    - "Panel shows contact name, owner, and last activity date for each contact"
    - "Panel closes via overlay click, Esc key, or close button"
  artifacts:
    - path: "apps/insights/services.py"
      provides: "get_stage_contacts service function"
      contains: "def get_stage_contacts"
    - path: "apps/insights/views.py"
      provides: "StageContactsView API endpoint"
      contains: "class StageContactsView"
    - path: "apps/insights/urls.py"
      provides: "URL route for stage-contacts endpoint"
      contains: "stage-contacts"
    - path: "apps/insights/tests/test_stage_contacts.py"
      provides: "Tests for stage contacts endpoint"
      contains: "class TestStageContacts"
    - path: "frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx"
      provides: "Slide-in panel component for stage contact list"
      exports: ["FunnelDrilldownPanel"]
    - path: "frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx"
      provides: "Funnel chart with onClick handler"
      contains: "onStageClick"
  key_links:
    - from: "ConversionFunnelChart.tsx"
      to: "AdminAnalyticsDashboard.tsx"
      via: "onStageClick callback prop"
      pattern: "onStageClick"
    - from: "AdminAnalyticsDashboard.tsx"
      to: "FunnelDrilldownPanel.tsx"
      via: "funnelDrilldown state passed as props"
      pattern: "funnelDrilldown"
    - from: "FunnelDrilldownPanel.tsx"
      to: "/api/v1/insights/admin/stage-contacts/"
      via: "useAdminStageContacts hook with enabled: !!stage"
      pattern: "useAdminStageContacts"
    - from: "StageContactsView"
      to: "get_stage_contacts service"
      via: "view calls service function"
      pattern: "get_stage_contacts"
---

<objective>
Build the funnel stage drill-down feature: admin clicks a funnel chart segment and a slide-in panel shows the list of contacts currently in that pipeline stage.

Purpose: Enable admins to explore the conversion funnel interactively -- clicking a stage reveals which contacts are there, supporting coaching decisions.
Output: Backend stage-contacts endpoint, FunnelDrilldownPanel component, interactive ConversionFunnelChart with onClick, and dashboard state coordination.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-interactive-visualizations---drill-down/18-RESEARCH.md
@apps/insights/services.py
@apps/insights/views.py
@apps/insights/urls.py
@apps/insights/serializers.py
@frontend/src/api/insights.ts
@frontend/src/hooks/useInsights.ts
@frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
@frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx
@frontend/src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend stage-contacts endpoint with tests</name>
  <files>
    apps/insights/services.py
    apps/insights/serializers.py
    apps/insights/views.py
    apps/insights/urls.py
    apps/insights/tests/test_stage_contacts.py
  </files>
  <action>
    Create a new admin-only endpoint GET /api/v1/insights/admin/stage-contacts/?stage={stage_value} that returns contacts currently in a given pipeline stage.

    **Service function in services.py -- add `get_stage_contacts(stage, limit=100)`:**
    - Accept `stage` parameter (string matching PipelineStage values, or None for "No Activity")
    - Use Subquery to find each JournalContact's current (most recent) stage from JournalStageEvent
    - Filter JournalContact records where current_stage matches the requested stage
    - Extract distinct Contact objects from matching JournalContacts
    - select_related('owner') to avoid N+1
    - Return dict with 'contacts' list (id, full_name, email, owner_name, last_activity_date) and 'total_count'
    - For last_activity_date: use same Subquery pattern as get_stalled_contacts (latest JournalStageEvent.created_at)
    - Handle stage=None case (contacts with no stage events) by filtering where current_stage is null
    - Target: <5 queries

    **Serializer in serializers.py:**
    - Add StageContactItemSerializer (id, full_name, email, owner_name, last_activity_date)
    - Add StageContactsResponseSerializer (contacts list, total_count, stage)

    **View in views.py -- add StageContactsView(APIView):**
    - permission_classes: [IsAuthenticated, IsAdmin]
    - Parse `stage` query param (required, return 400 if missing)
    - Parse `limit` via get_safe_int_param (default 100, min 1, max 500)
    - Call get_stage_contacts(stage=stage, limit=limit)
    - Use StageContactsResponseSerializer for response
    - Handle special value "none" or empty string for null stage (No Activity contacts)

    **URL in urls.py:**
    - path('admin/stage-contacts/', StageContactsView.as_view(), name='admin-stage-contacts')

    **Tests in test_stage_contacts.py:**
    - Test admin can access endpoint (200)
    - Test non-admin gets 403
    - Test stage parameter is required (400)
    - Test returns contacts in correct stage
    - Test returns empty list for stage with no contacts
    - Test "none" stage parameter returns contacts with no stage events
    - Use same test patterns as existing test_user_detail.py (create admin user, create contacts, create journals, create stage events)
  </action>
  <verify>
    Run: cd /home/matkukla/projects/DonorCRM && python manage.py test apps.insights.tests.test_stage_contacts -v2
    All tests pass. No import errors.
  </verify>
  <done>
    GET /api/v1/insights/admin/stage-contacts/?stage=contact returns contacts in "Contact" stage.
    Admin-only access enforced (403 for non-admin).
    Stage parameter required (400 if missing).
    Tests pass covering access control, filtering, and edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build interactive funnel chart and drill-down panel</name>
  <files>
    frontend/src/api/insights.ts
    frontend/src/hooks/useInsights.ts
    frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx
    frontend/src/pages/admin/analytics/components/FunnelDrilldownPanel.tsx
    frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
  </files>
  <action>
    **API types in insights.ts:**
    - Add StageContactItem interface: { id: string, full_name: string, email: string | null, owner_name: string, last_activity_date: string | null }
    - Add StageContactsResponse interface: { contacts: StageContactItem[], total_count: number, stage: string }
    - Add StageContactsParams interface: { stage: string, limit?: number }
    - Add getAdminStageContacts(params: StageContactsParams) function calling GET /insights/admin/stage-contacts/

    **Hook in useInsights.ts:**
    - Add useAdminStageContacts(stage: string | null) hook
    - Query key: ["insights", "admin", "stage-contacts", stage]
    - Use enabled: !!stage to prevent fetch when stage is null (per research Pattern 3)
    - staleTime: STALE_TIME (5 minutes)

    **Update ConversionFunnelChart.tsx:**
    - Accept new optional prop: onStageClick?: (stage: string) => void
    - Include `stage` field in chartData useMemo transformation (from data.funnel stage field)
    - Add onClick handler on Funnel component: (data: any, index: number) => { if (data.stage !== undefined && onStageClick) onStageClick(data.stage ?? 'none') }
    - Add cursor="pointer" to Funnel component for visual feedback
    - Update CardDescription to include "Click a stage to view contacts" hint text

    **Create FunnelDrilldownPanel.tsx:**
    - Props: { open: boolean, stage: string | null, onClose: () => void }
    - Use Sheet/SheetContent (side="right") from @/components/ui/sheet with className "w-full sm:max-w-2xl overflow-y-auto" to override default sm:max-w-sm
    - SheetHeader with SheetTitle showing stage name (map stage value to display label using STAGE_LABELS constant)
    - SheetDescription showing total count
    - Use useAdminStageContacts(stage) hook for conditional data fetching
    - Loading state: 5 skeleton rows (animate-pulse pattern)
    - Empty state: "No contacts in this stage" centered text
    - Data state: Table with columns Name, Owner, Last Activity using Table/TableHeader/TableBody/TableRow/TableCell from @/components/ui/table
    - Format last_activity_date using date-fns format (or show "No activity" if null)
    - Import format from "date-fns"
    - Define STAGE_LABELS map: { contact: "Contact", meet: "Meet", close: "Close", decision: "Decision", thank: "Thank", next_steps: "Next Steps", none: "No Activity" }

    **Update AdminAnalyticsDashboard.tsx:**
    - Add useState for funnelDrilldown: { open: boolean, stage: string | null } initialized to { open: false, stage: null }
    - Add handleStageClick function: sets funnelDrilldown to { open: true, stage }
    - Add handleFunnelClose function: sets funnelDrilldown to { open: false, stage: null }
    - Pass onStageClick={handleStageClick} to ConversionFunnelChart
    - Render FunnelDrilldownPanel with open={funnelDrilldown.open} stage={funnelDrilldown.stage} onClose={handleFunnelClose}
    - Place panel render AFTER the grid layout (at bottom of component, before closing tags)
  </action>
  <verify>
    Run: cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit
    TypeScript compilation succeeds with no errors.
    Run: cd /home/matkukla/projects/DonorCRM/frontend && npx vite build
    Vite build succeeds.
  </verify>
  <done>
    ConversionFunnelChart has onClick handler that calls onStageClick with stage value.
    FunnelDrilldownPanel opens as slide-in sheet showing contact list for selected stage.
    AdminAnalyticsDashboard coordinates panel state between funnel chart and drill-down panel.
    Panel conditionally fetches data only when open (enabled: !!stage).
    TypeScript compiles cleanly, Vite builds successfully.
  </done>
</task>

</tasks>

<verification>
1. Backend: python manage.py test apps.insights.tests.test_stage_contacts -v2 -- all tests pass
2. Frontend: npx tsc --noEmit -- no TypeScript errors
3. Frontend: npx vite build -- build succeeds
4. Integration: Funnel chart segments are clickable (cursor: pointer), clicking opens slide-in panel with contact list
</verification>

<success_criteria>
- Admin can click any funnel stage segment and see a slide-in panel with contacts in that stage
- Panel shows contact name, owner name, and last activity date in a table
- Panel fetches data only when opened (no eager loading)
- Panel closes via Esc, overlay click, or close button
- Backend endpoint enforces admin-only access
- All tests pass, TypeScript compiles, Vite builds
</success_criteria>

<output>
After completion, create `.planning/phases/18-interactive-visualizations---drill-down/18-01-SUMMARY.md`
</output>
