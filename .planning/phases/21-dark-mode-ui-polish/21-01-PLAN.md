---
phase: 21-dark-mode-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/imports/ImportDialog.tsx
  - frontend/src/components/imports/ImportCard.tsx
  - frontend/src/components/imports/SPOImportTile.tsx
  - frontend/src/components/imports/ExportCard.tsx
  - frontend/src/pages/admin/ImportCenter.tsx
  - frontend/src/pages/admin/analytics/components/TimePeriodComparison.tsx
  - frontend/src/pages/admin/analytics/components/AlertsPanel.tsx
  - frontend/src/pages/admin/analytics/components/UserComparison.tsx
  - frontend/src/pages/groups/GroupDetail.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/dashboard/StatCard.tsx
  - frontend/src/pages/settings/Settings.tsx
autonomous: true

must_haves:
  truths:
    - "All pages render with consistent colors in dark mode (no hardcoded light-only colors visible)"
    - "Status colors (green/red/yellow/blue) remain visually distinct in dark mode"
    - "All text meets WCAG 4.5:1 contrast ratio in both light and dark mode"
  artifacts:
    - path: "frontend/src/components/imports/ImportDialog.tsx"
      provides: "Dark mode color pairs for import dialog status indicators"
      contains: "dark:"
    - path: "frontend/src/components/imports/ImportCard.tsx"
      provides: "Dark mode color pairs for import card status"
      contains: "dark:"
    - path: "frontend/src/components/imports/SPOImportTile.tsx"
      provides: "Dark mode color pairs for SPO import tile"
      contains: "dark:"
    - path: "frontend/src/pages/admin/analytics/components/TimePeriodComparison.tsx"
      provides: "Dark mode color pairs for comparison indicators"
      contains: "dark:"
    - path: "frontend/src/pages/admin/analytics/components/AlertsPanel.tsx"
      provides: "Dark mode color pairs for alert backgrounds"
      contains: "dark:"
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Dark mode color pairs for success/info badge variants"
      contains: "dark:"
    - path: "frontend/src/components/dashboard/StatCard.tsx"
      provides: "Dark mode color pairs for stat trend indicators"
      contains: "dark:"
  key_links:
    - from: "NeedsAttention.tsx"
      to: "all 12 fixed files"
      via: "same dark: variant pattern"
      pattern: "dark:bg-.*-950|dark:text-.*-400|dark:border-.*-800"
---

<objective>
Fix all 50 hardcoded dark mode color occurrences across 12 files (QAL-03) by adding paired `dark:` Tailwind variants for semantic status colors and replacing generic surfaces with semantic tokens.

Purpose: All pages render correctly in dark mode without hardcoded light-only colors bleeding through as unreadable or invisible elements.
Output: 12 modified frontend component files with correct dark mode color pairing.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dark-mode-ui-polish/21-RESEARCH.md

# Reference pattern (already correct dark mode implementation)
@frontend/src/components/dashboard/NeedsAttention.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dark mode colors in import-related components (20 occurrences, 5 files)</name>
  <files>
    frontend/src/components/imports/ImportDialog.tsx
    frontend/src/components/imports/ImportCard.tsx
    frontend/src/components/imports/SPOImportTile.tsx
    frontend/src/components/imports/ExportCard.tsx
    frontend/src/pages/admin/ImportCenter.tsx
  </files>
  <action>
Replace hardcoded Tailwind color classes with paired `dark:` variants using the NeedsAttention.tsx pattern as reference. Apply these specific mappings:

**Color mapping rules:**
- `bg-green-50` -> `bg-green-50 dark:bg-green-950/50`
- `bg-red-50` -> `bg-red-50 dark:bg-red-950/50`
- `bg-yellow-50` -> `bg-yellow-50 dark:bg-yellow-950/50`
- `bg-blue-50` -> `bg-blue-50 dark:bg-blue-950/50`
- `text-green-600` -> `text-green-600 dark:text-green-400`
- `text-green-800` -> `text-green-800 dark:text-green-200`
- `text-red-600` -> `text-red-600 dark:text-red-400`
- `text-red-700` -> `text-red-700 dark:text-red-300`
- `text-red-800` -> `text-red-800 dark:text-red-200`
- `text-red-900` -> `text-red-900 dark:text-red-200`
- `text-yellow-600` -> `text-yellow-600 dark:text-yellow-400`
- `text-yellow-800` -> `text-yellow-800 dark:text-yellow-200`
- `text-blue-600` -> `text-blue-600 dark:text-blue-400`
- `border-yellow-200` -> `border-yellow-200 dark:border-yellow-800`
- `border-green-200` -> `border-green-200 dark:border-green-800`
- `border-green-500` -> `border-green-500 dark:border-green-400`
- `border-blue-200` -> `border-blue-200 dark:border-blue-800`

**Per-file occurrence counts (from research):**
- ImportDialog.tsx: 12 occurrences (green-50/600, blue-600, red-50/600/700/800, yellow-50/200)
- ImportCard.tsx: 3 occurrences (green-50/500/600, yellow-50/200)
- SPOImportTile.tsx: 3 occurrences (yellow-50/200/600/800)
- ExportCard.tsx: 1 occurrence (green-600)
- ImportCenter.tsx: 1 occurrence (blue-50, border-blue-200)

Do NOT replace colors that already have `dark:` variants. Do NOT change `bg-white` to `bg-background` in these files unless it is a standalone background (not inside a status indicator). Status colors must remain colored in dark mode.
  </action>
  <verify>
Run `grep -rn "bg-green-50\b" --include="*.tsx" frontend/src/components/imports/ frontend/src/pages/admin/ImportCenter.tsx | grep -v "dark:"` -- should return 0 results (all green-50 instances have dark: pair).
Run `grep -rn "text-red-600\b" --include="*.tsx" frontend/src/components/imports/ | grep -v "dark:"` -- should return 0 results.
Run `cd frontend && npx tsc --noEmit` -- no TypeScript errors.
  </verify>
  <done>All 20 hardcoded color occurrences in the 5 import-related files have paired dark: variants. No remaining unpaired semantic colors in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dark mode colors in analytics, settings, and shared UI components (18 occurrences, 7 files)</name>
  <files>
    frontend/src/pages/admin/analytics/components/TimePeriodComparison.tsx
    frontend/src/pages/admin/analytics/components/AlertsPanel.tsx
    frontend/src/pages/admin/analytics/components/UserComparison.tsx
    frontend/src/pages/groups/GroupDetail.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/dashboard/StatCard.tsx
    frontend/src/pages/settings/Settings.tsx
  </files>
  <action>
Apply the same dark: variant pairing pattern to the remaining 7 files. Use NeedsAttention.tsx as reference.

**Per-file specifics:**
- TimePeriodComparison.tsx (6 occurrences): `text-green-600` -> `text-green-600 dark:text-green-400`, `text-red-600` -> `text-red-600 dark:text-red-400`, `text-gray-400` -> `text-muted-foreground` (gray is generic, use semantic token)
- AlertsPanel.tsx (2 occurrences): `bg-red-50` -> `bg-red-50 dark:bg-red-950/50`, `border-red-100` -> `border-red-100 dark:border-red-900/50`, `text-red-900` -> `text-red-900 dark:text-red-200`, `bg-blue-50` -> `bg-blue-50 dark:bg-blue-950/50`, `border-blue-100` -> `border-blue-100 dark:border-blue-900/50`
- UserComparison.tsx (2 occurrences): `text-green-600` -> `text-green-600 dark:text-green-400`
- GroupDetail.tsx (2 occurrences): `border-gray-300` -> `border-border` (generic border, use semantic token)
- badge.tsx (2 occurrences): `bg-green-100 text-green-800` -> `bg-green-100 dark:bg-green-900/50 text-green-800 dark:text-green-200`, `bg-blue-100 text-blue-800` -> `bg-blue-100 dark:bg-blue-900/50 text-blue-800 dark:text-blue-200`
- StatCard.tsx (1 occurrence): `text-green-600` -> `text-green-600 dark:text-green-400`, `text-red-600` -> `text-red-600 dark:text-red-400`
- Settings.tsx (3 occurrences): `text-green-600` -> `text-green-600 dark:text-green-400`

**Decision logic:** Use `dark:` paired variants for status/semantic colors (green=success, red=error, blue=info). Use semantic tokens (`text-muted-foreground`, `border-border`) for generic grays. Reference the mapping table in research.
  </action>
  <verify>
Run `grep -rn "bg-green-100\b\|bg-blue-100\b" --include="*.tsx" frontend/src/components/ui/badge.tsx | grep -v "dark:"` -- should return 0 results.
Run `grep -rn "border-gray-300" --include="*.tsx" frontend/src/pages/groups/GroupDetail.tsx` -- should return 0 results (replaced with border-border).
Run `grep -rn "text-gray-400\b" --include="*.tsx" frontend/src/pages/admin/analytics/components/TimePeriodComparison.tsx` -- should return 0 results (replaced with text-muted-foreground).
Run `cd frontend && npx tsc --noEmit` -- no TypeScript errors.
  </verify>
  <done>All 18 hardcoded color occurrences in the 7 analytics/settings/UI files have been replaced with paired dark: variants or semantic tokens. Zero unpaired hardcoded colors remain across all 12 files.</done>
</task>

</tasks>

<verification>
1. `grep -rn "bg-green-50\b\|bg-red-50\b\|bg-yellow-50\b\|bg-blue-50\b" --include="*.tsx" frontend/src/ | grep -v "dark:" | grep -v "node_modules"` -- should only show NeedsAttention.tsx and GivingSummaryCard.tsx (already correct files)
2. `grep -rn "text-green-600\b\|text-red-600\b\|text-yellow-600\b\|text-blue-600\b" --include="*.tsx" frontend/src/ | grep -v "dark:" | grep -v "node_modules"` -- should return 0 or only already-correct files
3. `grep -rn "border-gray-300" --include="*.tsx" frontend/src/ | grep -v "node_modules"` -- should return 0 results
4. `cd frontend && npx tsc --noEmit` passes
5. `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- All 50 hardcoded color occurrences across 12 files are fixed with dark: variants or semantic tokens
- NeedsAttention.tsx pattern is consistently applied (paired dark: variants for status colors)
- No TypeScript errors introduced
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/21-dark-mode-ui-polish/21-01-SUMMARY.md`
</output>
