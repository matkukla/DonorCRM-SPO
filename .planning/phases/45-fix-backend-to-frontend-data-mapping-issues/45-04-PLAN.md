---
phase: 45-fix-backend-to-frontend-data-mapping-issues
plan: 04
type: execute
wave: 3
depends_on:
  - 45-03
files_modified: []
autonomous: false
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - ORG-05

must_haves:
  truths:
    - "Org contacts show their organization name (not blank) in the contact list Name column"
    - "Searching by org name finds the org contact"
    - "Contact create form allows submitting with only organization_name"
    - "Contact detail view shows organization_name in the Contact Information card"
    - "Editing an org contact shows the org name pre-filled and saves correctly"
---

<objective>
Human verification that org-type contacts work end-to-end in the running application. The backend tests confirm API correctness; this checkpoint confirms the UI renders and behaves correctly.

Purpose: Manual verification required because React rendering, form interaction, and visual correctness cannot be confirmed by pytest or tsc.
Output: User approval to close Phase 45.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-backend-to-frontend-data-mapping-issues/45-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full-stack org-contact support:
    - Backend: Contact.full_name falls back to organization_name; ContactListSerializer/DetailSerializer/CreateSerializer all include organization_name; both search endpoints include organization_name; CSV export uses full_name property
    - Frontend: ContactListItem + ContactCreate TypeScript interfaces include organization_name; ContactForm has Organization Name input + relaxed validation; ContactDetail shows organization_name in Contact Information card
  </what-built>
  <how-to-verify>
    Start the dev server if not running:
    - Backend: `python manage.py runserver` (from project root)
    - Frontend: `npm run dev` (from frontend/)

    Run through these 4 checks:

    **Check 1 — Contact list shows org name:**
    Navigate to /contacts. If any org-type contacts exist (imported via RE with blank first/last), verify they show their organization name in the Name column instead of blank.
    If no org contacts exist yet, skip to Check 2 to create one first.

    **Check 2 — Create an org contact:**
    Navigate to /contacts/new.
    - Verify "Organization Name" input field appears ABOVE First Name / Last Name in the Basic Information section.
    - Leave First Name and Last Name blank.
    - Enter an Organization Name: "Test Org Ltd"
    - Click "Create Contact".
    - Expected: contact is created successfully (no validation error blocking submission).

    **Check 3 — Org contact in list:**
    After creating "Test Org Ltd", navigate back to /contacts.
    Verify the contact appears with "Test Org Ltd" in the Name column (not blank).
    Search for "Test Org" in the search box. Verify the contact appears in results.

    **Check 4 — Org contact detail view:**
    Click on "Test Org Ltd" contact.
    - Verify the page header shows "Test Org Ltd" (full_name fallback working).
    - Verify "Test Org Ltd" appears in the Contact Information card with a building icon.
    - Click Edit.
    - Verify Organization Name field is pre-filled with "Test Org Ltd".
    - Change it to "Test Org LLC", save.
    - Verify the change persists after returning to the detail page.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all 4 manual-only behaviors from VALIDATION.md:
- Contact list shows org name in Name column
- Create form accepts org name without first/last
- Organization name field editable and pre-populated on edit
- Detail view shows organization_name
</verification>

<success_criteria>
User types "approved" after verifying all 4 checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/45-fix-backend-to-frontend-data-mapping-issues/45-04-SUMMARY.md`
</output>
