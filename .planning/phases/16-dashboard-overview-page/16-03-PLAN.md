---
phase: 16-dashboard-overview-page
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - frontend/src/pages/admin/analytics/components/TrendCharts.tsx
  - frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard Overview displays a line chart showing team metrics over past 12 weeks"
    - "Dashboard Overview displays a funnel chart showing pipeline stage distribution with counts"
    - "Dashboard Overview displays Team Activity Table with sortable columns"
    - "Dashboard Overview displays Alerts Panel with coaching prompts"
    - "Dashboard Overview displays summary cards (total contacts, active journals, stalled contacts, conversion rate)"
    - "All 5 widgets are visible on one page with responsive grid layout"
  artifacts:
    - path: "frontend/src/pages/admin/analytics/components/TrendCharts.tsx"
      provides: "12-week line chart widget"
      contains: "TrendCharts"
      min_lines: 30
    - path: "frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx"
      provides: "Pipeline funnel chart widget"
      contains: "ConversionFunnelChart"
      min_lines: 30
    - path: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      provides: "Complete dashboard page with all widgets"
      contains: "TeamActivityTable|AlertsPanel|TrendCharts|ConversionFunnelChart"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/admin/analytics/components/TrendCharts.tsx"
      to: "frontend/src/hooks/useInsights.ts"
      via: "useAdminTeamTrends hook"
      pattern: "useAdminTeamTrends"
    - from: "frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx"
      to: "frontend/src/hooks/useInsights.ts"
      via: "useAdminConversionFunnel hook"
      pattern: "useAdminConversionFunnel"
    - from: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      to: "frontend/src/pages/admin/analytics/components/"
      via: "imports all 4 widget components"
      pattern: "import.*TeamActivityTable|import.*AlertsPanel|import.*TrendCharts|import.*ConversionFunnelChart"
---

<objective>
Create the Trend Charts and Conversion Funnel Chart widgets, then wire all 5 widgets into the AdminAnalyticsDashboard page.

Purpose: This plan completes Phase 16 by creating the remaining two chart widgets (DASH-04 trend chart, PIPE-01 funnel chart) and assembling all widgets into the dashboard page. The existing AdminAnalyticsDashboard.tsx currently shows summary cards and a funnel table -- this plan replaces the table with a proper funnel chart and adds the trend chart, team activity table, and alerts panel.

Output: A complete Dashboard Overview page with 5 functional widgets: summary cards, Team Activity Table, Alerts Panel, Trend Charts, and Conversion Funnel Chart.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Plan 01 creates: useAdminTeamTrends hook and TrendDataPoint types
@.planning/phases/16-dashboard-overview-page/16-01-SUMMARY.md

# Plan 02 creates: TeamActivityTable and AlertsPanel components
@.planning/phases/16-dashboard-overview-page/16-02-SUMMARY.md

# Current dashboard page to be enhanced
@frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx

# Chart patterns to follow
@frontend/src/pages/journals/components/ReportCharts.tsx
@frontend/src/components/ui/chart.tsx

# Data hooks
@frontend/src/hooks/useInsights.ts
@frontend/src/api/insights.ts

# Research on chart patterns
@.planning/phases/16-dashboard-overview-page/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrendCharts and ConversionFunnelChart widget components</name>
  <files>
    frontend/src/pages/admin/analytics/components/TrendCharts.tsx
    frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx
  </files>
  <action>
    **TrendCharts.tsx** -- 12-week multi-line chart (DASH-04):

    Create `frontend/src/pages/admin/analytics/components/TrendCharts.tsx`:
    - Import: LineChart, Line, XAxis, YAxis, CartesianGrid from recharts
    - Import: ChartContainer, ChartTooltip, ChartTooltipContent, type ChartConfig from @/components/ui/chart
    - Import: Card, CardContent, CardDescription, CardHeader, CardTitle from @/components/ui/card
    - Import: useAdminTeamTrends from @/hooks/useInsights

    Define chart config outside component (following ReportCharts.tsx pattern):
    ```typescript
    const trendConfig = {
      decisions_logged: { label: "Decisions", color: "hsl(var(--chart-1))" },
      donations_received: { label: "Donations", color: "hsl(var(--chart-2))" },
      stage_progressions: { label: "Stage Changes", color: "hsl(var(--chart-3))" },
    } satisfies ChartConfig
    ```

    Component:
    - Fetch: `const { data, isLoading } = useAdminTeamTrends()`
    - Loading: ChartSkeleton (reuse inline skeleton pattern from ReportCharts.tsx -- Card with min-h-[300px] bg-muted rounded animate-pulse)
    - Empty: Show "No trend data available" centered in card
    - Render: Card > CardHeader (title "Team Metrics (12 Weeks)", description "Decisions, donations, and stage progressions") > CardContent > ChartContainer > LineChart
    - LineChart with 3 Line components for decisions_logged, donations_received, stage_progressions
    - XAxis dataKey="week_label", tickLine={false}, tickMargin={10}, axisLine={false}
    - YAxis with tickLine={false}, axisLine={false}
    - ChartTooltip with ChartTooltipContent
    - All Lines: strokeWidth={2}, dot={{ r: 4 }}, isAnimationActive={false} (per research pitfall #6 -- avoid animation on line charts, dashboard has many widgets)

    Export: `export function TrendCharts()`

    **ConversionFunnelChart.tsx** -- Pipeline funnel visualization (PIPE-01):

    Create `frontend/src/pages/admin/analytics/components/ConversionFunnelChart.tsx`:
    - Import: FunnelChart, Funnel, LabelList, Tooltip from recharts
    - Import: ChartContainer from @/components/ui/chart
    - Import: Card, CardContent, CardDescription, CardHeader, CardTitle from @/components/ui/card
    - Import: useAdminConversionFunnel from @/hooks/useInsights
    - Import: useMemo from react

    Define funnel colors array (matching STAGE_COLORS from ReportCharts.tsx):
    ```typescript
    const FUNNEL_COLORS = [
      "hsl(var(--chart-1))",
      "hsl(var(--chart-2))",
      "hsl(var(--chart-3))",
      "hsl(var(--chart-4))",
      "hsl(var(--chart-5))",
      "hsl(var(--chart-6))",
    ]
    ```

    Component:
    - Fetch: `const { data, isLoading } = useAdminConversionFunnel()`
    - Transform data with useMemo (per research pitfall #2 -- transform data format for Recharts):
      ```typescript
      const chartData = useMemo(() => {
        if (!data?.funnel) return []
        return data.funnel.map((stage, index) => ({
          name: stage.label,
          value: stage.count,
          percentage: stage.percentage,
          fill: FUNNEL_COLORS[index % FUNNEL_COLORS.length],
        }))
      }, [data])
      ```
    - Loading: Card with skeleton (min-h-[400px] bg-muted rounded animate-pulse)
    - Empty: Show "No pipeline data" centered in card
    - Render: Card > CardHeader (title "Pipeline Funnel", description with total_contacts_in_pipeline count) > CardContent > ChartContainer (min-h-[400px]) > FunnelChart
    - FunnelChart with Funnel component: dataKey="value", data={chartData}, isAnimationActive (keep animation on funnel -- it looks good per research)
    - Custom Tooltip showing stage name, count, and percentage (follow research code example)
    - LabelList position="right" showing stage names

    Export: `export function ConversionFunnelChart()`

    Both components should define inline ChartSkeleton and EmptyChart helper functions (or copy the pattern from ReportCharts.tsx). Keep them local to avoid import overhead.
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit`
    TypeScript compiles without errors. Verify both files exist and export their components.
  </verify>
  <done>
    TrendCharts.tsx displays a 3-line chart for decisions, donations, stage progressions over 12 weeks. ConversionFunnelChart.tsx displays a Recharts FunnelChart with stage counts and labels. Both handle loading and empty states. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire all widgets into AdminAnalyticsDashboard page</name>
  <files>frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx</files>
  <action>
    Rewrite `frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx` to integrate all 5 widgets:

    Imports to add:
    - TeamActivityTable from ./components/TeamActivityTable
    - AlertsPanel from ./components/AlertsPanel
    - TrendCharts from ./components/TrendCharts
    - ConversionFunnelChart from ./components/ConversionFunnelChart

    Remove:
    - Remove the inline Table/funnel-table rendering (the HTML table that currently shows funnel data)
    - Remove the useAdminConversionFunnel hook from this file (ConversionFunnelChart handles its own data)
    - Remove the funnelData/funnelLoading variables
    - Keep useAdminDashboardOverview for the summary cards (still needed)

    Page layout (responsive grid using Tailwind):
    ```
    Admin Sub-Navigation (keep existing)

    Header: "Analytics Dashboard" with subtitle (keep existing)

    Summary Cards Row (keep existing 4-card grid)

    Donations Card (keep existing)

    Two-column grid (lg:grid-cols-2, 1 col on mobile):
      Left: <TrendCharts />
      Right: <ConversionFunnelChart />

    Two-column grid (lg:grid-cols-3, 1 col on mobile):
      Left (col-span-2): <TeamActivityTable />
      Right (col-span-1): <AlertsPanel />
    ```

    Key changes:
    - Each widget component manages its own data fetching, loading, and error states independently
    - AdminAnalyticsDashboard only fetches useAdminDashboardOverview for the summary cards
    - Remove the full-page loading/error gates that currently block all content. Instead:
      - Summary cards section shows loading skeleton only for its own data
      - Each widget handles its own loading independently (widgets load in parallel)
    - Keep the admin sub-navigation exactly as-is (Users, Import Center, Analytics NavLinks)

    Simplify the component structure:
    - The current AdminAnalyticsDashboard has 3 returns (loading, error, data). Simplify to 1 return:
      - Admin sub-nav (always visible)
      - Header (always visible)
      - Summary cards (show skeleton if isLoading, show data if available, show error if error)
      - Widget grid (always rendered -- each widget handles its own states)

    This means the page renders immediately with navigation and header, and widgets fill in as their data loads (per research pitfall #5 -- avoid inconsistent widget loading).
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit`
    TypeScript compiles without errors.
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx vite build`
    Build succeeds without errors.
  </verify>
  <done>
    AdminAnalyticsDashboard.tsx integrates all 5 widgets: summary cards, Team Activity Table, Alerts Panel, Trend Charts, Conversion Funnel Chart. Each widget loads independently. Responsive grid layout adapts from 1-column mobile to multi-column desktop. Vite build succeeds. Page no longer blocks on full data load -- navigation and header render immediately.
  </done>
</task>

</tasks>

<verification>
1. Frontend: `cd frontend && npx tsc --noEmit` -- compiles cleanly
2. Frontend: `cd frontend && npx vite build` -- builds successfully
3. Dashboard page imports all 4 widget components
4. No remaining inline funnel table (replaced by ConversionFunnelChart)
5. Each widget component fetches its own data independently
</verification>

<success_criteria>
- Dashboard Overview page displays all 5 widgets: summary cards, team activity table, alerts panel, trend charts, conversion funnel chart
- Summary cards show total contacts, active journals, stalled contacts, conversion rate (DASH-01)
- Team Activity Table is sortable by date and user (DASH-02)
- Alerts Panel shows rule-based coaching prompts with colored severity boxes (DASH-03)
- Trend Charts show 12-week line chart with decisions, donations, stage progressions (DASH-04)
- Conversion Funnel Chart shows pipeline stage distribution with counts (PIPE-01)
- Responsive layout: single column on mobile, multi-column on desktop
- TypeScript compiles and Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-overview-page/16-03-SUMMARY.md`
</output>
