---
phase: 30-data-migration-backend-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - apps/contacts/models.py
  - apps/contacts/views.py
  - apps/contacts/urls.py
  - apps/dashboard/services.py
  - apps/dashboard/views.py
  - apps/insights/services.py
  - apps/users/views.py
  - apps/imports/services.py
  - apps/imports/views.py
  - apps/imports/tasks.py
  - apps/core/management/commands/generate_sample_data.py
autonomous: true
requirements:
  - MIG-03
  - MIG-05

must_haves:
  truths:
    - "Contact.update_giving_stats() queries Gift model (not Donation) and converts cents to Decimal for stored stats"
    - "Contact.has_active_pledge and Contact.monthly_pledge_amount query RecurringGift (not Pledge)"
    - "Dashboard get_support_progress() uses RecurringGift with monthly_equivalent property"
    - "Dashboard get_giving_summary() queries Gift model with amount_cents conversion"
    - "Dashboard get_recent_gifts() returns Gift objects"
    - "Insights _scope_donations() returns Gift queryset; _scope_pledges() returns RecurringGift queryset"
    - "All insights service functions use Gift amount_cents instead of Donation amount"
    - "Users CurrentUserView queries RecurringGift for active pledge count"
    - "Late pledge features are removed (get_late_donations returns empty, late_pledges excluded from needs_attention)"
  artifacts:
    - path: "apps/contacts/models.py"
      provides: "update_giving_stats querying Gift; has_active_recurring_gift and monthly_recurring_gift_amount properties"
      contains: "self.gifts.all()"
    - path: "apps/dashboard/services.py"
      provides: "All dashboard service functions querying Gift/RecurringGift"
      contains: "from apps.gifts.models import"
    - path: "apps/insights/services.py"
      provides: "All insights service functions querying Gift/RecurringGift"
      contains: "from apps.gifts.models import"
  key_links:
    - from: "apps/contacts/models.py"
      to: "apps/gifts/models.py"
      via: "self.gifts.all() reverse relation"
      pattern: "self\\.gifts\\.all\\(\\)"
    - from: "apps/dashboard/services.py"
      to: "apps/gifts/models.py"
      via: "Gift and RecurringGift imports"
      pattern: "from apps\\.gifts\\.models import"
    - from: "apps/insights/services.py"
      to: "apps/gifts/models.py"
      via: "Gift and RecurringGift imports"
      pattern: "from apps\\.gifts\\.models import"
---

<objective>
Switch all backend services from Donation/Pledge to Gift/RecurringGift. Update Contact model properties to query new models, rewrite all dashboard and insights service functions, and update user views.

Purpose: MIG-03 requires contact stats from Gift model. MIG-05 requires all backend services (dashboard, insights, analytics, imports, users) to use Gift/RecurringGift exclusively. After this plan, no service function queries Donation or Pledge.

Output: All backend service files updated to use Gift/RecurringGift models.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-data-migration-backend-cutover/30-RESEARCH.md
@.planning/phases/30-data-migration-backend-cutover/30-01-SUMMARY.md
@apps/gifts/models.py
@apps/contacts/models.py
@apps/dashboard/services.py
@apps/insights/services.py
@apps/users/views.py
@apps/contacts/views.py
@apps/contacts/urls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Contact model and contact views to use Gift/RecurringGift</name>
  <files>
    apps/contacts/models.py
    apps/contacts/views.py
    apps/contacts/urls.py
  </files>
  <action>
1. Update `Contact.update_giving_stats()` in `apps/contacts/models.py`:
   - Change `self.donations.all()` to `self.gifts.all()`
   - Change `Sum('amount')` to `Sum('amount_cents')`, then convert: `self.total_given = Decimal(agg['total_cents'] or 0) / Decimal(100)`
   - Change `Count('id')` stays the same
   - Change `Min('date')` / `Max('date')` to `Min('gift_date')` / `Max('gift_date')`
   - For last_gift_amount: `last_gift = gifts.order_by('-gift_date').first()` then `self.last_gift_amount = last_gift.amount_dollars if last_gift else None`
   - Add `from decimal import Decimal` at the top of the method (or module level)

2. Update `Contact.has_active_pledge` property to `has_active_recurring_gift`:
   - Change `self.pledges.filter(status='active').exists()` to `self.recurring_gifts.filter(status='active').exists()`
   - Keep old property name `has_active_pledge` as an alias pointing to the new one for backward compatibility during transition (will be cleaned up in Phase 31 UI work)

3. Update `Contact.monthly_pledge_amount` property to `monthly_recurring_gift_amount`:
   - Change `self.pledges.filter(status='active')` to `self.recurring_gifts.filter(status='active')`
   - Each recurring gift already has `monthly_equivalent` property (added in Plan 01)
   - Keep old property name `monthly_pledge_amount` as alias

4. Update `ContactDonationsView` in `apps/contacts/views.py`:
   - Rename to `ContactGiftsView`
   - Change `from apps.donations.models import Donation` to `from apps.gifts.models import Gift`
   - Change queryset to `Gift.objects.filter(donor_contact_id=contact_id)` (note: `donor_contact_id` not `contact_id`)
   - Change ordering to `.order_by('-gift_date')` (not `-date`)
   - Change owner filter to `donor_contact__owner=user` (not `contact__owner`)
   - Use GiftSerializer (create a minimal one inline or import -- see below)

5. Update `ContactPledgesView` in `apps/contacts/views.py`:
   - Rename to `ContactRecurringGiftsView`
   - Change `from apps.pledges.models import Pledge` to `from apps.gifts.models import RecurringGift`
   - Change queryset to `RecurringGift.objects.filter(donor_contact_id=contact_id)`
   - Change owner filter to `donor_contact__owner=user`

6. For serializers needed by the contact views:
   - Create minimal `GiftSerializer` and `RecurringGiftSerializer` in `apps/gifts/serializers.py` if they don't exist yet. Include all fields from the Gift/RecurringGift models plus the `amount_dollars` property as a read-only field.
   - Import them in the contact views.

7. Update `apps/contacts/urls.py`:
   - Keep URL paths as `<uuid:pk>/donations/` and `<uuid:pk>/pledges/` for now (Phase 31 will rename URLs)
   - Point to the renamed view classes `ContactGiftsView` and `ContactRecurringGiftsView`
   - Update the import statements
  </action>
  <verify>
    Run `python manage.py check` to verify no system check errors. Verify `from apps.gifts.models import Gift, RecurringGift` works in the updated files.
  </verify>
  <done>
    Contact.update_giving_stats() queries Gift with cents conversion. Contact views serve Gift/RecurringGift data. Contact properties query recurring_gifts relation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard services, insights services, and user views</name>
  <files>
    apps/dashboard/services.py
    apps/dashboard/views.py
    apps/insights/services.py
    apps/users/views.py
    apps/imports/services.py
    apps/imports/views.py
    apps/imports/tasks.py
    apps/core/management/commands/generate_sample_data.py
  </files>
  <action>
**Dashboard services.py:**
1. Replace imports:
   - Remove `from apps.donations.models import Donation`
   - Remove `from apps.pledges.models import Pledge, PledgeStatus`
   - Add `from apps.gifts.models import Gift, RecurringGift, RecurringGiftStatus`

2. Update `get_needs_attention(user)`:
   - Remove `pledges` scoping and `late_pledges` query entirely (RecurringGift has no is_late field)
   - Remove `late_pledge_count` from return dict
   - Set `late_pledges: []` and `late_pledge_count: 0` as static empty values to preserve API response shape (frontend expects these keys)

3. Update `get_late_donations(user, limit=10)`:
   - Return empty list (late pledge detection not available on RecurringGift)
   - Keep function signature for API compatibility, just return `[]`

4. Update `get_support_progress(user)`:
   - Change `Pledge.objects` to `RecurringGift.objects`
   - Change `contact__owner=user` to `donor_contact__owner=user`
   - Change `PledgeStatus.ACTIVE` to `RecurringGiftStatus.ACTIVE`
   - `monthly_equivalent` property already exists on RecurringGift (from Plan 01)

5. Update `get_recent_gifts(user, days=30, limit=10)`:
   - Change `Donation.objects` to `Gift.objects`
   - Change `contact__owner=user` to `donor_contact__owner=user`
   - Change `.filter(date__gte=start_date)` to `.filter(gift_date__gte=start_date)`
   - Change `.select_related('contact')` to `.select_related('donor_contact')`

6. Update `get_giving_summary(user, year=None)`:
   - Change `Donation.objects` to `Gift.objects`
   - Change `contact__owner=user` to `donor_contact__owner=user`
   - Change `Sum('amount')` to `Sum('amount_cents')` then divide by 100 for `given`
   - Change date filters from `date__gte/date__lte` to `gift_date__gte/gift_date__lte`
   - Change `Pledge.objects` to `RecurringGift.objects`
   - Change `PledgeStatus.ACTIVE` to `RecurringGiftStatus.ACTIVE`
   - Change `contact__owner` to `donor_contact__owner`

7. Update `get_monthly_gifts(user, months=12)`:
   - Change `Donation.objects` to `Gift.objects`
   - Change `contact__owner=user` to `donor_contact__owner=user`
   - Change `Sum('amount')` to `Sum('amount_cents')` -- convert cents to dollars in the monthly_map: `float(item['total']) / 100`
   - Change `TruncMonth('date')` to `TruncMonth('gift_date')`
   - Change `filter(date__gte=start_date)` to `filter(gift_date__gte=start_date)`

8. Update `get_dashboard_summary(user)`:
   - Remove `late_donations_count` Pledge queries
   - Set `late_donations_count: 0` as static value
   - Update `recent_gifts` values() call: change `amount` to `amount_cents`, `date` to `gift_date`, `contact_id` to `donor_contact_id`, `contact__first_name` to `donor_contact__first_name`, `contact__last_name` to `donor_contact__last_name`

**Dashboard views.py:**
- Remove any imports of `PledgeSerializer` or `DonationSerializer` if present
- If views reference these serializers directly, update to Gift equivalents

**Insights services.py:**
1. Replace imports:
   - Remove `from apps.donations.models import Donation`
   - Remove `from apps.pledges.models import Pledge, PledgeStatus`
   - Add `from apps.gifts.models import Gift, RecurringGift, RecurringGiftStatus`

2. Rename and update `_scope_donations(user)` to `_scope_gifts(user)`:
   - `Gift.objects.all()` / `Gift.objects.filter(donor_contact__owner=user)`

3. Rename and update `_scope_pledges(user)` to `_scope_recurring_gifts(user)`:
   - `RecurringGift.objects.all()` / `RecurringGift.objects.filter(donor_contact__owner=user)`

4. Update `get_donations_by_month(user, year=None)`:
   - Use `_scope_gifts(user)` instead of `_scope_donations(user)`
   - Change `Sum('amount')` to `Sum('amount_cents')`, convert in map: `float(item['total'] or 0) / 100`
   - Change `TruncMonth('date')` to `TruncMonth('gift_date')`
   - Change `filter(date__year=year)` to `filter(gift_date__year=year)`

5. Update `get_donations_by_year(user, years=5)`:
   - Same pattern: `_scope_gifts`, `amount_cents`, `gift_date`, divide by 100

6. Update `get_monthly_commitments(user)`:
   - Use `_scope_recurring_gifts(user)`, `RecurringGiftStatus.ACTIVE`
   - Change `pledge.contact` to `rg.donor_contact`
   - Change `float(pledge.amount)` to `float(rg.amount_dollars)`
   - Remove `last_fulfilled_date` from output (RecurringGift doesn't have it)

7. Update `get_late_donations(user, limit=50)`:
   - Return empty structure `{'late_donations': [], 'total_count': 0}` (RecurringGift has no is_late)

8. Update `get_transactions(user, limit=100, offset=0, contact_id=None, date_from=None, date_to=None)`:
   - Change `Donation.objects` to `Gift.objects`
   - Change `.select_related('contact', 'pledge')` to `.select_related('donor_contact')`
   - Change date filters: `date__gte` to `gift_date__gte`, `date__lte` to `gift_date__lte`
   - Change contact filter: `contact_id` to `donor_contact_id`
   - Update dict keys: `d.contact` to `d.donor_contact`, `d.amount` to `float(d.amount_dollars)`, `d.date` to `d.gift_date`
   - Remove `donation_type`, `payment_method`, `pledge_id`, `thanked` from output (Gift doesn't have these)

9. Update `get_dashboard_overview(date_from=None, date_to=None)`:
   - Change `Donation.objects` to `Gift.objects`
   - Change `filter(date__gte=...)` to `filter(gift_date__gte=...)`
   - Change `Sum('amount')` to `Sum('amount_cents')`, convert result: `float((donation_stats['total_amount'] or 0)) / 100`

10. Update `get_team_trends(weeks=12, ...)`:
    - Change Donation queryset to Gift: `Gift.objects.filter(gift_date__gte=start_date)` with `TruncWeek('gift_date')`

11. Update `get_user_trends(user_id, weeks=12)`:
    - Same pattern: `Gift.objects.filter(donor_contact__owner_id=user_id, gift_date__gte=start_date)` with `TruncWeek('gift_date')`

12. Update `get_user_performance()`:
    - Change donation subqueries to use `Gift`: `Gift.objects.filter(donor_contact__owner=OuterRef('pk'))`
    - Change `Sum('amount')` to `Sum('amount_cents')` in subquery, then `float(user.total_donation_amount or 0) / 100` in result

13. Update `get_user_drilldown(user_id)`:
    - Change `Donation.objects.filter(contact__owner_id=user_id)` to `Gift.objects.filter(donor_contact__owner_id=user_id)`
    - Change `Sum('amount')` to `Sum('amount_cents')`, convert: `float((donation_stats['total_amount'] or 0)) / 100`

**Users views.py:**
- Change `from apps.pledges.models import PledgeStatus` to `from apps.gifts.models import RecurringGiftStatus`
- Change Pledge query to `RecurringGift.objects.filter(donor_contact__owner=OuterRef('pk'), status=RecurringGiftStatus.ACTIVE)` (note: `donor_contact__owner` not `contact__owner`)

**Imports services.py, views.py, tasks.py:**
- The old SPO CSV import functions in `apps/imports/services.py` reference `Donation`, `DonationType`, `PaymentMethod`, `Pledge`, `PledgeFrequency`, `PledgeStatus`. These are the v1.0 import pipeline that is superseded by Phase 28-29 RE imports.
- Remove or comment out the old SPO-specific import functions that create Donation/Pledge objects. Keep the generic utility functions and RE import functions intact.
- In `apps/imports/views.py`: Update `ExportDonationsView` to export Gift data instead of Donation data (change queryset, field names)
- In `apps/imports/tasks.py`: Remove or update `import_donations_async` that references Donation

**generate_sample_data.py:**
- Update to create Gift and RecurringGift objects instead of Donation and Pledge objects
- Use `amount_cents=int(amount * 100)`, `gift_date` instead of `date`, `donor_contact` instead of `contact`
  </action>
  <verify>
    Run `python manage.py check` for system checks. Run `grep -r "from apps.donations" apps/ --include="*.py" | grep -v migrations | grep -v tests | grep -v donations/` to verify no non-test, non-migration code outside apps/donations/ still imports from donations. Same for pledges: `grep -r "from apps.pledges" apps/ --include="*.py" | grep -v migrations | grep -v tests | grep -v pledges/`.
  </verify>
  <done>
    All dashboard, insights, user, import, and contact service functions query Gift/RecurringGift exclusively. No service function outside apps/donations/ or apps/pledges/ imports from those old apps (except test files which will be cleaned in Plan 03).
  </done>
</task>

</tasks>

<verification>
1. `grep -r "from apps.donations" apps/ --include="*.py" | grep -v migrations | grep -v tests | grep -v donations/` returns NO results
2. `grep -r "from apps.pledges" apps/ --include="*.py" | grep -v migrations | grep -v tests | grep -v pledges/` returns NO results
3. Contact.update_giving_stats() uses `self.gifts.all()` with cents conversion
4. Dashboard get_support_progress() uses RecurringGift.monthly_equivalent
5. All insights service functions use Gift.amount_cents with /100 conversion
6. `python manage.py check` passes
</verification>

<success_criteria>
- Zero imports of Donation/Pledge models outside their own apps (excluding tests/migrations)
- Contact stats calculated from Gift model with correct cents-to-Decimal conversion
- Dashboard and insights services fully functional with new models
- Late pledge features gracefully return empty data (no errors)
- API response shapes preserved for frontend compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/30-data-migration-backend-cutover/30-02-SUMMARY.md`
</output>
