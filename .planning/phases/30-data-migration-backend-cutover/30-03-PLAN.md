---
phase: 30-data-migration-backend-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - apps/donations/
  - apps/pledges/
  - config/settings/base.py
  - config/api_urls.py
  - config/celery.py
  - apps/gifts/migrations/0004_remove_donation_pledge_apps.py
autonomous: true
requirements:
  - MIG-04

must_haves:
  truths:
    - "apps/donations/ directory is completely removed from the codebase"
    - "apps/pledges/ directory is completely removed from the codebase"
    - "INSTALLED_APPS no longer lists apps.donations or apps.pledges"
    - "api_urls.py routes /gifts/ to apps.gifts.urls (with /donations/ and /pledges/ as backward-compatible aliases)"
    - "Celery beat schedule no longer references apps.pledges.tasks.check_late_pledges"
    - "python manage.py check passes with no errors"
  artifacts:
    - path: "config/settings/base.py"
      provides: "INSTALLED_APPS without donations or pledges"
    - path: "config/api_urls.py"
      provides: "URL routes with gifts/ (and donations/, pledges/ aliases) pointing to apps.gifts.urls"
    - path: "config/celery.py"
      provides: "Beat schedule without check-late-pledges-daily"
  key_links:
    - from: "config/settings/base.py"
      to: "apps/gifts/"
      via: "INSTALLED_APPS includes apps.gifts (replacement for donations/pledges)"
      pattern: "'apps\\.gifts'"
    - from: "config/api_urls.py"
      to: "apps/gifts/urls.py"
      via: "URL include for Gift/RecurringGift endpoints"
      pattern: "include\\('apps\\.gifts\\.urls'\\)"
---

<objective>
Remove the old Donation and Pledge apps entirely from the codebase after verifying all services have been cut over. Remove INSTALLED_APPS entries, URL routes, Celery beat schedule, and the app directories themselves.

Purpose: MIG-04 requires old models to be removed after migration verification. This is the final cleanup step that completes the Donation/Pledge to Gift/RecurringGift transition.

Output: Clean codebase with no Donation/Pledge references outside of git history.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-data-migration-backend-cutover/30-RESEARCH.md
@.planning/phases/30-data-migration-backend-cutover/30-02-SUMMARY.md
@config/settings/base.py
@config/api_urls.py
@config/celery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old apps and update configuration</name>
  <files>
    config/settings/base.py
    config/api_urls.py
    config/celery.py
    apps/events/models.py
  </files>
  <action>
1. **config/settings/base.py:**
   - Remove `'apps.donations'` from LOCAL_APPS list
   - Remove `'apps.pledges'` from LOCAL_APPS list
   - Update SPECTACULAR_SETTINGS if it has donation/pledge tags -- remove those tags
   - Leave `'apps.gifts'` in LOCAL_APPS (already there from Phase 27)

2. **config/api_urls.py:**
   - Remove `path('donations/', include('apps.donations.urls')),`
   - Remove `path('pledges/', include('apps.pledges.urls')),`
   - Add `path('gifts/', include('apps.gifts.urls')),` (urls.py was created in Plan 30-02 Task 3)
   - Add backward-compatible aliases that point to the same views so the frontend continues working until Phase 31 renames them:
     - `path('donations/', include('apps.gifts.urls')),` (alias -- old path still works)
     - `path('pledges/', include('apps.gifts.urls')),` (alias -- old path still works)

3. **config/celery.py:**
   - Remove the `'check-late-pledges-daily'` entry from `app.conf.beat_schedule` (references `apps.pledges.tasks.check_late_pledges`)
   - Keep other beat schedule entries intact

4. **apps/events/models.py:**
   - Keep existing EventType values (DONATION_RECEIVED, FIRST_DONATION, PLEDGE_CREATED, PLEDGE_UPDATED, PLEDGE_LATE, PLEDGE_CANCELLED) -- these are stored in the database and renaming would orphan existing Event records per research recommendation
   - Do NOT add new event types -- the existing names are fine as historical labels

5. **Verify no remaining references:**
   - Run comprehensive grep: `grep -rn "from apps.donations\|from apps.pledges\|apps\.donations\|apps\.pledges" --include="*.py" apps/ config/` excluding migrations
   - Any remaining references must be in migration files only (which is expected and correct)
  </action>
  <verify>
    Run `python manage.py check` to verify Django still starts without the old apps. Run `grep -rn "apps.donations\|apps.pledges" config/ --include="*.py"` to confirm zero references in config files.
  </verify>
  <done>
    Configuration files no longer reference donations or pledges apps. Celery beat schedule clean. EventType names preserved for existing records.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete old app directories and create cleanup migration</name>
  <files>
    apps/donations/
    apps/pledges/
    apps/gifts/migrations/0004_remove_donation_pledge_apps.py
  </files>
  <action>
1. **Delete entire `apps/donations/` directory:**
   - This removes: models.py, views.py, serializers.py, filters.py, export_views.py, signals.py, urls.py, admin.py, apps.py, tests/, migrations/
   - WARNING: Do NOT delete the migrations directory yet -- Django needs the migration history. Actually, since we are removing the app from INSTALLED_APPS, Django will no longer track these migrations. The migration history rows remain in the django_migrations table but are harmless.
   - Proceed with full directory removal.

2. **Delete entire `apps/pledges/` directory:**
   - Same as above: full directory removal including all files and subdirectories

3. **Clean up any test files that imported from old apps:**
   - Check `apps/dashboard/tests/` for DonationFactory/PledgeFactory imports
   - Check `apps/insights/tests/` for DonationFactory imports
   - Check `apps/imports/tests/` for Donation/Pledge imports
   - Update these test files to import from `apps/gifts/` models and create appropriate GiftFactory/RecurringGiftFactory if they don't exist
   - Create `apps/gifts/tests/factories.py` with `GiftFactory` and `RecurringGiftFactory` using factory_boy:
     - GiftFactory: `donor_contact = factory.SubFactory(ContactFactory)`, `amount_cents = factory.Faker('random_int', min=100, max=100000)`, `gift_date = factory.Faker('date_object')`, etc.
     - RecurringGiftFactory: similar with `frequency = 'monthly'`, `status = 'active'`, `start_date = factory.Faker('date_object')`

4. **Final verification:**
   - `python manage.py check` -- must pass
   - `python manage.py showmigrations` -- donations/pledges should NOT appear (apps removed from INSTALLED_APPS)
   - `grep -rn "from apps.donations\|from apps.pledges" apps/ --include="*.py"` -- should return ZERO results (no migration files to exclude since apps are deleted)
  </action>
  <verify>
    Run `python manage.py check` to verify no errors. Run `ls apps/donations/ 2>/dev/null; echo $?` should return exit code 2 (directory not found). Same for `ls apps/pledges/ 2>/dev/null; echo $?`. Run `grep -rn "from apps.donations\|from apps.pledges" apps/ --include="*.py"` to verify zero results.
  </verify>
  <done>
    apps/donations/ and apps/pledges/ directories deleted. All test files updated to use Gift/RecurringGift factories. python manage.py check passes. No remaining references to old apps in the codebase.
  </done>
</task>

</tasks>

<verification>
1. `ls apps/donations/ 2>&1` returns "No such file or directory"
2. `ls apps/pledges/ 2>&1` returns "No such file or directory"
3. `grep -rn "apps.donations\|apps.pledges" apps/ config/ --include="*.py"` returns zero results
4. `python manage.py check` passes
5. `python manage.py showmigrations` does NOT list donations or pledges
6. Celery beat schedule has no check-late-pledges entry
</verification>

<success_criteria>
- Old Donation and Pledge apps completely removed from codebase
- Django starts and passes all system checks without old apps
- No import errors or missing module references
- Test factories created for Gift/RecurringGift models
- Configuration files clean of all donation/pledge references
</success_criteria>

<output>
After completion, create `.planning/phases/30-data-migration-backend-cutover/30-03-SUMMARY.md`
</output>
