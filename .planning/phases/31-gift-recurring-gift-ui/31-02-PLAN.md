---
phase: 31-gift-recurring-gift-ui
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - frontend/src/pages/donations/DonationList.tsx
  - frontend/src/pages/donations/DonationDetail.tsx
  - frontend/src/pages/donations/DonationForm.tsx
  - frontend/src/api/donations.ts
  - frontend/src/hooks/useDonations.ts
autonomous: true
requirements: [UI-GIFT-01, UI-GIFT-03, UI-GIFT-05]

must_haves:
  truths:
    - "Donations list page loads data from Gift model via /donations/ API and displays correct columns (Donor Name, Amount, Date, Fund, Description)"
    - "Clicking a donation row opens a slide-in detail panel showing donation details and solicitor credits"
    - "Solicitor credits table is hidden when a donation has no credits"
    - "Donation list filters (search, date range, amount range, fund, owner) work with Gift model field names"
    - "Record Donation form creates a gift by sending amount_cents to the API"
    - "CSV export from Donations list page works"
  artifacts:
    - path: "frontend/src/pages/donations/DonationList.tsx"
      provides: "Donations list page using Gift model data with slide-in detail panel"
      min_lines: 100
    - path: "frontend/src/pages/donations/DonationDetail.tsx"
      provides: "Slide-in detail panel (DonationDetailPanel) with solicitor credits"
      contains: "SheetContent"
    - path: "frontend/src/pages/donations/DonationForm.tsx"
      provides: "Donation form that creates Gift records with amount_cents conversion"
      contains: "amount_cents"
  key_links:
    - from: "frontend/src/pages/donations/DonationList.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useGifts hook"
      pattern: "useGifts"
    - from: "frontend/src/pages/donations/DonationList.tsx"
      to: "frontend/src/hooks/useFilterParams.ts"
      via: "giftFilterParsers"
      pattern: "giftFilterParsers"
    - from: "frontend/src/pages/donations/DonationDetail.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useGift hook returning GiftWithCredits"
      pattern: "useGift"
---

<objective>
Rewrite the Donations list page, detail view, and form to use the Gift model API while keeping all visible text as "Donations".

Purpose: The existing DonationList, DonationDetail, and DonationForm reference old Donation model fields (donation_type, payment_method, thanked, contact/contact_name). These need to be rewritten to use Gift model fields (amount_dollars, gift_date, donor_contact, description, fund_name) and the new hooks/API from Plan 01. The detail page changes from a full-page view to a slide-in panel with solicitor credit display (new feature). All visible labels remain "Donations" per user decision.

Output: Working Donations list page with filters, slide-in detail panel with credits, and donation create/edit form
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md

@frontend/src/pages/donations/DonationList.tsx
@frontend/src/pages/donations/DonationDetail.tsx
@frontend/src/pages/donations/DonationForm.tsx
@frontend/src/api/gifts.ts
@frontend/src/hooks/useGifts.ts
@frontend/src/hooks/useFilterParams.ts
@frontend/src/lib/filter-presets.ts
@frontend/src/pages/admin/analytics/components/UserDrilldownPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DonationList.tsx and DonationDetail.tsx for Gift model with slide-in panel</name>
  <files>
    frontend/src/pages/donations/DonationList.tsx
    frontend/src/pages/donations/DonationDetail.tsx
    frontend/src/api/donations.ts
    frontend/src/hooks/useDonations.ts
  </files>
  <action>
CRITICAL: All visible text stays as "Donations". Page title = "Donations", button = "Record Donation", empty state = "No donations recorded yet." NO renaming. URL path stays /donations.

1. **frontend/src/pages/donations/DonationList.tsx** -- Rewrite to use Gift model:
   - Replace imports: use `useGifts` from `@/hooks/useGifts`, `giftFilterParsers` from `@/hooks/useFilterParams`, `giftPresets` from `@/lib/filter-presets`, types from `@/api/gifts`.
   - Remove: `useMarkDonationThanked`, `donationTypeLabels`, `paymentMethodLabels`, donation_type/payment_method/thanked columns and filters.
   - Add state for slide-in panel: `const [selectedGiftId, setSelectedGiftId] = useState<string | null>(null)`
   - Column definitions (ColumnDef<Gift>):
     - "Donor Name": `donor_contact_name`, render as Link to `/contacts/${row.original.donor_contact}` with click stopPropagation
     - "Amount": `amount_dollars`, render with formatCurrency
     - "Date": `gift_date`, render with formatLocalDate (from @/lib/utils, handles UTC bug)
     - "Fund": `fund_name`, render as text or "---" if null
     - "Description": `description`, render truncated (max 40 chars)
   - Remove the actions column (no more thank/edit dropdown -- clicking row opens panel)
   - Filter bar changes:
     - Remove: donation_type dropdown, payment_method dropdown, thanked dropdown
     - Keep: search input (search param), date range inputs (gift_date_after/gift_date_before), amount range (min_amount/max_amount), fund dropdown, admin owner dropdown
     - filterLabels: search="Search", gift_date_after="From", gift_date_before="To", min_amount="Min Amount", max_amount="Max Amount", fund="Fund", owner="Owner"
     - exportUrl: "/donations/export/csv/" (same path, now hits Gift export via alias)
   - onRowClick: `setSelectedGiftId(gift.id)` instead of navigating to detail page
   - After DataTable, render: `<DonationDetailPanel open={!!selectedGiftId} giftId={selectedGiftId} onClose={() => setSelectedGiftId(null)} />`
   - Import DonationDetailPanel from `./DonationDetail`

2. **frontend/src/pages/donations/DonationDetail.tsx** -- Convert from full page to slide-in panel:
   - Rename the default export to a named export: `export function DonationDetailPanel({ open, giftId, onClose }: DonationDetailPanelProps)`
   - Also keep a default export wrapper that reads id from URL params and renders the panel (for backward compatibility with /donations/:id route):
     ```typescript
     export default function DonationDetail() {
       const { id } = useParams<{ id: string }>()
       const navigate = useNavigate()
       return <DonationDetailPanel open={true} giftId={id || null} onClose={() => navigate("/donations")} />
     }
     ```
   - Panel structure (follow UserDrilldownPanel pattern exactly):
     ```
     <Sheet open={open} onOpenChange={(isOpen) => !isOpen && onClose()}>
       <SheetContent side="right" className="w-full sm:max-w-lg overflow-y-auto">
     ```
   - Use `useGift(giftId)` from `@/hooks/useGifts` (returns GiftWithCredits)
   - Loading state: skeleton shimmer (same pattern as UserDrilldownPanel)
   - Header: SheetTitle = formatCurrency(gift.amount_dollars), SheetDescription = "from {donor_contact_name} on {formatLocalDate(gift.gift_date)}"
   - Details section:
     - Donor: Link to `/contacts/${gift.donor_contact}`
     - Date: formatLocalDate(gift.gift_date)
     - Fund: gift.fund_name or "No fund"
     - Description: gift.description or "No description"
     - External ID: gift.external_gift_id (only if non-empty)
   - **Solicitor Credits section** (ONLY if gift.credits && gift.credits.length > 0):
     - Heading: "Solicitor Credits"
     - Use Table/TableHeader/TableBody/TableRow/TableCell from shadcn
     - Columns: Solicitor Name | Amount | Percentage
     - Percentage = (credit.amount_cents / gift.amount_cents * 100).toFixed(1) + "%"
     - If no credits: hide this entire section (do NOT show an empty table)
   - Action buttons:
     - Edit button -> navigate to `/donations/${giftId}/edit`
     - Delete button -> useDeleteGift mutation, on success close panel
   - Timestamps: Created / Updated at bottom

3. **frontend/src/api/donations.ts** -- Simplify to re-export from gifts.ts for backward compatibility:
   - Replace the entire file contents with re-exports from gifts.ts:
     ```typescript
     // Backward compatibility -- re-export Gift types as Donation names
     // This file will be removed once all consumers are migrated
     export { getGifts as getDonations, getGift as getDonation, createGift as createDonation, updateGift as updateDonation, deleteGift as deleteDonation } from "./gifts"
     export type { Gift as Donation, GiftCreate as DonationCreate, GiftUpdate as DonationUpdate } from "./gifts"
     ```

4. **frontend/src/hooks/useDonations.ts** -- Simplify to re-export from useGifts.ts:
   - Replace with re-exports:
     ```typescript
     export { useGifts as useDonations, useGift as useDonation, useCreateGift as useCreateDonation, useUpdateGift as useUpdateDonation, useDeleteGift as useDeleteDonation } from "./useGifts"
     ```
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -20` should show no type errors.
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npm run build 2>&1 | tail -5` should succeed.
Manually verify: DonationList page still says "Donations" in the title, URL is /donations, columns are Donor Name | Amount | Date | Fund | Description.
  </verify>
  <done>
Donations list page loads Gift model data with correct columns. Clicking a row opens a slide-in panel showing donation details. Panel shows solicitor credits table when credits exist and hides it when they don't. All filters work with Gift model field names. Page title and all visible text says "Donations". CSV export works via /donations/export/csv/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DonationForm.tsx for Gift model with amount_cents conversion</name>
  <files>
    frontend/src/pages/donations/DonationForm.tsx
  </files>
  <action>
CRITICAL: All visible text stays as "Donations". Form title = "Record Donation" / "Edit Donation". NO renaming.

Rewrite DonationForm.tsx to create/update Gift records:
- Replace imports: use `useGift`, `useCreateGift`, `useUpdateGift` from `@/hooks/useGifts`, types from `@/api/gifts`
- Remove: donation_type and payment_method dropdowns entirely (Gift model has no donation_type or payment_method fields)
- Form data state: `{ donor_contact: string, amount: string, gift_date: string, description: string }`
  - Note: `amount` is the dollar string the user types. Convert to `amount_cents` on submit.
  - `gift_date` instead of `date`
  - `description` instead of `notes`
- On submit:
  - Convert amount: `amount_cents: Math.round(parseFloat(formData.amount) * 100)`
  - Submit: `{ donor_contact: formData.donor_contact, amount_cents, gift_date: formData.gift_date, description: formData.description }`
- When editing, load existing gift with `useGift(id)` and populate:
  - `amount` = gift.amount_dollars (display value)
  - `gift_date` = gift.gift_date
  - `description` = gift.description
  - `donor_contact` = gift.donor_contact, `selectedContact = { id: gift.donor_contact, name: gift.donor_contact_name }`
- After successful create/edit, navigate to `/donations` (list page, not detail page since detail is now a panel)
- Keep: Contact search/selection (same pattern), amount input with $ prefix, date input
- Add: Description textarea (replacing notes)
- Remove: donation_type dropdown, payment_method dropdown
- Card titles: "Donor" card title stays. "Donation Details" card title stays. "Notes" card -> "Description" card.
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -20` should show no type errors.
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npm run build 2>&1 | tail -5` should succeed.
  </verify>
  <done>
DonationForm creates Gift records by converting dollar input to amount_cents. Form fields are: contact selector, amount ($), date, description. No donation_type or payment_method fields. Edit mode loads existing Gift data correctly. All visible text says "Donation".
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds with no errors
2. Donations list page shows columns: Donor Name | Amount | Date | Fund | Description
3. Clicking a row opens slide-in panel with gift details
4. Panel shows solicitor credits table when credits exist
5. Panel hides credits section when no credits
6. Search filter works (searches donor name and description)
7. Date range, amount range, fund, and owner filters work
8. CSV export downloads successfully
9. Record Donation form works with amount_cents conversion
10. Edit Donation form loads existing data correctly
</verification>

<success_criteria>
- Donations list page fully rewired to Gift model with no visible terminology changes
- Slide-in detail panel shows solicitor credits per locked decision
- Credits section hidden when no credits per locked decision
- DonationForm creates gifts with correct amount_cents conversion
- All URLs remain /donations/* -- no path changes
- CSV export works via /donations/export/csv/
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-02-SUMMARY.md`
</output>
