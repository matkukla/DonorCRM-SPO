---
phase: 31-gift-recurring-gift-ui
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - frontend/src/pages/gifts/GiftList.tsx
  - frontend/src/pages/gifts/GiftDetailPanel.tsx
  - frontend/src/pages/gifts/GiftForm.tsx
autonomous: true
requirements: [UI-GIFT-01, UI-GIFT-03, UI-GIFT-05]

must_haves:
  truths:
    - "User can view a paginated list of gifts with Donor Name, Amount, Gift Date, Fund, Description columns"
    - "User can search gifts by donor name and filter by date range, amount range, and owner (admin)"
    - "Clicking a gift row opens a slide-in panel showing gift details and solicitor credit breakdown"
    - "Gift detail panel hides credits section when no solicitor credits exist"
    - "User can create and edit gifts with dollar-to-cents conversion"
  artifacts:
    - path: "frontend/src/pages/gifts/GiftList.tsx"
      provides: "Gifts list page with FilterBar, DataTable, search, and slide-in panel trigger"
      min_lines: 150
    - path: "frontend/src/pages/gifts/GiftDetailPanel.tsx"
      provides: "Sheet slide-in panel with gift details and conditional solicitor credit table"
      min_lines: 80
    - path: "frontend/src/pages/gifts/GiftForm.tsx"
      provides: "Gift create/edit form with dollar input converting to amount_cents"
      min_lines: 80
  key_links:
    - from: "frontend/src/pages/gifts/GiftList.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useGifts hook for list data"
      pattern: "useGifts"
    - from: "frontend/src/pages/gifts/GiftList.tsx"
      to: "frontend/src/pages/gifts/GiftDetailPanel.tsx"
      via: "Sheet open state triggered by row click"
      pattern: "GiftDetailPanel"
    - from: "frontend/src/pages/gifts/GiftDetailPanel.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useGift hook for detail with credits"
      pattern: "useGift"
    - from: "frontend/src/pages/gifts/GiftForm.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useCreateGift/useUpdateGift mutations"
      pattern: "useCreateGift"
---

<objective>
Build the Gifts list page with filters and search, the gift detail slide-in panel with solicitor credit breakdown, and the gift create/edit form.

Purpose: This is the primary gift browsing experience. Users need to see, search, filter, and drill into gift details. The slide-in panel with solicitor credits is new UI (not a port of old DonationDetail).

Output: Three new page components in frontend/src/pages/gifts/ that provide the complete gift browsing and management experience.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md
@frontend/src/pages/donations/DonationList.tsx
@frontend/src/pages/donations/DonationDetail.tsx
@frontend/src/pages/donations/DonationForm.tsx
@frontend/src/api/gifts.ts
@frontend/src/hooks/useGifts.ts
@frontend/src/hooks/useFilterParams.ts
@frontend/src/components/shared/FilterBar.tsx
@frontend/src/components/shared/DataTable.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/pages/insights/UserDrilldownPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gifts list page with FilterBar, DataTable, search, and slide-in detail panel</name>
  <files>
    frontend/src/pages/gifts/GiftList.tsx
    frontend/src/pages/gifts/GiftDetailPanel.tsx
  </files>
  <action>
    **1. Create frontend/src/pages/gifts/GiftList.tsx:**
    - Follow the exact structure of DonationList.tsx but adapted for Gift model
    - Page title: "Gifts" with subtitle "Track and manage donor gifts"
    - "Record Gift" button navigates to /gifts/new
    - Use `useFilterParams(giftFilterParsers)` for URL-synced filters
    - Use `useGifts(queryParams)` for data fetching
    - Columns per locked decision:
      1. Donor Name -- Link to /contacts/{donor_contact}, font-medium text-primary hover:underline, onClick stopPropagation
      2. Amount -- formatCurrency(row.original.amount_dollars), font-semibold
      3. Gift Date -- formatLocalDate(row.original.gift_date) with Calendar icon (use formatLocalDate from @/lib/utils for UTC date bug)
      4. Fund -- row.original.fund_name || "N/A", text-muted-foreground
      5. Description -- row.original.description, truncated if long (max-w-[200px] truncate), text-muted-foreground
    - NO type, payment_method, or thanked columns (Gift model doesn't have these)
    - NO actions dropdown column (clicking row opens slide-in panel per locked decision)
    - Row click: set selectedGiftId state, open sheet
    - FilterBar children (NO fund dropdown -- per locked decision "No fund filter added", fund filter is deferred):
      - Search form (same pattern as DonationList search)
      - Date range inputs (gift_date_after, gift_date_before) with labels "From" / "To"
      - Amount range inputs (min_amount, max_amount)
      - Admin owner dropdown (using useUsers, gated on isAdmin)
    - FilterBar props: activeFilters, presets=giftPresets, exportUrl="/gifts/export/csv/", exportParams
    - Filter labels: search: "Search", gift_date_after: "From", gift_date_before: "To", min_amount: "Min Amount", max_amount: "Max Amount", owner: "Owner"
    - DataTable: pageCount from data.count / PAGE_SIZE, pageIndex from filters.page - 1, onPageChange, onRowClick sets selectedGiftId
    - Render GiftDetailPanel at bottom: `<GiftDetailPanel open={!!selectedGiftId} giftId={selectedGiftId} onClose={() => setSelectedGiftId(null)} />`
    - PAGE_SIZE = 20
    - Empty state: DataTable handles this natively

    **2. Create frontend/src/pages/gifts/GiftDetailPanel.tsx:**
    - Use Sheet component from @/components/ui/sheet (matching UserDrilldownPanel pattern)
    - Props: { open: boolean, giftId: string | null, onClose: () => void }
    - Fetch gift detail with useGift(giftId) -- returns GiftWithCredits with credits array
    - Sheet opens from right, className="w-full sm:max-w-lg overflow-y-auto"
    - Loading state: show Skeleton placeholders in SheetContent
    - Content layout:
      - SheetHeader:
        - SheetTitle: formatCurrency(gift.amount_dollars)
        - SheetDescription: "from {donor_contact_name} on {formatLocalDate(gift_date)}"
      - Details section (dl/dt/dd or simple label-value pairs):
        - Donor: Link to /contacts/{donor_contact}
        - Gift Date: formatLocalDate
        - Fund: fund_name or "None"
        - Description: gift.description or "No description"
        - External ID: gift.external_gift_id (only if non-empty)
      - Solicitor Credits section -- CONDITIONALLY rendered per locked decision:
        - Only show when gift.credits && gift.credits.length > 0
        - Section header: "Solicitor Credits"
        - Simple table with columns: Solicitor Name | Amount | Percentage
        - Solicitor Name: credit.solicitor_name
        - Amount: formatCurrency(credit.amount_dollars)
        - Percentage: calculate as (credit.amount_cents / gift.amount_cents * 100).toFixed(1) + "%"
        - Table styling: simple HTML table or shadcn Table component, consistent with project style
      - Footer with Edit button linking to /gifts/{id}/edit

    **IMPORTANT decisions to honor:**
    - Gift detail opens as SLIDE-IN PANEL (Sheet), NOT a full page (per locked decision)
    - Credits shown on gift detail view ONLY (not in list columns)
    - Simple table layout for credits: Solicitor Name | Amount | Percentage
    - Hide credits section ENTIRELY when gift has no solicitor credits
    - Donor name in list is a LINK to contact page (per locked decision)
    - Clicking gift row opens slide-in panel (per locked decision)
    - NO fund filter dropdown in FilterBar (per locked decision "No fund filter added"; fund filter is deferred)
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check GiftList.tsx renders correct columns and GiftDetailPanel conditionally renders credits. Verify NO fund filter dropdown exists in FilterBar.</manual>
  </verify>
  <done>GiftList.tsx renders Donor Name, Amount, Gift Date, Fund, Description columns with FilterBar, search, date/amount filters, admin owner dropdown. NO fund filter dropdown (per user decision). Row click opens GiftDetailPanel sheet showing gift details with conditional solicitor credit table.</done>
</task>

<task type="auto">
  <name>Task 2: Gift create/edit form with dollar-to-cents conversion</name>
  <files>
    frontend/src/pages/gifts/GiftForm.tsx
  </files>
  <action>
    Create frontend/src/pages/gifts/GiftForm.tsx following the DonationForm.tsx pattern but adapted for Gift model.

    **Form fields:**
    - Donor Contact: contact selector (existing pattern -- either dropdown or search input)
    - Amount: dollar input (user types dollars like "100.00"), converted to amount_cents on submit via Math.round(parseFloat(dollarValue) * 100)
    - Gift Date: date input (required)
    - Fund: optional fund selector dropdown (using useFunds)
    - Description: optional textarea

    **Create vs Edit mode:**
    - Path /gifts/new -> create mode (no existing data)
    - Path /gifts/:id/edit -> edit mode (prefill from useGift)
    - Use useCreateGift() and useUpdateGift() mutations
    - On success, navigate back to /gifts and show toast

    **Key implementation details:**
    - Dollar-to-cents conversion: the form displays and accepts dollar amounts, but submits amount_cents integer to the API
    - For edit mode, pre-fill dollar amount from gift.amount_dollars (already a decimal string from serializer)
    - Validation: amount must be positive, gift_date required, donor_contact required
    - Use existing form patterns: useState for form data, validation errors as Record<string, string>
    - Format: export default function GiftForm() for routing

    **IMPORTANT:** The Gift model does NOT have donation_type, payment_method, or thanked fields. Do NOT include them.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check form has correct fields and dollar-to-cents conversion logic</manual>
  </verify>
  <done>GiftForm.tsx handles create and edit modes with dollar-to-cents conversion on submit. Form fields: donor contact, amount (dollars), gift date, fund, description. No old Donation-specific fields (type, payment, thanked).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- GiftList.tsx imports from @/hooks/useGifts and @/hooks/useFilterParams
- GiftList.tsx uses giftFilterParsers, giftPresets
- GiftList.tsx renders exactly 5 columns: Donor Name, Amount, Gift Date, Fund, Description
- GiftList.tsx FilterBar does NOT contain a Fund dropdown filter (fund filter is deferred)
- GiftDetailPanel.tsx conditionally renders credits section
- GiftForm.tsx converts dollar input to cents on submit
- No references to old Donation types in new gift page files
</verification>

<success_criteria>
- Gifts list page displays gift data with correct columns per locked decision
- Search filters by donor name
- Date range, amount range, and owner filters all work
- NO fund filter dropdown in FilterBar (per locked decision)
- Clicking a gift row opens the Sheet slide-in panel
- Gift detail panel shows solicitor credits when present, hides when absent
- Credit table shows Solicitor Name, Amount, Percentage columns
- Gift create form submits amount_cents to API
- Gift edit form pre-fills from existing data
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-02-SUMMARY.md`
</output>
