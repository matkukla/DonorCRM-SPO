---
phase: 31-gift-recurring-gift-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gifts/serializers.py
  - apps/gifts/views.py
  - apps/gifts/filters.py
  - apps/gifts/urls.py
  - apps/gifts/export_views.py
  - frontend/src/api/gifts.ts
  - frontend/src/hooks/useGifts.ts
  - frontend/src/hooks/useFilterParams.ts
  - frontend/src/hooks/useContacts.ts
  - frontend/src/api/contacts.ts
  - frontend/src/lib/filter-presets.ts
autonomous: true
requirements: [UI-GIFT-03, UI-GIFT-04, UI-GIFT-05, UI-GIFT-07]

must_haves:
  truths:
    - "Gift list API supports search by donor name and column ordering"
    - "Gift detail API returns solicitor credit breakdown with solicitor names and amounts"
    - "CSV export endpoints for gifts and recurring gifts exist and return filtered data"
    - "Frontend has typed API functions and React Query hooks for all gift/recurring gift operations"
  artifacts:
    - path: "apps/gifts/serializers.py"
      provides: "GiftDetailSerializer with nested GiftCreditReadSerializer, fund_name field"
      contains: "GiftDetailSerializer"
    - path: "apps/gifts/export_views.py"
      provides: "CSV export views for Gift and RecurringGift"
      contains: "GiftExportCSVView"
    - path: "frontend/src/api/gifts.ts"
      provides: "Gift, GiftWithCredits, GiftCredit, RecurringGift types and all API functions"
      exports: ["getGifts", "getGift", "getRecurringGifts", "getRecurringGift"]
    - path: "frontend/src/hooks/useGifts.ts"
      provides: "React Query hooks for Gift and RecurringGift CRUD"
      exports: ["useGifts", "useGift", "useRecurringGifts", "useRecurringGift"]
  key_links:
    - from: "frontend/src/api/gifts.ts"
      to: "/api/v1/gifts/"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*gifts"
    - from: "frontend/src/hooks/useGifts.ts"
      to: "frontend/src/api/gifts.ts"
      via: "import { getGifts }"
      pattern: "from.*@/api/gifts"
    - from: "apps/gifts/views.py"
      to: "apps/gifts/serializers.py"
      via: "GiftDetailSerializer for retrieve action"
      pattern: "GiftDetailSerializer"
---

<objective>
Create the backend enhancements needed for the frontend (detail serializer with credits, search/ordering, CSV exports, owner filter, fund name) and build the complete frontend API layer and React Query hooks for Gift and RecurringGift.

Purpose: The existing Gift/RecurringGift backend has basic CRUD but lacks search, ordering, export, owner filter, fund name display, and credit detail serialization. The frontend has no Gift API layer at all. This plan creates the complete data plumbing so subsequent plans can build pages.

Output: Enhanced backend API endpoints, new CSV export views, new frontend api/gifts.ts with types, new hooks/useGifts.ts with React Query hooks, updated filter parsers and presets.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@apps/gifts/serializers.py
@apps/gifts/views.py
@apps/gifts/filters.py
@apps/gifts/urls.py
@apps/gifts/models.py
@apps/contacts/export_views.py
@frontend/src/api/donations.ts
@frontend/src/hooks/useDonations.ts
@frontend/src/hooks/useFilterParams.ts
@frontend/src/lib/filter-presets.ts
@frontend/src/api/contacts.ts
@frontend/src/hooks/useContacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend enhancements -- detail serializer with credits, search/ordering, CSV exports, owner + fund name</name>
  <files>
    apps/gifts/serializers.py
    apps/gifts/views.py
    apps/gifts/filters.py
    apps/gifts/urls.py
    apps/gifts/export_views.py
  </files>
  <action>
    **1. serializers.py -- Add GiftDetailSerializer with credits and fund_name:**
    - Create GiftCreditReadSerializer (ModelSerializer on GiftCredit):
      - Fields: id, solicitor, solicitor_name (CharField source='solicitor.normalized_name', read_only), amount_cents, amount_dollars (DecimalField source='amount_dollars', read_only, max_digits=12, decimal_places=2)
    - Create GiftDetailSerializer inheriting from GiftSerializer:
      - Add `credits = GiftCreditReadSerializer(many=True, read_only=True)` (GiftCredit related_name is 'credits' -- verify by checking model, may be 'giftcredit_set')
      - Add `fund_name = serializers.CharField(source='fund.name', read_only=True, default=None)`
      - Meta inherits from GiftSerializer.Meta, adds 'credits' and 'fund_name' to fields
    - Also add `fund_name` to base GiftSerializer for list view display:
      - `fund_name = serializers.CharField(source='fund.name', read_only=True, default=None)`
      - Add 'fund_name' to Meta.fields
    - Add `fund_name` to RecurringGiftSerializer as well for list page display

    **2. views.py -- Add SearchFilter + OrderingFilter, use GiftDetailSerializer for retrieve:**
    - Import `from rest_framework.filters import SearchFilter, OrderingFilter`
    - On GiftListCreateView:
      - Add SearchFilter and OrderingFilter to filter_backends: `filter_backends = [DjangoFilterBackend, SearchFilter, OrderingFilter]`
      - Add `search_fields = ['donor_contact__first_name', 'donor_contact__last_name', 'description']`
      - Add `ordering_fields = ['gift_date', 'amount_cents', 'created_at']`
    - On GiftDetailView:
      - Override get_serializer_class: return GiftDetailSerializer for GET, GiftCreateSerializer for PUT/PATCH
      - Import GiftDetailSerializer
      - Add prefetch_related('credits__solicitor') to get_queryset() for GiftDetailView only
    - On RecurringGiftListCreateView:
      - Add SearchFilter and OrderingFilter to filter_backends
      - Add `search_fields = ['donor_contact__first_name', 'donor_contact__last_name', 'description']`
      - Add `ordering_fields = ['start_date', 'amount_cents', 'status', 'created_at']`

    **3. filters.py -- Add owner filter for admin filtering:**
    - On GiftFilterSet: Add `owner = django_filters.UUIDFilter(field_name='donor_contact__owner')` and add to fields list
    - On RecurringGiftFilterSet: Add `owner = django_filters.UUIDFilter(field_name='donor_contact__owner')` and add to fields list

    **4. export_views.py -- Create CSV export views (new file):**
    - Follow the exact pattern from apps/contacts/export_views.py (read it for reference)
    - Create Echo class (local, 3 lines -- per Phase 22 decision)
    - Import sanitize_csv_value from apps/contacts/export_views (or redefine locally if not importable)
    - Create GiftExportCSVView:
      - permission_classes = [permissions.IsAuthenticated]
      - Owner-scoped queryset (admin/finance/read_only see all, others see own contacts' gifts)
      - Apply GiftFilterSet(request.query_params, queryset=qs)
      - Limit to 10,000 rows
      - CSV columns: Donor Name, Amount, Gift Date, Fund, Description
      - Use amount_dollars (not amount_cents) for CSV output
      - Filename: gifts_YYYY-MM-DD.csv
      - Use StreamingHttpResponse with Content-Disposition header
    - Create RecurringGiftExportCSVView:
      - Same pattern
      - CSV columns: Donor Name, Amount, Frequency, Status, Start Date, End Date, Fund
      - Filename: recurring_gifts_YYYY-MM-DD.csv

    **5. urls.py -- Add export URL patterns:**
    - Add: `path('export/csv/', GiftExportCSVView.as_view(), name='gift-export-csv')`
    - Add: `path('recurring/export/csv/', RecurringGiftExportCSVView.as_view(), name='recurring-gift-export-csv')`
    - Import the new views from export_views

    **IMPORTANT:** Check the GiftCredit model's related_name to Gift. If it's 'credits', use that. If it's the default 'giftcredit_set', use that in the serializer source field OR add prefetch_related with the correct name.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python manage.py check --deploy 2>&1 | head -20</automated>
    <manual>Verify GiftDetailSerializer includes credits field, export views exist, search/ordering backends added</manual>
  </verify>
  <done>GiftDetailSerializer returns nested credits with solicitor names. Search and ordering work on list views. CSV export endpoints exist at /gifts/export/csv/ and /gifts/recurring/export/csv/. Owner filter available on both filter sets. Fund name included in list and detail responses.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend API layer, hooks, filter parsers, and presets for Gift/RecurringGift</name>
  <files>
    frontend/src/api/gifts.ts
    frontend/src/hooks/useGifts.ts
    frontend/src/hooks/useFilterParams.ts
    frontend/src/hooks/useContacts.ts
    frontend/src/api/contacts.ts
    frontend/src/lib/filter-presets.ts
  </files>
  <action>
    **1. Create frontend/src/api/gifts.ts (new file):**
    - Define TypeScript interfaces matching actual backend serializer shapes:
      ```
      Gift { id, donor_contact, donor_contact_name, fund, fund_name, external_gift_id, amount_cents, amount_dollars, gift_date, description, created_at, updated_at }
      GiftCredit { id, solicitor, solicitor_name, amount_cents, amount_dollars }
      GiftWithCredits extends Gift { credits: GiftCredit[] }
      GiftCreate { donor_contact, fund?, amount_cents, gift_date, description?, external_gift_id? }
      RecurringGift { id, donor_contact, donor_contact_name, fund, fund_name, external_gift_id, amount_cents, amount_dollars, frequency, start_date, end_date, status, monthly_equivalent, description, created_at, updated_at }
      RecurringGiftCreate { donor_contact, fund?, amount_cents, frequency, status?, start_date, end_date?, description?, external_gift_id? }
      RecurringGiftStatus = "active" | "held" | "completed" | "cancelled" | "terminated"
      RecurringGiftFrequency = "monthly" | "quarterly" | "semi_annually" | "annually" | "bimonthly" | "biweekly" | "weekly" | "irregular"
      ```
    - Create label maps:
      ```
      recurringGiftStatusLabels: { active: "Active", held: "Held", completed: "Completed", cancelled: "Cancelled", terminated: "Terminated" }
      recurringGiftFrequencyLabels: { monthly: "Monthly", quarterly: "Quarterly", semi_annually: "Semi-Annually", annually: "Annually", bimonthly: "Bi-Monthly", biweekly: "Bi-Weekly", weekly: "Weekly", irregular: "Irregular" }
      ```
    - API functions (all use apiClient from "./client" and PaginatedResponse from "./contacts"):
      - getGifts(params): GET /gifts/ -> PaginatedResponse<Gift>
      - getGift(id): GET /gifts/{id}/ -> GiftWithCredits
      - createGift(data): POST /gifts/ -> Gift
      - updateGift(id, data): PATCH /gifts/{id}/ -> Gift
      - deleteGift(id): DELETE /gifts/{id}/
      - getRecurringGifts(params): GET /gifts/recurring/ -> PaginatedResponse<RecurringGift>
      - getRecurringGift(id): GET /gifts/recurring/{id}/ -> RecurringGift
      - createRecurringGift(data): POST /gifts/recurring/ -> RecurringGift
      - updateRecurringGift(id, data): PATCH /gifts/recurring/{id}/ -> RecurringGift
      - deleteRecurringGift(id): DELETE /gifts/recurring/{id}/

    **2. Create frontend/src/hooks/useGifts.ts (new file):**
    - Follow exact pattern from hooks/useDonations.ts but with "gifts" and "recurring-gifts" query keys
    - Hooks: useGifts(params), useGift(id), useCreateGift, useUpdateGift, useDeleteGift
    - Hooks: useRecurringGifts(params), useRecurringGift(id), useCreateRecurringGift, useUpdateRecurringGift, useDeleteRecurringGift
    - Mutations invalidate ["gifts"], ["recurring-gifts"], ["contacts"], ["dashboard"] caches

    **3. Update frontend/src/hooks/useFilterParams.ts:**
    - Add giftFilterParsers matching backend GiftFilterSet fields:
      ```
      page: parseAsInteger.withDefault(1), search: parseAsString, donor_contact: parseAsString, gift_date_after: parseAsString, gift_date_before: parseAsString, min_amount: parseAsString, max_amount: parseAsString, owner: parseAsString, ordering: parseAsString
      ```
    - Add recurringGiftFilterParsers matching backend RecurringGiftFilterSet fields:
      ```
      page: parseAsInteger.withDefault(1), search: parseAsString, donor_contact: parseAsString, status: parseAsString, frequency: parseAsString, owner: parseAsString, ordering: parseAsString
      ```
    - Export both new parser configs
    - Keep existing donationFilterParsers and pledgeFilterParsers for now (will be removed in Plan 04)
    - **NOTE:** Do NOT add `fund: parseAsString` to giftFilterParsers. Per user decision, no fund filter is added to the Gifts list page (fund filter is deferred). The fund_name is a display column only.

    **4. Update frontend/src/lib/filter-presets.ts:**
    - Add giftPresets array (similar to donationPresets but without donation_type, payment_method, thanked):
      - "Recent" preset: gift_date_after = 30 days ago, others null
      - "This Month" preset: gift_date_after = first day of current month
      - "Large Gifts" preset: min_amount = 100 (in cents for backend: "10000")
    - Add recurringGiftPresets array:
      - "Active" preset: status = "active", others null
      - "Held" preset: status = "held"
      - "All Statuses" preset: status = null
    - Keep existing donationPresets and pledgePresets for now

    **5. Update frontend/src/api/contacts.ts:**
    - Rename getContactDonations to getContactGifts (keep same endpoint path /contacts/{id}/donations/ as backend still uses that URL alias -- verify in config/api_urls.py)
    - Add getContactRecurringGifts calling /contacts/{id}/pledges/ (same alias pattern)
    - Export with new names. Keep old names as aliases if other code references them.

    **6. Update frontend/src/hooks/useContacts.ts:**
    - Add useContactGifts(contactId) hook using getContactGifts
    - Add useContactRecurringGifts(contactId) hook using getContactRecurringGifts
    - Use query keys ["contacts", contactId, "gifts"] and ["contacts", contactId, "recurring-gifts"]
    - Keep old hooks temporarily for backward compat

    **IMPORTANT:** Amount min/max filters need to convert dollars to cents for the backend. The backend GiftFilterSet filters on amount_cents, so if the user types "100" (meaning $100), the frontend must send min_amount=10000. Handle this in the query params conversion or document it clearly for the list page to handle.
    Actually, re-reading the old DonationList -- the old amount filter sent raw values too and the old DonationFilterSet used amount_min/amount_max on an amount (decimal) field. The new GiftFilterSet uses amount_cents. So either:
    (a) Change GiftFilterSet to accept dollar amounts and convert internally, OR
    (b) Have the frontend convert dollar input to cents before sending.
    Approach (a) is cleaner -- add custom filter methods on GiftFilterSet that multiply by 100. Do this in Task 1.
    UPDATE for Task 1: On GiftFilterSet, change min_amount and max_amount to use a custom method filter that converts dollar input to cents, or change them to filter on amount_dollars property. Simplest: use NumberFilter with method='filter_min_amount_dollars' that converts the input value * 100 to filter on amount_cents. Same for max_amount.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that api/gifts.ts exports all types and functions, hooks/useGifts.ts exports all hooks</manual>
  </verify>
  <done>frontend/src/api/gifts.ts exists with all Gift/RecurringGift types and API functions. frontend/src/hooks/useGifts.ts exists with all CRUD hooks. Filter parsers and presets exist for both gift types (no fund filter in giftFilterParsers per user decision). Contact hooks support gift queries.</done>
</task>

</tasks>

<verification>
- `python manage.py check` passes with no errors
- `npx tsc --noEmit` passes in frontend/
- GiftDetailSerializer includes 'credits' and 'fund_name' fields
- GiftExportCSVView and RecurringGiftExportCSVView exist in export_views.py
- SearchFilter and OrderingFilter are in filter_backends for both list views
- api/gifts.ts exports Gift, GiftWithCredits, RecurringGift types
- hooks/useGifts.ts exports useGifts, useGift, useRecurringGifts, useRecurringGift hooks
- giftFilterParsers and recurringGiftFilterParsers exported from useFilterParams.ts
- giftFilterParsers does NOT contain a 'fund' key (per user decision: no fund filter)
</verification>

<success_criteria>
- Backend Gift detail endpoint returns credits array with solicitor_name and amount_dollars per credit
- Search parameter on gift list filters by donor first/last name
- GET /gifts/export/csv/ returns streaming CSV with gift data
- GET /gifts/recurring/export/csv/ returns streaming CSV with recurring gift data
- Owner filter works on both gift filter sets
- Fund name included in all gift list and detail responses
- Frontend TypeScript compiles with zero errors
- All new API functions call correct backend endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md`
</output>
