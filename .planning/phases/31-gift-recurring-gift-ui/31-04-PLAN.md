---
phase: 31-gift-recurring-gift-ui
plan: 04
type: execute
wave: 3
depends_on: ["31-02", "31-03"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/dashboard/RecentDonations.tsx
  - frontend/src/components/dashboard/LateDonations.tsx
  - frontend/src/components/dashboard/SupportProgress.tsx
  - frontend/src/components/dashboard/NeedsAttention.tsx
  - frontend/src/components/dashboard/GivingSummaryCard.tsx
  - frontend/src/pages/contacts/ContactDetail.tsx
  - frontend/src/pages/insights/DonationsByMonthYear.tsx
  - frontend/src/pages/insights/MonthlyCommitments.tsx
  - frontend/src/pages/insights/LateDonations.tsx
autonomous: false
requirements: [UI-GIFT-01, UI-GIFT-02, UI-GIFT-06, DASH-02]

must_haves:
  truths:
    - "Sidebar shows 'Gifts' and 'Recurring Gifts' instead of 'Donations' and 'Pledges'"
    - "Dashboard cards say 'Recent Gifts', 'Total Given', 'Monthly Support' instead of old labels"
    - "Contact detail page has a single 'Gifts' tab with one-time gifts and recurring gifts sections"
    - "URL /donations redirects to /gifts, /pledges redirects to /recurring-gifts"
    - "Late Pledges section shows 'Late detection coming soon' placeholder"
    - "Insights sidebar labels updated to gift terminology"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Updated nav items with Gifts and Recurring Gifts labels"
      contains: "Gifts"
    - path: "frontend/src/App.tsx"
      provides: "Routes for /gifts and /recurring-gifts with Navigate redirects from old paths"
      contains: "/gifts"
    - path: "frontend/src/pages/contacts/ContactDetail.tsx"
      provides: "Single 'Gifts' tab with one-time and recurring gift sections"
      contains: "Gifts"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/gifts/GiftList.tsx"
      via: "Route path='/gifts'"
      pattern: "path.*gifts"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "/gifts"
      via: "navItems href"
      pattern: "href.*gifts"
    - from: "frontend/src/pages/contacts/ContactDetail.tsx"
      to: "frontend/src/hooks/useContacts.ts"
      via: "useContactGifts and useContactRecurringGifts hooks"
      pattern: "useContactGifts"
---

<objective>
Wire everything together: update sidebar navigation, routes, dashboard labels, contact detail gifts tab, insights labels, and clean up old donation/pledge files.

Purpose: This is the integration plan that makes the new gift/recurring gift pages reachable and renames all user-facing labels from old terminology to new.

Output: Complete UI rebrand from Donations/Pledges to Gifts/Recurring Gifts across all frontend pages.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md
@.planning/phases/31-gift-recurring-gift-ui/31-02-SUMMARY.md
@.planning/phases/31-gift-recurring-gift-ui/31-03-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/components/dashboard/RecentDonations.tsx
@frontend/src/components/dashboard/LateDonations.tsx
@frontend/src/components/dashboard/SupportProgress.tsx
@frontend/src/components/dashboard/NeedsAttention.tsx
@frontend/src/components/dashboard/GivingSummaryCard.tsx
@frontend/src/pages/contacts/ContactDetail.tsx
@frontend/src/pages/insights/DonationsByMonthYear.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar nav, App.tsx routes with redirects, dashboard component label renames</name>
  <files>
    frontend/src/App.tsx
    frontend/src/components/layout/Sidebar.tsx
    frontend/src/pages/Dashboard.tsx
    frontend/src/components/dashboard/RecentDonations.tsx
    frontend/src/components/dashboard/LateDonations.tsx
    frontend/src/components/dashboard/SupportProgress.tsx
    frontend/src/components/dashboard/NeedsAttention.tsx
    frontend/src/components/dashboard/GivingSummaryCard.tsx
  </files>
  <action>
    **1. Sidebar.tsx -- Rename nav items:**
    - Change "Donations" nav item to "Gifts" with href="/gifts" (keep same icon DollarSign)
    - Change "Pledges" nav item to "Recurring Gifts" with href="/recurring-gifts" (keep same icon)
    - In the insights sub-navigation section (if visible in Sidebar):
      - "Donations by Month/Year" -> "Gifts by Month/Year"
      - "Late Donations" -> "Late Gifts"

    **2. App.tsx -- Update routes and add redirects:**
    - Replace old donation routes with gift routes:
      - /gifts -> GiftList
      - /gifts/new -> GiftForm
      - /gifts/:id/edit -> GiftForm
      - (NO /gifts/:id route needed -- detail is slide-in panel, not separate page)
    - Replace old pledge routes with recurring gift routes:
      - /recurring-gifts -> RecurringGiftList
      - /recurring-gifts/new -> RecurringGiftForm
      - /recurring-gifts/:id -> RecurringGiftDetail
      - /recurring-gifts/:id/edit -> RecurringGiftForm
    - Add backward-compatible redirects using <Navigate>:
      - /donations -> /gifts (replace)
      - /donations/* -> /gifts (catch-all)
      - /pledges -> /recurring-gifts (replace)
      - /pledges/* -> /recurring-gifts (catch-all)
    - Remove old DonationList, DonationDetail, DonationForm, PledgeList, PledgeDetail, PledgeForm imports
    - Add new imports for GiftList, GiftForm, RecurringGiftList, RecurringGiftDetail, RecurringGiftForm
    - All gift/recurring gift routes should be wrapped in ProtectedPage (same as old donation/pledge routes)

    **3. Dashboard component label renames (per locked decisions):**

    **RecentDonations.tsx:**
    - CardTitle: "Recent Gifts" (was "Recent Donations")
    - CardDescription: "Latest gifts received" (was "Latest donations received" or similar)
    - Any internal "donation" references in display text -> "gift"
    - Links to donations -> links to /gifts

    **LateDonations.tsx:**
    - This component shows late pledges/donations.
    - Per locked decision: Late Pledges section kept as placeholder with "Late detection coming soon" text
    - Replace the late donations/pledges content with a placeholder:
      ```
      <div className="p-4 bg-amber-50 dark:bg-amber-950/50 rounded-lg border border-amber-200 dark:border-amber-800">
        <span className="text-sm font-medium text-amber-900 dark:text-amber-200">Late detection coming soon</span>
      </div>
      ```
    - Keep the component shell for layout consistency

    **SupportProgress.tsx:**
    - CardTitle: "Monthly Support" (per locked decision -- NOT "Pledged Monthly Support" or "Monthly Recurring Gift Support")
    - Any "pledge" text in labels -> "recurring gift" or just remove if not needed
    - Data already comes from RecurringGift model via backend (Phase 30 cutover)

    **NeedsAttention.tsx:**
    - "Late Pledges" section label -> Replace with "Late detection coming soon" placeholder (per locked decision)
    - Any "donation" text -> "gift"
    - Any "pledge" text -> "recurring gift" where appropriate

    **GivingSummaryCard.tsx:**
    - "Recurring Pledges" label -> "Recurring Gifts"
    - "Total Donated" -> "Total Given" (per locked decision)
    - Any other donation/pledge text -> gift/recurring gift equivalents

    **Dashboard.tsx:**
    - Stat card labels: "Recent Donations" -> "Recent Gifts", "Active Pledges" -> "Active Recurring Gifts" (or whatever the current labels are)
    - If stat cards reference donation-specific data, update labels to gift terminology
    - Import path updates if component files are renamed

    **IMPORTANT locked decisions to honor:**
    - "Recent Donations" -> "Recent Gifts"
    - "Total Donated" -> "Total Given"
    - Late Pledges section: "Late detection coming soon" placeholder
    - Support progress card title: "Monthly Support" (exact text)
    - Dashboard: label rename ONLY -- no layout or data changes
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify sidebar says Gifts/Recurring Gifts, dashboard cards say Recent Gifts/Total Given/Monthly Support</manual>
  </verify>
  <done>Sidebar shows "Gifts" and "Recurring Gifts" nav items. Routes updated with /gifts and /recurring-gifts paths. Old /donations and /pledges paths redirect to new paths. Dashboard cards display "Recent Gifts", "Total Given", "Monthly Support", and "Late detection coming soon" placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Contact detail Gifts tab, insights labels, and old file cleanup</name>
  <files>
    frontend/src/pages/contacts/ContactDetail.tsx
    frontend/src/pages/insights/DonationsByMonthYear.tsx
    frontend/src/pages/insights/MonthlyCommitments.tsx
    frontend/src/pages/insights/LateDonations.tsx
  </files>
  <action>
    **1. ContactDetail.tsx -- Merge Donations and Pledges tabs into single "Gifts" tab:**
    - Per locked decision: single "Gifts" tab with two sections inside (one-time gifts table + recurring gifts table)
    - Replace the existing Donations and Pledges TabsTrigger/TabsContent with a single "Gifts" tab:
      ```
      <TabsTrigger value="gifts">
        Gifts ({giftsCount + recurringGiftsCount})
      </TabsTrigger>
      ```
    - TabsContent for "gifts":
      - Section 1: "One-Time Gifts" header
        - Use useContactGifts(contactId) to fetch gifts for this contact
        - Simple table showing: Amount | Gift Date | Fund | Description
        - Empty state: "No one-time gifts recorded"
      - Section 2 (Card with mt-4): "Recurring Gifts" header
        - Use useContactRecurringGifts(contactId) to fetch recurring gifts
        - Simple table showing: Amount | Frequency | Status | Start Date
        - Status as colored Badge (same variant mapping as RecurringGiftList)
        - Empty state: "No recurring gifts recorded"
    - Remove old useContactDonations and useContactPledges imports
    - Add imports for useContactGifts, useContactRecurringGifts from @/hooks/useContacts
    - Use formatCurrency for amounts, formatLocalDate for dates
    - Gift rows could link to the main gifts page or just be static rows (no slide-in on contact page -- keep it simple)

    **2. Insights pages -- Label-only updates:**

    **DonationsByMonthYear.tsx:**
    - Page title: "Gifts by Month/Year" (was "Donations by Month/Year")
    - Update any subtitle/description text
    - Chart labels if they say "Donations" -> "Gifts"
    - This is a label-only change -- do NOT change data queries (backend already returns Gift data)

    **MonthlyCommitments.tsx:**
    - Page title: Keep as "Monthly Commitments" or change to "Monthly Recurring Gifts" (Claude's discretion -- "Monthly Commitments" is cleaner)
    - Update any "pledge" references in display text -> "recurring gift"

    **LateDonations.tsx (insights page, not dashboard component):**
    - Page title: "Late Gifts" (was "Late Donations")
    - Update any "donation" or "pledge" text in the display
    - If this page shows late pledge data that now returns empty: add a note "Late detection not yet available for recurring gifts"

    **3. Sidebar insights sub-navigation (if not already done in Task 1):**
    - Ensure insights links in sidebar use updated labels: "Gifts by Month/Year", "Late Gifts"

    **4. Delete old frontend files that are now replaced:**
    - Delete: frontend/src/api/donations.ts
    - Delete: frontend/src/api/pledges.ts
    - Delete: frontend/src/hooks/useDonations.ts
    - Delete: frontend/src/hooks/usePledges.ts
    - Delete: frontend/src/pages/donations/ directory (DonationList.tsx, DonationDetail.tsx, DonationForm.tsx)
    - Delete: frontend/src/pages/pledges/ directory (PledgeList.tsx, PledgeDetail.tsx, PledgeForm.tsx)
    - BEFORE deleting: verify NO remaining imports reference these files. Grep for:
      - "from.*api/donations" -- should be zero after all updates
      - "from.*api/pledges" -- should be zero
      - "from.*hooks/useDonations" -- should be zero
      - "from.*hooks/usePledges" -- should be zero
      - "from.*pages/donations" -- should be zero (only App.tsx had these, updated in Task 1)
      - "from.*pages/pledges" -- should be zero

    **5. Remove old filter parsers and presets:**
    - In useFilterParams.ts: Remove donationFilterParsers and pledgeFilterParsers (verify no remaining consumers first)
    - In filter-presets.ts: Remove donationPresets and pledgePresets

    **IMPORTANT:** The insights pages and dashboard are label-only changes. Do NOT change data fetching logic -- the backend already returns Gift/RecurringGift data since Phase 30.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
    <manual>Verify contact detail has single Gifts tab, insights pages have updated labels, no old donation/pledge files remain</manual>
  </verify>
  <done>Contact detail has single "Gifts" tab with one-time and recurring gift sections. Insights pages show "Gifts by Month/Year" and "Late Gifts" labels. Old donation/pledge API, hook, and page files deleted. No remaining references to old donation/pledge modules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete UI rebrand</name>
  <what-built>
    Complete frontend rebrand from Donations/Pledges to Gifts/Recurring Gifts including:
    - Sidebar navigation with "Gifts" and "Recurring Gifts" items
    - Gifts list page with filters, search, and slide-in detail panel
    - Recurring Gifts list page with status/frequency filters
    - Dashboard cards with renamed labels (Recent Gifts, Total Given, Monthly Support)
    - Contact detail page with merged "Gifts" tab
    - CSV exports for both gift types
    - Old /donations and /pledges URLs redirect to new paths
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev`
    2. Start the backend: `python manage.py runserver`
    3. Verify sidebar: "Gifts" and "Recurring Gifts" items visible (not "Donations" or "Pledges")
    4. Click "Gifts" -- list page loads with Donor Name, Amount, Gift Date, Fund, Description columns
    5. Click a gift row -- slide-in panel opens with gift details
    6. If the gift has solicitor credits (RE-imported gifts), verify credit table shows Solicitor Name, Amount, Percentage
    7. Click "Recurring Gifts" -- list page loads with Donor Name, Amount, Frequency, Status, Start Date, Fund columns
    8. Verify status badges are color-coded (active=green, held=yellow, etc.)
    9. Check Dashboard -- cards should say "Recent Gifts", "Total Given", "Monthly Support"
    10. Check Contact detail page -- single "Gifts" tab with two sections
    11. Navigate to /donations -- should redirect to /gifts
    12. Navigate to /pledges -- should redirect to /recurring-gifts
    13. Verify no remaining "Donations" or "Pledges" text visible anywhere in the UI
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Sidebar shows "Gifts" and "Recurring Gifts" (grep Sidebar.tsx for these strings)
- App.tsx has routes for /gifts and /recurring-gifts
- App.tsx has Navigate redirects from /donations and /pledges
- Dashboard components use updated labels
- ContactDetail.tsx has single "Gifts" tab
- No files exist in frontend/src/pages/donations/ or frontend/src/pages/pledges/
- No files exist at frontend/src/api/donations.ts or frontend/src/api/pledges.ts
- grep -r "from.*api/donations\|from.*api/pledges\|from.*hooks/useDonations\|from.*hooks/usePledges" frontend/src/ returns zero results
</verification>

<success_criteria>
- All user-facing text says "Gifts" instead of "Donations" and "Recurring Gifts" instead of "Pledges"
- Sidebar, dashboard, contact detail, and insights pages all use new terminology
- Old /donations and /pledges URLs redirect to new paths
- Single "Gifts" tab on contact detail with one-time and recurring sections
- Dashboard label rename is complete with no layout/data changes
- Late Pledges section shows "Late detection coming soon" placeholder
- Support progress card title is exactly "Monthly Support"
- No old donation/pledge frontend files remain
- Human verified the complete UI
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-04-SUMMARY.md`
</output>
