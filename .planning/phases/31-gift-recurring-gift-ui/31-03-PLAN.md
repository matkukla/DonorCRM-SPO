---
phase: 31-gift-recurring-gift-ui
plan: 03
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - frontend/src/pages/pledges/PledgeList.tsx
  - frontend/src/pages/pledges/PledgeDetail.tsx
  - frontend/src/pages/pledges/PledgeForm.tsx
  - frontend/src/api/pledges.ts
  - frontend/src/hooks/usePledges.ts
  - frontend/src/pages/contacts/ContactDetail.tsx
  - frontend/src/hooks/useContacts.ts
  - frontend/src/api/contacts.ts
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/dashboard/NeedsAttention.tsx
  - frontend/src/components/dashboard/LateDonations.tsx
  - frontend/src/api/dashboard.ts
autonomous: true
requirements: [UI-GIFT-02, UI-GIFT-04, UI-GIFT-06, DASH-02]

must_haves:
  truths:
    - "Pledges list page loads data from RecurringGift model and displays correct columns (Donor Name, Amount, Frequency, Status, Start Date, Fund)"
    - "Pledges list has expanded status options (active/held/completed/cancelled/terminated) and frequency options (8 total)"
    - "Contact detail page Donations tab shows Gift model data with amount_dollars and gift_date"
    - "Contact detail page Pledges tab shows RecurringGift model data with frequency labels"
    - "Dashboard Late Pledges section shows 'Late detection coming soon' placeholder"
    - "Dashboard stat cards and labels are unchanged from user perspective"
  artifacts:
    - path: "frontend/src/pages/pledges/PledgeList.tsx"
      provides: "Pledges list using RecurringGift model data"
      min_lines: 100
    - path: "frontend/src/pages/contacts/ContactDetail.tsx"
      provides: "Contact detail with updated Donations and Pledges tabs using Gift/RecurringGift data"
      contains: "amount_dollars"
    - path: "frontend/src/components/dashboard/NeedsAttention.tsx"
      provides: "Late pledges section with 'Late detection coming soon' placeholder"
      contains: "Late detection coming soon"
  key_links:
    - from: "frontend/src/pages/pledges/PledgeList.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useRecurringGifts hook"
      pattern: "useRecurringGifts"
    - from: "frontend/src/pages/contacts/ContactDetail.tsx"
      to: "frontend/src/hooks/useContacts.ts"
      via: "useContactDonations and useContactPledges hooks"
      pattern: "useContactDonations|useContactPledges"
---

<objective>
Rewrite the Pledges list/detail/form pages for RecurringGift model, update Contact detail tabs, and update Dashboard late pledges section.

Purpose: The Pledges pages reference old Pledge model fields (is_late, days_late, fulfillment_percentage, next_expected_date) that don't exist on RecurringGift. The Contact detail page Donations/Pledges tabs render old Donation/Pledge data shapes. The Dashboard NeedsAttention and LateDonations components reference late pledge data that no longer exists (RecurringGift has no is_late field). All visible labels stay as "Pledges" and "Donations" per user decision.

Output: Working Pledges list/detail/form pages, updated Contact detail tabs, and Dashboard with "Late detection coming soon" placeholder
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md

@frontend/src/pages/pledges/PledgeList.tsx
@frontend/src/pages/pledges/PledgeDetail.tsx
@frontend/src/pages/pledges/PledgeForm.tsx
@frontend/src/api/pledges.ts
@frontend/src/hooks/usePledges.ts
@frontend/src/pages/contacts/ContactDetail.tsx
@frontend/src/hooks/useContacts.ts
@frontend/src/api/contacts.ts
@frontend/src/pages/Dashboard.tsx
@frontend/src/components/dashboard/NeedsAttention.tsx
@frontend/src/components/dashboard/LateDonations.tsx
@frontend/src/api/dashboard.ts
@frontend/src/api/gifts.ts
@frontend/src/hooks/useGifts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Pledges pages for RecurringGift model</name>
  <files>
    frontend/src/pages/pledges/PledgeList.tsx
    frontend/src/pages/pledges/PledgeDetail.tsx
    frontend/src/pages/pledges/PledgeForm.tsx
    frontend/src/api/pledges.ts
    frontend/src/hooks/usePledges.ts
  </files>
  <action>
CRITICAL: All visible text stays as "Pledges". Page title = "Pledges", button = "Create Pledge". NO renaming. URL path stays /pledges.

1. **frontend/src/pages/pledges/PledgeList.tsx** -- Rewrite for RecurringGift:
   - Replace imports: use `useRecurringGifts` from `@/hooks/useGifts`, `recurringGiftFilterParsers` from `@/hooks/useFilterParams`, `recurringGiftPresets` from `@/lib/filter-presets`, types from `@/api/gifts`
   - Column definitions (ColumnDef<RecurringGift>):
     - "Donor Name": `donor_contact_name`, Link to `/contacts/${row.original.donor_contact}` with stopPropagation
     - "Amount": `amount_dollars`, formatCurrency. Append ` / {frequencyLabel.toLowerCase()}` using recurringGiftFrequencyLabels
     - "Frequency": `frequency`, use `recurringGiftFrequencyLabels[row.original.frequency]`
     - "Status": `status`, Badge with status variant colors. Expanded status map:
       ```
       active: "success", held: "warning", completed: "secondary", cancelled: "destructive", terminated: "destructive"
       ```
     - "Start Date": `start_date`, formatLocalDate
     - "Fund": `fund_name`, text or "---"
   - REMOVE: monthly_equivalent column, fulfillment_percentage progress bar, is_late badge, days_late badge, next_expected_date column
   - REMOVE: Late Pledges toggle button (is_late filter no longer exists on backend)
   - REMOVE: action menu items for pause/resume/cancel (these actions don't exist on RecurringGift API)
   - Filter bar:
     - Keep: search input, status dropdown (expanded to 5 statuses), frequency dropdown (expanded to 8 frequencies)
     - Keep: amount range inputs (min_amount, max_amount) -- Note: min_amount/max_amount need to be added to recurringGiftFilterParsers if not already there. Check backend RecurringGiftFilterSet -- it does NOT have amount filters. SKIP amount range for pledges (backend doesn't support it).
     - REMOVE: is_late toggle, start_date_after/before date range inputs (backend RecurringGiftFilterSet doesn't have these)
     - filterLabels: search="Search", status="Status", frequency="Frequency", owner="Owner"
     - exportUrl: "/donations/export/csv/" -- WAIT: This needs to be the recurring gift export. Since /pledges/ alias maps to gifts app, /pledges/recurring/export/csv/ should work. Actually looking at url structure: gifts app urls are under /gifts/ prefix. The pledges alias includes all gifts urls. So /pledges/export/csv/ maps to gift export (wrong), and /pledges/recurring/export/csv/ maps to recurring gift export (correct). Use "/gifts/recurring/export/csv/" to be explicit.
   - onRowClick: navigate to `/pledges/${rg.id}` (detail page stays as full page)
   - Page header: title="Pledges", description="Manage recurring giving commitments"

2. **frontend/src/pages/pledges/PledgeDetail.tsx** -- Rewrite for RecurringGift:
   - Use `useRecurringGift(id)` from `@/hooks/useGifts`, `useDeleteRecurringGift` from `@/hooks/useGifts`
   - Display fields: Amount (amount_dollars), Frequency, Status (with Badge), Start Date, End Date (if set), Fund, Description, External Gift ID (if non-empty)
   - REMOVE: thanked status, linked pledge info, payment method, donation type, fulfillment bar, is_late badge, next_expected_date, days_late, last_fulfilled_date
   - REMOVE: pause/resume/cancel action buttons
   - Keep: Edit button (navigate to /pledges/:id/edit), Delete button
   - Back link: "Back to Pledges" -> /pledges
   - Header: Amount with status badge
   - Description: "from {donor_contact_name}, {frequency} since {formatLocalDate(start_date)}"

3. **frontend/src/pages/pledges/PledgeForm.tsx** -- Rewrite for RecurringGift:
   - Use `useRecurringGift`, `useCreateRecurringGift`, `useUpdateRecurringGift` from `@/hooks/useGifts`
   - Form data: `{ donor_contact, amount (dollar string), frequency, status, start_date, end_date, description }`
   - On submit: convert `amount_cents: Math.round(parseFloat(formData.amount) * 100)`, send `{ donor_contact, amount_cents, frequency, status, start_date, end_date, description }`
   - Frequency dropdown: all 8 RecurringGiftFrequency options with labels
   - Status dropdown: all 5 RecurringGiftStatus options with labels
   - When editing, load existing data: amount = rg.amount_dollars, frequency = rg.frequency, etc.
   - REMOVE: is_late, notes (now description), fulfillment fields
   - Title: "Create Pledge" / "Edit Pledge"
   - After submit navigate to `/pledges` (list)

4. **frontend/src/api/pledges.ts** -- Replace with re-exports for backward compatibility:
   ```typescript
   // Backward compatibility -- re-export RecurringGift types as Pledge names
   export { getRecurringGifts as getPledges, getRecurringGift as getPledge, createRecurringGift as createPledge, updateRecurringGift as updatePledge, deleteRecurringGift as deletePledge } from "./gifts"
   export type { RecurringGift as Pledge, RecurringGiftCreate as PledgeCreate, RecurringGiftUpdate as PledgeUpdate, RecurringGiftStatus as PledgeStatus, RecurringGiftFrequency as PledgeFrequency } from "./gifts"
   export { recurringGiftStatusLabels as pledgeStatusLabels, recurringGiftFrequencyLabels as pledgeFrequencyLabels } from "./gifts"
   ```

5. **frontend/src/hooks/usePledges.ts** -- Replace with re-exports:
   ```typescript
   export { useRecurringGifts as usePledges, useRecurringGift as usePledge, useCreateRecurringGift as useCreatePledge, useUpdateRecurringGift as useUpdatePledge, useDeleteRecurringGift as useDeletePledge } from "./useGifts"
   ```
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -20` should show no type errors.
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npm run build 2>&1 | tail -5` should succeed.
  </verify>
  <done>
Pledges list page shows RecurringGift data with columns Donor Name | Amount | Frequency | Status | Start Date | Fund. Status dropdown has 5 options (active/held/completed/cancelled/terminated). Frequency dropdown has 8 options. No is_late or fulfillment columns. PledgeForm creates RecurringGift with amount_cents conversion. PledgeDetail shows correct RecurringGift fields. All visible text says "Pledges".
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Contact detail tabs and Dashboard late pledges placeholder</name>
  <files>
    frontend/src/pages/contacts/ContactDetail.tsx
    frontend/src/hooks/useContacts.ts
    frontend/src/api/contacts.ts
    frontend/src/pages/Dashboard.tsx
    frontend/src/components/dashboard/NeedsAttention.tsx
    frontend/src/components/dashboard/LateDonations.tsx
    frontend/src/api/dashboard.ts
  </files>
  <action>
CRITICAL: All visible text stays as "Donations" and "Pledges". Tab labels stay as "Donations" and "Pledges". NO renaming.

1. **frontend/src/pages/contacts/ContactDetail.tsx** -- Update Donations and Pledges tabs:
   - Donations tab: Update the inline type annotation for donations items to match Gift shape:
     - Change `{ id: string; amount: string; date: string; donation_type: string; is_thanked: boolean }` to `{ id: string; amount_dollars: string; gift_date: string; description: string; fund_name: string | null }`
     - Render: `formatCurrency(donation.amount_dollars)` instead of `formatCurrency(donation.amount)`
     - Render: `formatLocalDate(donation.gift_date)` and `donation.description || ''` instead of `donation.date` and `donation.donation_type`
     - Remove: `donation.is_thanked` / "Thanked" badge (Gift model has no thanked field)
     - "Record Donation" button link stays: `/donations/new?contact=${id}`
   - Pledges tab: Update inline type to match RecurringGift shape:
     - Change `{ id: string; amount: string; frequency: string; status: string; is_late: boolean }` to `{ id: string; amount_dollars: string; frequency: string; status: string }`
     - Render: `formatCurrency(pledge.amount_dollars)` instead of `formatCurrency(pledge.amount)`
     - Remove: `pledge.is_late` / "Late" badge (RecurringGift has no is_late field)
     - "Create Pledge" button link stays: `/pledges/new?contact=${id}`
   - Tab labels stay: "Donations ({count})" and "Pledges ({count})"
   - Keep both tabs separate (user decision allows either single tab or two tabs -- keep two for simplicity)

2. **frontend/src/hooks/useContacts.ts** and **frontend/src/api/contacts.ts** -- No changes needed.
   The `getContactDonations` and `getContactPledges` functions call `/contacts/${id}/donations/` and `/contacts/${id}/pledges/` which are Contact app endpoints that now return Gift/RecurringGift serializer data (backend already updated in Phase 30). The hooks work fine -- we just need to update the component rendering to match the new data shape.

3. **frontend/src/components/dashboard/NeedsAttention.tsx** -- Update Late Pledges section:
   - The `latePledgeCount` and `latePledges` props will always be 0/empty (backend returns empty data for late pledges since RecurringGift has no is_late).
   - Replace the late pledges rendering block (lines ~87-108) with a static placeholder:
     ```tsx
     {/* Late Pledges -- placeholder until detection is implemented */}
     <div className="p-4 bg-amber-50 dark:bg-amber-950/50 border border-amber-100 dark:border-amber-900/50 rounded-lg">
       <div className="flex items-center gap-2">
         <Clock className="h-4 w-4 text-amber-600 dark:text-amber-400" />
         <span className="font-medium text-amber-900 dark:text-amber-200">
           Late detection coming soon
         </span>
       </div>
       <p className="text-sm text-amber-800 dark:text-amber-300 mt-1">
         Late pledge detection will be available in a future update.
       </p>
     </div>
     ```
   - Show this placeholder always (not conditionally on latePledgeCount > 0). Remove the latePledgeCount > 0 condition for this section.
   - Update `hasItems` calculation: remove `latePledgeCount > 0` from the condition. Always consider this section present since placeholder is always shown. Change to: `const hasItems = overdueTaskCount > 0 || thankYouCount > 0 || true` (or just remove the !hasItems check since we always show the placeholder).
     Actually, better approach: always show the late pledges placeholder, but keep the hasItems check for overdue tasks and thank you queue normally. The simplest change: always render the placeholder block regardless of latePledgeCount, remove the `latePledgeCount > 0 &&` conditional.

4. **frontend/src/components/dashboard/LateDonations.tsx** -- Update for empty data:
   - This component shows "Late Donations" with details about late pledges. Since the backend now returns empty data for late_donations, the component will show "All pledges are on track" by default.
   - Update the title from "Late Donations" to "Late Pledges" -- WAIT, no renaming! Keep as "Late Donations".
   - Actually, looking more carefully: the card title says "Late Donations" and description says "Active pledges past due". The `donations` prop will always be empty now. The empty state text "All pledges are on track" is fine.
   - No changes needed to this component -- it will gracefully show the empty state.

5. **frontend/src/pages/Dashboard.tsx** -- Minor updates:
   - The StatCard "Active Pledges" uses `data?.support_progress?.active_pledge_count` which still works (backend returns this from RecurringGift).
   - The StatCard "Recent Donations" uses `data?.recent_gifts` which works.
   - The "Items Needing Attention" StatCard adds `late_pledge_count` which will be 0. This is fine -- no change needed.
   - No text changes needed -- all labels stay as-is.

6. **frontend/src/api/dashboard.ts** -- No changes needed. The types (RecentGift, LateDonation, etc.) already match what the backend returns. The LatePledge type will just never have data.
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -20` should show no type errors.
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npm run build 2>&1 | tail -5` should succeed.
  </verify>
  <done>
Contact detail Donations tab renders Gift model data (amount_dollars, gift_date). Pledges tab renders RecurringGift data (amount_dollars, frequency, status -- no is_late). NeedsAttention shows "Late detection coming soon" placeholder. Dashboard stat cards work with Gift/RecurringGift backend data. All visible text unchanged from user perspective.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds with no errors
2. Pledges list shows columns: Donor Name | Amount | Frequency | Status | Start Date | Fund
3. Status filter has 5 options (active, held, completed, cancelled, terminated)
4. Frequency filter has 8 options
5. No is_late, days_late, or fulfillment columns anywhere
6. Contact detail Donations tab shows amount_dollars and gift_date
7. Contact detail Pledges tab shows amount_dollars, frequency, status (no is_late badge)
8. Dashboard NeedsAttention shows "Late detection coming soon" placeholder
9. Dashboard stat cards still display correctly
10. All visible text says "Donations" and "Pledges" throughout
</verification>

<success_criteria>
- Pledges pages fully rewired to RecurringGift model with expanded status/frequency enums
- Contact detail tabs render correct Gift/RecurringGift data shapes
- Dashboard late pledges placeholder displays "Late detection coming soon" per locked decision
- No is_late, days_late, fulfillment_percentage, or next_expected_date references remain in active code
- All URLs remain /pledges/* -- no path changes
- All visible labels unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-03-SUMMARY.md`
</output>
