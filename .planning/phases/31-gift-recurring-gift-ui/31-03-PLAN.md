---
phase: 31-gift-recurring-gift-ui
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - frontend/src/pages/recurring-gifts/RecurringGiftList.tsx
  - frontend/src/pages/recurring-gifts/RecurringGiftDetail.tsx
  - frontend/src/pages/recurring-gifts/RecurringGiftForm.tsx
autonomous: true
requirements: [UI-GIFT-02, UI-GIFT-04]

must_haves:
  truths:
    - "User can view a paginated list of recurring gifts with Donor Name, Amount, Frequency, Status, Start Date, Fund columns"
    - "User can filter recurring gifts by status and frequency"
    - "User can view recurring gift details including all installment fields"
    - "User can create and edit recurring gifts with correct frequency and status options"
  artifacts:
    - path: "frontend/src/pages/recurring-gifts/RecurringGiftList.tsx"
      provides: "Recurring gifts list page with FilterBar, DataTable, status/frequency filters"
      min_lines: 120
    - path: "frontend/src/pages/recurring-gifts/RecurringGiftDetail.tsx"
      provides: "Recurring gift detail page"
      min_lines: 50
    - path: "frontend/src/pages/recurring-gifts/RecurringGiftForm.tsx"
      provides: "Recurring gift create/edit form with expanded status and frequency options"
      min_lines: 80
  key_links:
    - from: "frontend/src/pages/recurring-gifts/RecurringGiftList.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useRecurringGifts hook"
      pattern: "useRecurringGifts"
    - from: "frontend/src/pages/recurring-gifts/RecurringGiftList.tsx"
      to: "frontend/src/hooks/useFilterParams.ts"
      via: "recurringGiftFilterParsers"
      pattern: "recurringGiftFilterParsers"
    - from: "frontend/src/pages/recurring-gifts/RecurringGiftDetail.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useRecurringGift hook for detail fetch"
      pattern: "useRecurringGift"
    - from: "frontend/src/pages/recurring-gifts/RecurringGiftForm.tsx"
      to: "frontend/src/hooks/useGifts.ts"
      via: "useCreateRecurringGift/useUpdateRecurringGift mutations"
      pattern: "useCreateRecurringGift"
---

<objective>
Build the Recurring Gifts list page with status/frequency filters, detail page, and create/edit form.

Purpose: Users need to manage recurring gift commitments with the expanded status (5 choices) and frequency (8 choices) options from the RE-compatible RecurringGift model.

Output: Three new page components in frontend/src/pages/recurring-gifts/ providing the complete recurring gift management experience.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gift-recurring-gift-ui/31-CONTEXT.md
@.planning/phases/31-gift-recurring-gift-ui/31-RESEARCH.md
@.planning/phases/31-gift-recurring-gift-ui/31-01-SUMMARY.md
@frontend/src/pages/pledges/PledgeList.tsx
@frontend/src/pages/pledges/PledgeDetail.tsx
@frontend/src/pages/pledges/PledgeForm.tsx
@frontend/src/api/gifts.ts
@frontend/src/hooks/useGifts.ts
@frontend/src/hooks/useFilterParams.ts
@frontend/src/components/shared/FilterBar.tsx
@frontend/src/components/shared/DataTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recurring Gifts list page with status and frequency filters</name>
  <files>
    frontend/src/pages/recurring-gifts/RecurringGiftList.tsx
  </files>
  <action>
    Create frontend/src/pages/recurring-gifts/RecurringGiftList.tsx following the PledgeList.tsx pattern but adapted for RecurringGift model.

    **Page structure:**
    - Page title: "Recurring Gifts" with subtitle "Manage recurring giving commitments"
    - "New Recurring Gift" button navigates to /recurring-gifts/new
    - Use `useFilterParams(recurringGiftFilterParsers)` for URL-synced filters
    - Use `useRecurringGifts(queryParams)` for data fetching
    - PAGE_SIZE = 20

    **Columns per locked decision (Donor Name | Amount | Frequency | Status | Start Date | Fund):**
    1. Donor Name -- Link to /contacts/{donor_contact}, font-medium text-primary hover:underline, onClick stopPropagation
    2. Amount -- formatCurrency(row.original.amount_dollars), font-semibold. Show monthly_equivalent below in smaller text: "(${monthly_equivalent}/mo)" if frequency != monthly
    3. Frequency -- Badge with recurringGiftFrequencyLabels[row.original.frequency]
    4. Status -- Badge with appropriate variant:
       - active: "success" (green)
       - held: "warning" (amber/yellow)
       - completed: "secondary" (gray)
       - cancelled: "secondary" (gray)
       - terminated: "destructive" (red)
       Use recurringGiftStatusLabels for display text
    5. Start Date -- formatLocalDate(row.original.start_date)
    6. Fund -- row.original.fund_name || "N/A", text-muted-foreground

    **Row click:** Navigate to /recurring-gifts/{id} (full detail page, not slide-in -- recurring gifts are more complex than one-time gifts)

    **FilterBar children:**
    - Search form (same pattern as GiftList)
    - Status dropdown using recurringGiftStatusLabels (5 options + "All Statuses")
    - Frequency dropdown using recurringGiftFrequencyLabels (8 options + "All Frequencies")
    - Admin owner dropdown (gated on isAdmin, using useUsers)

    **FilterBar props:**
    - activeFilters, presets=recurringGiftPresets
    - exportUrl="/gifts/recurring/export/csv/", exportParams
    - Filter labels: search: "Search", status: "Status", frequency: "Frequency", owner: "Owner"
    - Filter value labels: status: recurringGiftStatusLabels, frequency: recurringGiftFrequencyLabels, owner: user name map

    **DataTable:** pageCount, pageIndex, onPageChange, onRowClick to navigate to detail page

    **Status badge variant helper function:**
    ```
    function getStatusVariant(status: string): BadgeVariant {
      switch (status) {
        case "active": return "success"
        case "held": return "warning"
        case "completed": case "cancelled": return "secondary"
        case "terminated": return "destructive"
        default: return "secondary"
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check RecurringGiftList.tsx renders correct 6 columns with status/frequency filters</manual>
  </verify>
  <done>RecurringGiftList.tsx renders Donor Name, Amount (with monthly equivalent), Frequency, Status (color-coded badges), Start Date, Fund columns. Status and frequency dropdown filters work. Search filters by donor name. Admin owner filter available. CSV export button works.</done>
</task>

<task type="auto">
  <name>Task 2: Recurring gift detail page and create/edit form</name>
  <files>
    frontend/src/pages/recurring-gifts/RecurringGiftDetail.tsx
    frontend/src/pages/recurring-gifts/RecurringGiftForm.tsx
  </files>
  <action>
    **1. Create frontend/src/pages/recurring-gifts/RecurringGiftDetail.tsx:**
    - Follow PledgeDetail.tsx pattern adapted for RecurringGift fields
    - Route: /recurring-gifts/:id
    - Use useRecurringGift(id) to fetch data
    - Display sections:
      - Header: Amount (large, formatted), status badge, frequency badge
      - Details:
        - Donor: Link to contact page
        - Amount: formatCurrency(amount_dollars) with monthly equivalent
        - Frequency: recurringGiftFrequencyLabels[frequency]
        - Status: Badge with color variant
        - Start Date: formatLocalDate
        - End Date: formatLocalDate or "Ongoing"
        - Fund: fund_name or "None"
        - Description: description or "No description"
        - External ID: external_gift_id (only if non-empty)
      - Actions: Edit button linking to /recurring-gifts/{id}/edit, Back button to /recurring-gifts
    - Loading state: Skeleton layout
    - Error state: "Recurring gift not found" message

    **2. Create frontend/src/pages/recurring-gifts/RecurringGiftForm.tsx:**
    - Follow PledgeForm.tsx pattern adapted for RecurringGift
    - Create mode: /recurring-gifts/new
    - Edit mode: /recurring-gifts/:id/edit (prefill from useRecurringGift)
    - Form fields:
      - Donor Contact: contact selector
      - Amount: dollar input (converts to amount_cents on submit like GiftForm)
      - Frequency: Select dropdown with all 8 RecurringGiftFrequency options using recurringGiftFrequencyLabels
      - Status: Select dropdown with all 5 RecurringGiftStatus options using recurringGiftStatusLabels
      - Start Date: date input (required)
      - End Date: optional date input
      - Fund: optional fund selector
      - Description: optional textarea
    - Use useCreateRecurringGift() and useUpdateRecurringGift() mutations
    - On success: navigate to /recurring-gifts, show toast
    - Dollar-to-cents conversion same as GiftForm
    - Validation: amount positive, start_date required, donor_contact required, frequency required

    **IMPORTANT:** RecurringGift has 5 statuses (not 4 like old Pledge) and 8 frequencies (not 4). Make sure ALL options are available in dropdowns. Note frequency key is "semi_annually" (not "semi_annual").
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check RecurringGiftDetail shows all fields and RecurringGiftForm has all 5 status and 8 frequency options</manual>
  </verify>
  <done>RecurringGiftDetail.tsx shows complete recurring gift info with status badge, frequency, dates, fund. RecurringGiftForm.tsx handles create/edit with all 5 statuses and 8 frequencies in dropdowns, dollar-to-cents conversion.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- RecurringGiftList.tsx uses recurringGiftFilterParsers from useFilterParams
- RecurringGiftList.tsx renders 6 columns: Donor Name, Amount, Frequency, Status, Start Date, Fund
- Status badges use correct color variants (success/warning/secondary/destructive)
- RecurringGiftForm.tsx has Select dropdowns with all 5 statuses and 8 frequencies
- Dollar-to-cents conversion works in form submission
</verification>

<success_criteria>
- Recurring Gifts list page displays data with correct columns per locked decision
- Status and frequency filter dropdowns show all options with correct labels
- Status badges are color-coded (active=green, held=yellow, completed/cancelled=gray, terminated=red)
- Monthly equivalent shown under amount when frequency is not monthly
- Recurring gift detail page shows all installment fields
- Create/edit form has complete frequency and status options
- CSV export works with filtered data
</success_criteria>

<output>
After completion, create `.planning/phases/31-gift-recurring-gift-ui/31-03-SUMMARY.md`
</output>
