---
phase: 32-import-ui
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/admin/AdminUsers.tsx
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
  - frontend/src/pages/admin/analytics/StalledContacts.tsx
  - frontend/src/pages/admin/analytics/UserDetail.tsx
  - frontend/src/api/imports.ts
  - frontend/src/hooks/useImports.ts
  - frontend/src/components/imports/ImportCard.tsx
  - frontend/src/components/imports/ImportDialog.tsx
  - frontend/src/components/imports/SPOImportTile.tsx
  - frontend/src/components/imports/CSVPreviewTable.tsx
  - frontend/src/pages/admin/ImportCenter.tsx
  - frontend/package.json
autonomous: true
requirements:
  - UI-IMP-08
  - UI-IMP-06

must_haves:
  truths:
    - "Admin sub-navigation no longer has an Imports link on any admin page"
    - "/admin/imports route no longer exists in the app"
    - "ImportCenter page is deleted"
    - "SPO import components (ImportDialog, SPOImportTile, CSVPreviewTable, ImportCard) are deleted"
    - "SPO import functions and hooks are removed from API and hooks files"
    - "react-papaparse is uninstalled"
    - "Application compiles and runs without errors"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Clean routing without /admin/imports"
    - path: "frontend/src/api/imports.ts"
      provides: "Clean API layer with only RE, export, and legacy contact import functions"
    - path: "frontend/src/hooks/useImports.ts"
      provides: "Clean hooks with only RE, export, and batch hooks"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "no ImportCenter import"
      via: "removed import and route"
      pattern: "ImportCenter"
    - from: "frontend/src/api/imports.ts"
      to: "no SPO functions"
      via: "removed importFunds, importEntities, etc."
      pattern: "importFunds|importEntities|importTransactions|importPledges"
---

<objective>
Remove the old admin Import Center page, SPO import components, dead code from the API/hooks layer, admin sub-nav "Imports" links, and uninstall react-papaparse.

Purpose: Clean up all legacy import infrastructure that has been superseded by the new unified Import/Export page.
Output: Lean codebase with no dead import code or broken admin links.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-import-ui/32-CONTEXT.md
@.planning/phases/32-import-ui/32-RESEARCH.md
@.planning/phases/32-import-ui/32-01-SUMMARY.md
@.planning/phases/32-import-ui/32-02-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/api/imports.ts
@frontend/src/hooks/useImports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove admin imports route, admin sub-nav links, and delete old SPO components</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/pages/admin/AdminUsers.tsx,
    frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx,
    frontend/src/pages/admin/analytics/StalledContacts.tsx,
    frontend/src/pages/admin/analytics/UserDetail.tsx,
    frontend/src/pages/admin/ImportCenter.tsx,
    frontend/src/components/imports/ImportCard.tsx,
    frontend/src/components/imports/ImportDialog.tsx,
    frontend/src/components/imports/SPOImportTile.tsx,
    frontend/src/components/imports/CSVPreviewTable.tsx
  </files>
  <action>
**App.tsx:**
- Remove the `import ImportCenter from "@/pages/admin/ImportCenter"` line
- Remove the route: `<Route path="/admin/imports" element={<ProtectedPage requiredRole="admin"><ImportCenter /></ProtectedPage>} />`

**Admin page files (AdminUsers.tsx, AdminAnalyticsDashboard.tsx, StalledContacts.tsx, UserDetail.tsx):**
- In each file, find the admin sub-navigation section that contains NavLink elements
- Remove the NavLink for "Imports" that links to "/admin/imports"
- Each file has an inline admin sub-nav (not a shared component). Search for the text "Imports" and the path "/admin/imports" in the NavLink elements and remove them.
- Keep all other NavLink elements (Users, Analytics/Dashboard, etc.)

**Delete these files entirely (use rm or file deletion):**
- `frontend/src/pages/admin/ImportCenter.tsx` -- the entire admin import page
- `frontend/src/components/imports/ImportDialog.tsx` -- SPO import dialog with useReducer state machine
- `frontend/src/components/imports/SPOImportTile.tsx` -- SPO import tile component
- `frontend/src/components/imports/CSVPreviewTable.tsx` -- CSV preview table (only used by ImportDialog)
- `frontend/src/components/imports/ImportCard.tsx` -- old generic import card (replaced by FileDropZone)
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Verify /admin/imports route is gone, no "Imports" link in admin sub-nav, deleted files don't exist</manual>
  </verify>
  <done>No /admin/imports route in App.tsx. No "Imports" NavLink in any admin page. ImportCenter.tsx, ImportDialog.tsx, SPOImportTile.tsx, CSVPreviewTable.tsx, ImportCard.tsx all deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up API/hooks dead code and uninstall react-papaparse</name>
  <files>
    frontend/src/api/imports.ts,
    frontend/src/hooks/useImports.ts,
    frontend/package.json
  </files>
  <action>
**frontend/src/api/imports.ts:**
Remove these SPO-specific items that are no longer used:
- `ImportType` type alias (`'funds' | 'entities' | 'transactions' | 'pledges'`)
- `LatestImportRun` interface
- `DependencyCounts` interface
- `LatestImportsResponse` interface
- `SPOImportResult` interface
- `getLatestImports()` function
- `importFunds()` function
- `importEntities()` function
- `importTransactions()` function
- `importPledges()` function
- `downloadImportErrorsCSV()` function

Keep these (still used by the new page or other parts of the codebase):
- `ImportResult` interface (used by legacy contact import which is still live)
- `ImportError` interface
- `importContacts()` function (still used by generic contact import - will be wired in Phase 35)
- `importDonations()` function (still exists but returns 410 - keep for now, Phase 35 will replace)
- `exportContacts()` function
- `exportDonations()` function
- `downloadContactTemplate()` function
- `downloadDonationTemplate()` function
- `getFunds()` function and `FundOption` interface (used by filter dropdowns)
- `downloadFile()` helper function
- All new RE types and functions added in Plan 01 (`REImportType`, `REImportResponse`, `ImportBatchRecord`, `importRE`, `getImportBatches`)

**frontend/src/hooks/useImports.ts:**
Remove these SPO-specific hooks:
- `useLatestImports()` hook
- `useSPOImport()` hook
- Remove the `ImportType` import from `@/api/imports` (no longer exported)

Keep all other hooks (useImportContacts, useImportDonations, useExportContacts, useExportDonations, useDownloadContactTemplate, useDownloadDonationTemplate, useFunds, useREImport, useImportBatches).

**Uninstall react-papaparse:**
Run `npm uninstall react-papaparse` in the frontend directory. This package was only used by ImportDialog.tsx which is now deleted.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Verify no SPO functions/types in imports.ts, no SPO hooks in useImports.ts, react-papaparse not in package.json</manual>
  </verify>
  <done>imports.ts has no SPO functions or types. useImports.ts has no SPO hooks. react-papaparse is removed from package.json and package-lock.json. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no broken imports referencing deleted files)
2. No reference to ImportCenter anywhere in the codebase (grep confirms)
3. No reference to SPOImportTile, ImportDialog, CSVPreviewTable anywhere
4. No "Imports" NavLink in AdminUsers, AdminAnalyticsDashboard, StalledContacts, or UserDetail pages
5. react-papaparse not in package.json dependencies
6. `npm ls react-papaparse` returns "empty"
7. Navigating to /admin/imports in browser redirects to / (catch-all route)
</verification>

<success_criteria>
All legacy import code is removed. No broken links or imports. The codebase compiles cleanly. The only import infrastructure remaining is the new unified Import/Export page and the RE/MPD backend endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/32-import-ui/32-03-SUMMARY.md`
</output>
