---
phase: 32-import-ui
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - frontend/src/pages/imports/ImportExport.tsx
  - frontend/src/components/imports/FileDropZone.tsx
  - frontend/src/components/imports/ImportResultBanner.tsx
  - frontend/src/components/imports/CSVHeaderReference.tsx
  - frontend/src/components/imports/REImportTab.tsx
  - frontend/src/components/imports/REImportSection.tsx
  - frontend/src/components/imports/SmartsheetSection.tsx
  - frontend/src/components/imports/GenericImportSection.tsx
  - frontend/src/components/imports/ImportHistorySection.tsx
  - frontend/src/components/imports/ExportSection.tsx
autonomous: true
requirements:
  - UI-IMP-01
  - UI-IMP-02
  - UI-IMP-03
  - UI-IMP-04
  - UI-IMP-05
  - UI-IMP-07

must_haves:
  truths:
    - "Import/Export page shows RE Imports section with 4 tabs (Constituent, Solicitor, Gift, Recurring Gift)"
    - "Each RE tab has drag-and-drop file upload with filename/size preview and Import button"
    - "After RE import completes, result banner shows success/error/already-processed counts with expandable error details"
    - "Each RE tab shows CSV header reference below upload area"
    - "RE tabs show visual step numbering (Step 1 of 4, etc.)"
    - "Smartsheet section shows MPD import functionality"
    - "Generic section shows Coming soon placeholder for Contact and Donation imports"
    - "Export section shows existing contact and donation export buttons"
    - "Import history section lists past ImportBatch records"
  artifacts:
    - path: "frontend/src/pages/imports/ImportExport.tsx"
      provides: "Unified Import/Export page with all sections"
      contains: "REImportSection"
    - path: "frontend/src/components/imports/FileDropZone.tsx"
      provides: "Reusable drag-and-drop file upload component"
      contains: "handleDrop"
    - path: "frontend/src/components/imports/ImportResultBanner.tsx"
      provides: "Success/error/duplicate result display"
      contains: "is_duplicate"
    - path: "frontend/src/components/imports/CSVHeaderReference.tsx"
      provides: "Header reference table for RE import types"
      contains: "RE_IMPORT_HEADERS"
    - path: "frontend/src/components/imports/REImportTab.tsx"
      provides: "Parameterized RE import tab with upload, result, and header reference"
      contains: "useREImport"
    - path: "frontend/src/components/imports/REImportSection.tsx"
      provides: "RE section wrapper with 4 sub-tabs"
      contains: "TabsTrigger"
    - path: "frontend/src/components/imports/ImportHistorySection.tsx"
      provides: "ImportBatch history list"
      contains: "useImportBatches"
  key_links:
    - from: "frontend/src/components/imports/REImportTab.tsx"
      to: "frontend/src/hooks/useImports.ts"
      via: "useREImport hook"
      pattern: "useREImport"
    - from: "frontend/src/components/imports/ImportHistorySection.tsx"
      to: "frontend/src/hooks/useImports.ts"
      via: "useImportBatches hook"
      pattern: "useImportBatches"
    - from: "frontend/src/pages/imports/ImportExport.tsx"
      to: "frontend/src/components/imports/REImportSection.tsx"
      via: "component import"
      pattern: "REImportSection"
---

<objective>
Rewrite the ImportExport page with the unified section layout (RE Imports, Smartsheet, Generic, Exports, Import History) and create all new components for the import workflow.

Purpose: Delivers the complete user-facing import UI with all 5 import types organized in a clear layout per user decisions.
Output: Fully functional Import/Export page with all sections and components.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-import-ui/32-CONTEXT.md
@.planning/phases/32-import-ui/32-RESEARCH.md
@.planning/phases/32-import-ui/32-01-SUMMARY.md
@frontend/src/pages/imports/ImportExport.tsx
@frontend/src/components/imports/MPDImportTile.tsx
@frontend/src/components/imports/ExportCard.tsx
@frontend/src/api/imports.ts
@frontend/src/hooks/useImports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable import components (FileDropZone, ImportResultBanner, CSVHeaderReference)</name>
  <files>
    frontend/src/components/imports/FileDropZone.tsx,
    frontend/src/components/imports/ImportResultBanner.tsx,
    frontend/src/components/imports/CSVHeaderReference.tsx
  </files>
  <action>
**FileDropZone.tsx** - Reusable drag-and-drop file selector:
- Props: `onFileSelect: (file: File) => void`, `accept: string` (e.g., ".csv"), `maxSizeMB?: number` (default 10), `disabled?: boolean`, `selectedFile: File | null`, `onClear: () => void`
- Reuse the proven native drag-drop pattern from the existing ImportCard.tsx (handleDragOver, handleDrop with preventDefault, isDragging state)
- Visual states: empty (dashed border, Upload icon, "Drag & drop or click to browse"), dragging (blue border highlight), file selected (show filename + file size in KB/MB + Remove button)
- File validation: check extension matches `accept` prop, check size vs maxSizeMB, show toast.error on validation failure
- Hidden file input with click-to-browse fallback
- Dark mode: use semantic tokens (border-border, bg-muted, text-muted-foreground) with drag highlight using border-primary

**ImportResultBanner.tsx** - Displays import result after completion:
- Props: `result: REImportResponse`, `onDismiss?: () => void`
- Three visual states:
  1. **Duplicate** (`is_duplicate === true`): Blue info banner with Info icon, "Already Processed" badge, "This file has been imported before" message. Dark mode: bg-blue-50 dark:bg-blue-950/50, border-blue-200 dark:border-blue-800
  2. **Success** (`error_count === 0`): Green success banner with CheckCircle icon. Show created/updated/skipped counts.
  3. **Partial Success** (`error_count > 0`): Amber warning banner with AlertTriangle icon. Show created/updated/skipped counts + error count. Use shadcn Collapsible to show expandable error details list. Each error shows "Row {row}: {error}". Error list has `max-h-60 overflow-y-auto` for scrollable container.
- Optional dismiss X button in top-right corner

**CSVHeaderReference.tsx** - Shows required and optional CSV columns:
- Props: `importType: REImportType`
- Static data object `RE_IMPORT_HEADERS` (defined in this file as a const) mapping each import type to:
  - `required`: string[] of required column descriptions
  - `optional`: string[] of optional column descriptions
  - `reAliases`: Record<string, string> mapping canonical name to RE column name
- Use the exact header data from 32-RESEARCH.md "CSV Header Reference Data" section
- Render as a compact table/list with two sections: "Required" (with green dot) and "Optional" (with gray dot), showing both the canonical name and the RE alias
- Style: text-xs, inside a muted background container (bg-muted/50 rounded-lg p-3), with "CSV Column Reference" as a small heading
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Check all three components render correctly with proper types</manual>
  </verify>
  <done>FileDropZone handles drag-drop and click-to-browse with file validation; ImportResultBanner shows all three result states (duplicate/success/partial) with expandable errors; CSVHeaderReference displays correct headers for all 4 RE import types</done>
</task>

<task type="auto">
  <name>Task 2: Create section components and rewrite ImportExport page</name>
  <files>
    frontend/src/components/imports/REImportTab.tsx,
    frontend/src/components/imports/REImportSection.tsx,
    frontend/src/components/imports/SmartsheetSection.tsx,
    frontend/src/components/imports/GenericImportSection.tsx,
    frontend/src/components/imports/ImportHistorySection.tsx,
    frontend/src/components/imports/ExportSection.tsx,
    frontend/src/pages/imports/ImportExport.tsx
  </files>
  <action>
**REImportTab.tsx** - Single parameterized RE import tab:
- Props: `importType: REImportType`, `stepNumber: number`, `totalSteps: number`, `title: string`, `description: string`
- State: `selectedFile: File | null`, `result: REImportResponse | null`
- Uses `useREImport(importType)` hook for mutation
- Layout (top to bottom):
  1. Step indicator: `<Badge variant="secondary">Step {stepNumber} of {totalSteps}</Badge>`
  2. Description text (brief, e.g., "Import contact records from Raiser's Edge")
  3. `<FileDropZone>` for file selection (accept=".csv")
  4. Import button: full width, disabled when no file or isPending, shows Loader2 spinner during upload
  5. `<ImportResultBanner>` shown when result is not null (with onDismiss to clear)
  6. `<CSVHeaderReference>` always visible below

- On successful import: set result state. On error: toast.error with message from error.response.data.detail.
- Reset file and result when user starts new import.

**REImportSection.tsx** - RE section with 4 sub-tabs:
- Uses shadcn Tabs with defaultValue="constituent"
- Section header: "Raiser's Edge Imports" with Database icon
- Subtitle: "Import data exported from Raiser's Edge. Follow the recommended order for best results."
- TabsList with 4 triggers: "1. Constituents", "2. Solicitors", "3. Gifts", "4. Recurring Gifts"
- Each TabsContent renders `<REImportTab>` with appropriate props:
  - constituent: stepNumber=1, title="Constituents", description="Import or update donor contact records"
  - solicitor: stepNumber=2, title="Solicitors", description="Import fundraiser/solicitor records and auto-link to users"
  - gift: stepNumber=3, title="Gifts", description="Import one-time gift records with solicitor credits"
  - recurring_gift: stepNumber=4, title="Recurring Gifts", description="Import recurring gift/pledge records"

**SmartsheetSection.tsx** - Wraps existing MPDImportTile:
- Section header: "Smartsheet Import" with FileSpreadsheet icon
- Subtitle: "Upload monthly MPD Dashboard Report"
- Renders `<MPDImportTile />` directly (reuse as-is, but remove the outer Card wrapper -- MPDImportTile already provides its own Card)
- Actually: just render `<MPDImportTile />` inside the section. The MPDImportTile already has its own Card. No changes to MPDImportTile needed.

**GenericImportSection.tsx** - Placeholder section:
- Section header: "Generic CSV Import" with FileUp icon
- Two sub-items: "Contacts" and "Donations" each showing a placeholder card with:
  - Muted/disabled appearance
  - "Coming soon" badge
  - Brief description: "Import contacts from any CSV format" / "Import donations from any CSV format"
- Use a simple grid with two placeholder cards (not tabs since there's no content)

**ImportHistorySection.tsx** - Import history list:
- Section header: "Import History" with History icon
- Uses `useImportBatches()` hook to fetch recent batches
- Display as a table with columns: Date, Type, File, Status, Created, Updated, Errors, Uploaded By
- Status column uses colored badges: completed (green), failed (red), duplicate (blue), pending (yellow)
- Date formatted with date-fns `format(new Date(created_at), 'MMM d, yyyy h:mm a')`
- Loading state: skeleton rows
- Empty state: "No import history yet" message
- Show most recent 20 records (slice from the 50 returned by API)

**ExportSection.tsx** - Export cards moved from old page:
- Section header: "Export Data" with Download icon
- Reuses existing `<ExportCard>` component (no changes needed to ExportCard.tsx)
- Grid layout (2 columns on md+) with Contact Export and Donation Export cards
- Move the existing export logic from ImportExport.tsx (useExportContacts, useExportDonations, scope descriptions) into this component
- Accept `user` prop from parent or use useAuth() directly

**ImportExport.tsx** - Complete page rewrite:
- Remove all old import card/tab logic
- New structure using Section/Container layout:
  ```
  <Section>
    <Container>
      <h1>Import & Export</h1>
      <p>subtitle</p>
      <div className="space-y-10">
        <REImportSection />
        <SmartsheetSection />
        <GenericImportSection />
        <Separator />
        <ExportSection />
        <Separator />
        <ImportHistorySection />
      </div>
    </Container>
  </Section>
  ```
- Admin guard: Show RE Imports and Smartsheet sections only for admin users. Export and History sections visible to all authenticated users. Generic section visible to all (since it's just a placeholder).
- Use `useAuth()` to check user role for conditional rendering
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Check page renders all 5 sections with correct layout, RE tabs switch correctly, upload flow works end-to-end</manual>
  </verify>
  <done>ImportExport page renders all sections (RE Imports with 4 tabs, Smartsheet, Generic placeholder, Exports, Import History). Each RE tab has drag-drop upload, result banner, and CSV header reference. Admin-only gating on import sections. Import history shows recent batches.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. ImportExport page renders with all 5 sections visible
3. RE tabs switch between Constituent/Solicitor/Gift/Recurring Gift
4. File drop zone accepts files via drag-drop and click-to-browse
5. CSV header reference displays correct columns per import type
6. Generic section shows "Coming soon" placeholders
7. Export section preserves existing export functionality
8. Import history loads and displays ImportBatch records
</verification>

<success_criteria>
The unified Import/Export page is fully functional with all sections, RE import tabs work end-to-end (upload file -> see result banner), and import history displays recent imports. The page is accessible at /import-export from the sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/32-import-ui/32-02-SUMMARY.md`
</output>
