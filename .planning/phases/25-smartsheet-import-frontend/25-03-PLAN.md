---
phase: 25-smartsheet-import-frontend
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - frontend/src/components/mpd/MPDOverviewTable.tsx
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Admin dashboard shows an MPD Overview section below existing content"
    - "MPD Overview is a sortable table with one row per missionary"
    - "Table shows MPD Cap, Roll Forward Balance, and Months Remaining columns"
    - "Empty state shown when no MPD data exists"
  artifacts:
    - path: "frontend/src/components/mpd/MPDOverviewTable.tsx"
      provides: "Sortable MPD summary table component"
      contains: "MPDOverviewTable"
    - path: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      provides: "Admin dashboard with MPD section"
      contains: "MPDOverviewTable"
  key_links:
    - from: "frontend/src/components/mpd/MPDOverviewTable.tsx"
      to: "frontend/src/hooks/useMPD.ts"
      via: "useMPDOverview hook for data fetching"
      pattern: "useMPDOverview"
    - from: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      to: "frontend/src/components/mpd/MPDOverviewTable.tsx"
      via: "Component import and rendering"
      pattern: "MPDOverviewTable"
---

<objective>
Add MPD Overview sortable table to the admin analytics dashboard.

Purpose: Admin needs to see latest MPD financial data per missionary (MPD Cap, Roll Forward Balance, Months Remaining) on the analytics dashboard to monitor team financial health. Per locked decision: "new MPD Overview section below existing dashboard content" as a "sortable summary table."

Output: MPDOverviewTable component and updated AdminAnalyticsDashboard with MPD section.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-smartsheet-import-frontend/25-RESEARCH.md
@.planning/phases/25-smartsheet-import-frontend/25-02-SUMMARY.md
@frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
@frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx
@frontend/src/api/mpd.ts
@frontend/src/hooks/useMPD.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MPDOverviewTable component with TanStack Table sorting</name>
  <files>frontend/src/components/mpd/MPDOverviewTable.tsx</files>
  <action>
Create frontend/src/components/mpd/MPDOverviewTable.tsx — a self-contained widget that fetches and displays MPD data using TanStack Table for sorting. Follow the TeamActivityTable.tsx pattern exactly.

Component structure:
- Uses useMPDOverview() hook to fetch data
- Card wrapper with CardHeader ("MPD Overview") and CardContent containing the table
- Loading state: skeleton rows (match TeamActivityTable pattern)
- Empty state: "No MPD data available. Upload a Smartsheet report from the Import Center." with muted-foreground text
- Error state: destructive text message

TanStack Table setup (follow TeamActivityTable pattern):
```typescript
import {
  useReactTable,
  getCoreRowModel,
  getSortedRowModel,
  createColumnHelper,
  flexRender,
  type SortingState,
} from "@tanstack/react-table"
```

Column definitions using createColumnHelper<MPDMissionaryOverview>():
1. **Missionary** — accessor "user_name", sortable, cell renders name as plain text
2. **MPD Cap** — accessor "current_mpd_cap", sortable, cell renders with formatMPDCurrency(). Custom sortingFn that handles null and string-to-number conversion: parse to float for comparison, null sorts last.
3. **Roll Forward Balance** — accessor "latest_roll_forward_balance", sortable, same currency formatting and sorting as MPD Cap
4. **Months Remaining** — accessor "months_remaining_rf", sortable, cell renders as-is (it's a string — can be numeric or "infinite"). Custom sortingFn: "infinite" sorts as Infinity, numeric strings parsed to float, empty strings sort last.

Sortable headers: Use the exact pattern from TeamActivityTable — button with ArrowUpDown icon that toggles sorting on click.

Table styling: Use the same Table/TableBody/TableCell/TableHead/TableHeader/TableRow components from shadcn/ui (consistent with other tables in the app).

Import formatMPDCurrency from "@/api/mpd" and MPDMissionaryOverview type.
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit --pretty 2>&1 | head -30` — no type errors
  </verify>
  <done>MPDOverviewTable is a self-contained sortable table widget showing missionary name, MPD Cap, Roll Forward Balance, and Months Remaining with proper currency formatting and null handling.</done>
</task>

<task type="auto">
  <name>Task 2: Add MPD Overview section to AdminAnalyticsDashboard</name>
  <files>frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx</files>
  <action>
Modify AdminAnalyticsDashboard.tsx to include the MPDOverviewTable below existing content. Per locked decision: "new MPD Overview section below existing dashboard content" (not integrated into team table).

Add import at top of file:
```typescript
import { MPDOverviewTable } from "@/components/mpd/MPDOverviewTable"
```

Add the MPD section in the JSX, after the existing Comparison Row (the last grid with TimePeriodComparison and UserComparison) and before the drill-down panels. Place it as a full-width section:

```tsx
{/* MPD Overview - Full Width */}
<MPDOverviewTable />
```

This goes right before the `</div>` that closes the `<div className="space-y-6">` container, after the UserComparison grid.

The MPDOverviewTable is self-contained (fetches its own data via useMPDOverview) following the "each widget manages its own data fetching independently" pattern established in Phase 16.
  </action>
  <verify>
Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit --pretty 2>&1 | head -30` — no type errors
Verify AdminAnalyticsDashboard.tsx contains MPDOverviewTable import and usage.
  </verify>
  <done>Admin analytics dashboard includes MPD Overview sortable table as a new section below existing content. Table shows each missionary's MPD Cap, Roll Forward Balance, and Months Remaining.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors
2. MPDOverviewTable component exists with TanStack Table sorting
3. AdminAnalyticsDashboard renders MPDOverviewTable
4. Table columns: Missionary, MPD Cap, Roll Forward Balance, Months Remaining
5. Currency values formatted with formatMPDCurrency (handles string-to-number, null → "--")
6. months_remaining_rf displayed as-is (handles "infinite" string)
</verification>

<success_criteria>
- Admin dashboard has new "MPD Overview" section below existing content
- Table is sortable by all columns (name, MPD Cap, balance, months remaining)
- Currency values show as formatted USD (e.g., "$3,085")
- Months Remaining shows as-is including "infinite" where applicable
- Empty state shows meaningful message when no MPD data exists
- Loading and error states are handled
</success_criteria>

<output>
After completion, create `.planning/phases/25-smartsheet-import-frontend/25-03-SUMMARY.md`
</output>
