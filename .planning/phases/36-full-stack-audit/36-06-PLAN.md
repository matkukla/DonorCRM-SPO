---
phase: 36-full-stack-audit
plan: 06
type: execute
wave: 3
depends_on:
  - 36-04
  - 36-05
files_modified:
  - apps/gifts/tests/test_views.py
  - apps/gifts/tests/test_signals.py
  - apps/imports/tests/test_re_services.py
  - .planning/phases/36-full-stack-audit/36-AUDIT-REPORT.md
  - .planning/phases/36-full-stack-audit/36-HUMAN-TESTING-CHECKLIST.md
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "Critical code paths have automated test coverage: permission scoping, gift signals, and import pipeline"
    - "A comprehensive audit summary report lists all findings, fixes, and remaining items"
    - "A structured human testing checklist covers all 19 pending manual verification items plus new accessibility checks"
    - "API consistency review is documented with any fixes applied"
  artifacts:
    - path: "apps/gifts/tests/test_views.py"
      provides: "Permission scoping tests for Gift/RecurringGift views"
      contains: "test_.*cannot_see_other"
    - path: "apps/imports/tests/test_re_services.py"
      provides: "Import pipeline tests for RE services"
      contains: "test_import"
    - path: ".planning/phases/36-full-stack-audit/36-AUDIT-REPORT.md"
      provides: "Comprehensive audit summary"
      contains: "Findings"
    - path: ".planning/phases/36-full-stack-audit/36-HUMAN-TESTING-CHECKLIST.md"
      provides: "Structured testing checklist"
      contains: "checkbox"
  key_links:
    - from: "apps/gifts/tests/test_views.py"
      to: "apps/gifts/views.py"
      via: "API test client calls"
      pattern: "self.client.get.*gifts"
    - from: "apps/imports/tests/test_re_services.py"
      to: "apps/imports/re_services.py"
      via: "direct function calls"
      pattern: "import_re_"
---

<objective>
Add critical tests for untested code paths, review API consistency, and produce the final audit report with a human testing checklist.

Purpose: Tests for permission scoping and import pipeline are the most critical gaps -- these are security and data integrity paths. The audit report documents all findings and fixes. The human testing checklist consolidates 19 pending verification items plus accessibility checks into a single actionable document.

Output: New test files for gifts and imports. Audit summary report. Human testing checklist. API consistency review documented.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-full-stack-audit/36-RESEARCH.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/36-full-stack-audit/36-01-SUMMARY.md
@.planning/phases/36-full-stack-audit/36-02-SUMMARY.md
@.planning/phases/36-full-stack-audit/36-03-SUMMARY.md
@.planning/phases/36-full-stack-audit/36-04-SUMMARY.md
@.planning/phases/36-full-stack-audit/36-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add critical tests for permission scoping, gift signals, and import pipeline</name>
  <files>
    apps/gifts/tests/test_views.py
    apps/gifts/tests/test_signals.py
    apps/imports/tests/test_re_services.py
  </files>
  <action>
Research found these critical untested paths: gifts/ has no test files, imports/re_services.py has no tests, permission scoping tests are missing.

**A. Gift permission scoping tests (apps/gifts/tests/test_views.py):**
Create a new test file with tests that verify:
1. Staff user can only see their own gifts (not other users' gifts)
2. Admin user can see all gifts
3. Finance user can see all gifts (read access)
4. Read-only user can see their own gifts
5. Same pattern for RecurringGift views

Use the existing test infrastructure (check apps/contacts/tests/ or apps/journals/tests/ for patterns). Create two users, create gifts owned by each, and verify cross-user isolation:
```python
class TestGiftPermissionScoping(APITestCase):
    def setUp(self):
        self.user1 = User.objects.create_user(email='user1@test.com', password='test', role='staff')
        self.user2 = User.objects.create_user(email='user2@test.com', password='test', role='staff')
        self.admin = User.objects.create_user(email='admin@test.com', password='test', role='admin')
        # Create contacts and gifts for each user
        ...

    def test_staff_user_cannot_see_other_users_gifts(self):
        self.client.force_authenticate(self.user1)
        response = self.client.get('/api/v1/gifts/')
        # Should only contain user1's gifts

    def test_admin_can_see_all_gifts(self):
        self.client.force_authenticate(self.admin)
        response = self.client.get('/api/v1/gifts/')
        # Should contain all gifts
```
Adapt this pattern to the actual User model and Gift model structure. Check `apps/users/models.py` for the User model and role field, and `apps/gifts/models.py` for Gift fields.

**B. Gift signal tests (apps/gifts/tests/test_signals.py):**
Research flagged edge case 3.3: "verify signal handles both create and update". Create tests:
1. Creating a Gift triggers contact stat update (total_given, gift_count increase)
2. Updating a Gift amount triggers contact stat recalculation
3. Deleting a Gift triggers contact stat recalculation
4. Gift signal sets needs_thank_you=True on create

Check what signal handler exists in `apps/gifts/signals.py` to understand the exact behavior to test.

**C. Import pipeline tests (apps/imports/tests/test_re_services.py):**
Create basic smoke tests for the 4 RE import functions. These are the most complex code paths in the project (~1500 lines) with zero test coverage:
1. Test import_re_constituents with a minimal valid CSV (2-3 rows)
2. Test import_re_solicitors with a minimal valid CSV
3. Test import_re_gifts with a minimal valid CSV (multi-row grouping)
4. Test import_re_recurring_gifts with a minimal valid CSV
5. Test SHA256 dedup (import same data twice, verify second returns duplicate)
6. Test malformed CSV handling (verify graceful error, not 500)

Use `io.StringIO` to create in-memory CSV files. Check the actual CSV header expectations by reading the RE import functions' header parsing logic.

Keep tests focused and fast -- each test should run in under 2 seconds.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python -m pytest apps/gifts/tests/ apps/imports/tests/test_re_services.py -x --timeout=60 -q 2>&1 | tail -20</automated>
    <manual>All new tests pass. Permission scoping verified. Gift signals verified. Import pipeline smoke tested.</manual>
  </verify>
  <done>Gift permission scoping tests verify cross-user isolation for staff/admin/finance/read_only. Gift signal tests verify create/update/delete stat updates. Import pipeline tests cover all 4 RE functions, dedup, and error handling. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: API consistency review + audit report + human testing checklist</name>
  <files>
    .planning/phases/36-full-stack-audit/36-AUDIT-REPORT.md
    .planning/phases/36-full-stack-audit/36-HUMAN-TESTING-CHECKLIST.md
  </files>
  <action>
Three deliverables:

**A. API consistency review (fix inline, document in report):**
1. Check all remaining API consistency items from research:
   - Pagination: verify no views override pagination unexpectedly (research says FundListView, ContactJournalsView, TodaysFocusView use None -- confirm these are intentional)
   - HTTP status codes: verify 201 for creates, 204 for deletes, 400 for validation
   - Action endpoint naming: document inconsistencies (some use sub-paths, some use separate resources) but don't change -- these are established API contracts
   - CORS: verify CORS_ALLOWED_ORIGINS is properly configured from environment variables (not hardcoded)
2. Fix any consistency issues found that don't break API contracts. Document all findings.

**B. Audit summary report (.planning/phases/36-full-stack-audit/36-AUDIT-REPORT.md):**
Create a comprehensive report that summarizes ALL work done across plans 01-06:

```markdown
# Phase 36: Full-Stack Audit Report

**Completed:** {date}
**Scope:** Full codebase (12 Django apps, React frontend)

## Summary Statistics

| Dimension | Issues Found | Issues Fixed | Remaining |
|-----------|-------------|-------------|-----------|
| Known Tech Debt | 6 | 6 | 0 |
| Security | X | X | X |
| Performance | X | X | X |
| Code Quality | X | X | X |
| UI/UX | X | X | X |
| API Consistency | X | X | X |
| Tests Added | N/A | X tests | N/A |

## Findings by Dimension

### 1. Known Tech Debt (Plan 01)
{List each fix with before/after}

### 2. Security (Plan 02)
{List each fix}

### 3. Performance (Plan 03)
{List each optimization}

### 4. Code Quality (Plan 04)
{List removals and unifications}

### 5. UI/UX (Plan 05)
{List dark mode and accessibility fixes}

### 6. API Consistency (Plan 06)
{List findings and any fixes}

### 7. Tests Added (Plan 06)
{List new test files and what they cover}

## Remaining Items
{Items that were too risky to fix inline, or need human verification}

## Recommendations
{Future improvements identified during audit}
```

Fill in actual numbers and details from the SUMMARYs of plans 01-05 and the work done in this plan.

**C. Human testing checklist (.planning/phases/36-full-stack-audit/36-HUMAN-TESTING-CHECKLIST.md):**
Consolidate the 19 pending manual verification items from the v2.0-MILESTONE-AUDIT.md plus new items from this audit:

```markdown
# Human Testing Checklist

## How to Use
Test in Chrome DevTools. Toggle dark mode via the theme toggle button.
Base URL: {local dev URL, e.g., http://localhost:5173}

## Phase 30: Data Migration
- [ ] Verify gift/recurring gift counts match old donation/pledge counts

## Phase 31: Gift & Recurring Gift UI
- [ ] Donations list page loads and displays gift data
- [ ] Solicitor credit panel shows on gift detail (when credits exist)
- [ ] Pledge status/frequency filters work with new options
- [ ] Dashboard "Late detection coming soon" placeholder removed (was fixed in Plan 01)

## Phase 32: Import UI
- [ ] Drag-and-drop file upload works in RE import tabs
- [ ] Upload RE Constituent CSV end-to-end
- [ ] Import history auto-refreshes after upload
- [ ] Import tabs are admin-only (non-admin users see appropriate restriction)

## Phase 33: Prayer Intentions
- [ ] Prayer page loads with amber chapel aesthetic
- [ ] Focus mode keyboard navigation (arrow keys, space to mark prayed)
- [ ] Answered note dialog appears before marking as answered
- [ ] Contact detail Prayer tab shows intentions for that contact

## Phase 34: Dashboard Polish
- [ ] Drag dashboard tiles to reorder (grab handle visible)
- [ ] Drop indicator appears during drag
- [ ] Tile order resets on page refresh
- [ ] Drag works on Needs Attention, What Changed, Quick Actions sections
- [ ] No tile "snapping" issues or visual glitches
- [ ] Keyboard drag (Tab to handle, Enter to pick up, arrows to move)

## Phase 36: Audit Additions
- [ ] Dark mode: Prayer focus mode renders correctly
- [ ] Dark mode: Import result banners (success/error/duplicate) render correctly
- [ ] Dark mode: Admin analytics charts/heatmaps have visible colors
- [ ] Accessibility: Tab through sidebar items, verify focus ring visible
- [ ] Accessibility: Tab through data table, verify keyboard navigation
- [ ] Gift amount filter: filter by $100 min shows correct gifts
- [ ] Import history shows "Duplicate" status for re-uploaded files
```

Compile from the actual v2.0-MILESTONE-AUDIT.md human verification items and add accessibility/audit items.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && ls -la .planning/phases/36-full-stack-audit/36-AUDIT-REPORT.md .planning/phases/36-full-stack-audit/36-HUMAN-TESTING-CHECKLIST.md</automated>
    <manual>Report exists with all dimensions covered. Checklist exists with all 19+ items as checkboxes.</manual>
  </verify>
  <done>API consistency reviewed and documented. Audit report covers all 6 dimensions with counts and before/after details. Human testing checklist has 25+ items covering all pending manual verifications plus new audit items.</done>
</task>

</tasks>

<verification>
1. New test files exist and all tests pass
2. Audit report covers all 6 audit dimensions with specific findings
3. Human testing checklist has checkboxes for all pending manual verification items
4. API consistency review documented in report
5. Overall test suite still passes
</verification>

<success_criteria>
- Permission scoping tests verify staff isolation and admin visibility for Gift and RecurringGift
- Gift signal tests verify stat updates on create, update, and delete
- Import pipeline tests cover all 4 RE functions with minimal CSV inputs
- Audit report exists with statistics table, findings by dimension, and remaining items
- Human testing checklist has 25+ actionable items with checkboxes
- API consistency findings documented
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-full-stack-audit/36-06-SUMMARY.md`
</output>
