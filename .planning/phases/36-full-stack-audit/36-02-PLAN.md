---
phase: 36-full-stack-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/views.py
  - apps/insights/views.py
  - apps/tasks/views.py
  - apps/core/permissions.py
  - frontend/src/components/ProtectedRoute.tsx
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "Malformed query parameters (non-numeric 'days', 'year', etc.) return safe defaults instead of 500 errors"
    - "Read-only users cannot navigate to create/edit form routes in the frontend"
    - "Import parsers handle malformed CSV edge cases gracefully without 500 errors"
    - "All OWASP-relevant input validation patterns are applied to API endpoints"
  artifacts:
    - path: "apps/dashboard/views.py"
      provides: "Safe int parsing on all query parameters"
      contains: "get_safe_int_param"
    - path: "apps/insights/views.py"
      provides: "Safe int parsing on all query parameters"
      contains: "get_safe_int_param"
    - path: "apps/tasks/views.py"
      provides: "Safe int parsing on all query parameters"
      contains: "get_safe_int_param"
    - path: "frontend/src/components/ProtectedRoute.tsx"
      provides: "Write-route protection for read_only users"
      contains: "requiredRole"
  key_links:
    - from: "apps/dashboard/views.py"
      to: "apps/insights/views.py get_safe_int_param"
      via: "shared utility import or local implementation"
      pattern: "get_safe_int_param"
    - from: "frontend/src/components/ProtectedRoute.tsx"
      to: "frontend/src/App.tsx routes"
      via: "ProtectedRoute wrapper on write routes"
      pattern: "ProtectedRoute"
---

<objective>
Audit and fix security vulnerabilities across the entire codebase: unvalidated input parsing, missing write-route guards, and import parser edge cases.

Purpose: Security hardening is the highest-priority audit dimension. Unvalidated int() casts cause 500 errors on 11+ endpoints. Read-only users can navigate to write forms. Import parsers need edge case review.

Output: All int() query parameter parsing uses safe defaults. Write routes guarded for read_only users. Import parsers reviewed for malformed input handling.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-full-stack-audit/36-RESEARCH.md
@.planning/EDGE_CASE_AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all unvalidated int() casts across backend views</name>
  <files>
    apps/dashboard/views.py
    apps/insights/views.py
    apps/tasks/views.py
  </files>
  <action>
The `get_safe_int_param()` utility already exists in `apps/insights/views.py` (lines ~47-53). Apply it to all bare `int()` casts on query parameters across the codebase.

Research identified 11 instances:
- `apps/dashboard/views.py` lines ~104, ~163, ~164, ~219 (LateDonationsView, RecentGiftsView, MonthlyGiftsView)
- `apps/insights/views.py` lines ~89, ~118, ~136, ~171, ~172 (DonationsByYearView, LateDonationsView, FollowUpsView, TransactionsView)
- `apps/tasks/views.py` line ~123 (UpcomingTasksView)

For each file:
1. If the file doesn't already import `get_safe_int_param`, either import it from insights or copy the utility to `apps/core/utils.py` as a shared function (preferred -- avoids cross-app imports). The function:
   ```python
   def get_safe_int_param(request, key, default, min_val=1, max_val=1000):
       """Safely parse integer query parameter with bounds."""
       try:
           value = int(request.query_params.get(key, default))
       except (ValueError, TypeError):
           return default
       return max(min_val, min(value, max_val))
   ```
2. Replace every bare `int(request.query_params.get(...))` with `get_safe_int_param(request, ...)` using appropriate defaults and bounds.
3. For `year` parameters specifically (e.g., in GivingSummaryView), set min_val=2000, max_val=2100.
4. Search the ENTIRE backend for any other bare `int(request.query_params` or `int(request.GET` patterns that were missed in research.

Also fix `GivingSummaryView` in dashboard/views.py which does `int(year)` without try/except (from API consistency findings).
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && grep -rn "int(request\.\(query_params\|GET\)\.get" apps/ --include="*.py" | grep -v "get_safe_int_param" | grep -v "test_" | wc -l</automated>
    <manual>Count should be 0. All bare int() casts on request parameters replaced with get_safe_int_param.</manual>
  </verify>
  <done>Zero bare int() casts on request query parameters remain. All use get_safe_int_param with appropriate defaults and bounds. Utility is in apps/core/utils.py as shared function.</done>
</task>

<task type="auto">
  <name>Task 2: Add write-route guards and review import parser input validation</name>
  <files>
    apps/core/permissions.py
    frontend/src/components/ProtectedRoute.tsx
    frontend/src/App.tsx
    apps/imports/re_services.py
  </files>
  <action>
Two security fixes:

**A. Frontend write-route guards (ProtectedRoute):**
Research found that `ProtectedRoute` with `requiredRole` exists for admin routes, but write routes are NOT guarded -- `read_only` users can navigate to `/contacts/new`, `/donations/new`, etc.

1. Read the current `ProtectedRoute` component to understand its interface (it has a `requiredRole` prop).
2. In `App.tsx`, find all routes that lead to create/edit forms (look for routes like `/contacts/new`, `/contacts/:id/edit`, `/donations/new`, `/pledges/new`, `/journals/new`, etc.).
3. Wrap each write route with `<ProtectedRoute requiredRole="staff">` (or the minimum role that should have write access -- check the existing role hierarchy in the User model: STAFF, ADMIN, FINANCE, READ_ONLY). The correct guard is: prevent `read_only` users from accessing write routes. Use `requiredRole` that excludes read_only but includes staff, admin, and finance.
4. If `ProtectedRoute` doesn't support the exact semantics needed (e.g., it checks for exact role match rather than "at least this role"), modify it to support `excludeRoles={['read_only']}` or a comparable pattern.

**B. Import parser edge case review (apps/imports/re_services.py):**
1. Review all 4 RE import functions for input validation edge cases:
   - Extremely long field values (>10000 chars): Add max length truncation on string fields before DB save
   - Null bytes in CSV data: Strip `\x00` from all string values
   - Empty/whitespace-only required fields: Ensure they're treated as missing
2. The file already has encoding detection (UTF-8-sig, UTF-8, Windows-1252 cascade). Verify it handles completely binary/corrupted files gracefully (returns error, not 500).
3. Add a try/except around the CSV reader instantiation that catches `csv.Error` for malformed CSV files and returns a user-friendly error message in the ImportBatch result.

Note: This is not pen-test depth -- focus on the real attack surface of CSV upload.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python -m pytest apps/imports/ -x --timeout=30 -q 2>&1 | tail -20 && cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Navigate to a write route (e.g., /contacts/new) as a read_only user -- should redirect. Upload a corrupted CSV -- should show error, not 500.</manual>
  </verify>
  <done>Read-only users redirected from all write routes. Import parsers handle null bytes, overly long values, and corrupted CSVs gracefully. No 500 errors on malformed input.</done>
</task>

</tasks>

<verification>
1. `grep -rn "int(request" apps/ --include="*.py"` shows zero bare int() casts on query params
2. ProtectedRoute guards all write routes in App.tsx
3. Import parsers handle edge cases without 500 errors
4. Backend tests pass, frontend compiles
</verification>

<success_criteria>
- All 11+ unvalidated int() casts replaced with get_safe_int_param
- get_safe_int_param lives in apps/core/utils.py as shared utility
- Write routes in App.tsx wrapped with ProtectedRoute for read_only exclusion
- Import parsers strip null bytes, truncate oversized values, catch csv.Error
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-full-stack-audit/36-02-SUMMARY.md`
</output>
