---
phase: 1
slug: duplicate-contact-checking-merging-github-issue-37
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-27
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the Duplicate Contact Checking + Merging feature. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (manual, no components.json) |
| Preset | not applicable (components installed individually) |
| Component library | Radix UI primitives via shadcn/ui |
| Icon library | lucide-react |
| Font | Inter, system-ui, sans-serif |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Compact element spacing, gap-2 |
| md | 16px | Default element spacing, gap-4, p-4 |
| lg | 24px | Card padding (p-6), section gaps |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Page-level vertical spacing |
| 3xl | 64px | Not used in this phase |

Exceptions: 44px minimum touch targets for merge radio buttons and action buttons (accessibility requirement).

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm` |
| Label | 14px | 500 (medium) | 1.4 | `text-sm font-medium` |
| Heading | 30px | 600 (semibold) | 1.2 | `text-3xl font-semibold tracking-tight` |
| Subheading | 24px | 600 (semibold) | 1.2 | `text-2xl font-semibold` (CardTitle) |

Source: Established project convention from ContactList.tsx (h1 text-3xl), card.tsx (CardTitle text-2xl), dialog.tsx (DialogTitle text-lg font-semibold).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(0 0% 100%)` / `--background` | Page background, surfaces |
| Secondary (30%) | `hsl(220 30% 95%)` / `--secondary` | Cards, sidebar, muted panels, comparison card backgrounds |
| Accent (10%) | `hsl(348 85% 61%)` / `--primary` | Primary CTA buttons, active nav, merge action button |
| Destructive | `hsl(355 62% 58%)` / `--destructive` | Delete/discard contact button, error states |

Accent reserved for:
- "Merge Contacts" primary CTA button (outline-first: red border + text, solid red on hover)
- "Scan for Duplicates" primary CTA
- Active sidebar nav item highlight (bg-primary/10 text-primary)
- Confidence badge "High" tier background tint

### Confidence Badge Colors

| Tier | Criteria | Badge Variant | Visual |
|------|----------|---------------|--------|
| High | Exact email or phone match | `destructive` | Red background, white text |
| Medium | Name similarity >= 0.6 | `warning` | Amber background, amber text |
| Low | Name similarity >= 0.4 | `secondary` | Gray background, gray text |

Source: CONTEXT.md locked decision on 3-tier confidence scoring. Badge variants from existing badge.tsx.

---

## Component Inventory

### New shadcn Components Required

| Component | Source | Purpose |
|-----------|--------|---------|
| RadioGroup | `npx shadcn@latest add radio-group` | Field-by-field merge value selection |
| AlertDialog | `npx shadcn@latest add alert-dialog` | Merge confirmation (destructive action) |

### Existing Components Reused

| Component | Usage in This Phase |
|-----------|-------------------|
| Card, CardHeader, CardContent, CardTitle, CardDescription | Duplicate pair comparison cards, merge field cards |
| Badge (success, warning, secondary, destructive variants) | Confidence tier badges, field conflict indicators |
| Button (default, secondary, destructive, ghost variants) | CTAs, dismiss, merge, create anyway |
| Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter | Creation-time duplicate warning modal |
| DataTable | Duplicate pairs list on /contacts/duplicates |
| Table, TableRow, TableCell | Field-by-field comparison rows in merge view |
| Input | Search/filter on duplicates page |
| DropdownMenu | Confidence filter, bulk actions |
| FilterBar | Duplicates page filtering |

### New Components to Build

| Component | Location | Purpose |
|-----------|----------|---------|
| DuplicateWarningDialog | `frontend/src/pages/contacts/components/DuplicateWarningDialog.tsx` | Modal shown during contact creation when duplicates detected |
| DuplicateList | `frontend/src/pages/contacts/DuplicateList.tsx` | Page at /contacts/duplicates listing all duplicate pairs |
| DuplicateMergeView | `frontend/src/pages/contacts/DuplicateMergeView.tsx` | Side-by-side comparison + field-by-field merge UI |
| ConfidenceBadge | `frontend/src/pages/contacts/components/ConfidenceBadge.tsx` | Reusable badge mapping confidence tier to badge variant |
| MergeFieldRow | `frontend/src/pages/contacts/components/MergeFieldRow.tsx` | Single field row with left/right radio selection |

---

## Page Layouts

### 1. Duplicate List Page (`/contacts/duplicates`)

**Layout:** Section > Container > space-y-6 (matches ContactList pattern)

```
+--------------------------------------------------+
| [Back to Contacts]                                |
| h1: Potential Duplicates                          |
| p: Review and resolve duplicate contacts          |
|                                                   |
| [Scan for Duplicates]          [Filter: All ▼]   |
|                                                   |
| +----------------------------------------------+ |
| | DataTable                                     | |
| | Columns: Contact A | Contact B | Confidence  | |
| |          | Match Reason | Actions             | |
| |                                               | |
| | John Smith    Jon Smith     [High]            | |
| | john@...      jon@...       Email match       | |
| |                          [Review] [Dismiss]   | |
| +----------------------------------------------+ |
|                                                   |
| Empty state (when no duplicates found)            |
+--------------------------------------------------+
```

**Header:** Same pattern as ContactList — h1 left, CTA button right.
**Table:** DataTable with server-side pagination (PAGE_SIZE = 20).
**Row click:** Navigates to DuplicateMergeView for that pair.

### 2. Duplicate Merge View (`/contacts/duplicates/:pairId`)

**Layout:** Section > Container > max-w-4xl mx-auto (wider than ContactForm's max-w-2xl to accommodate side-by-side)

```
+--------------------------------------------------+
| [Back to Duplicates]                              |
| h1: Merge Contacts                                |
| p: Compare and merge duplicate records            |
|                                                   |
| Confidence: [High] — Exact email match            |
|                                                   |
| +--- Survivor Selection -----------------------+ |
| |  ( ) Keep Left: John Smith                    | |
| |  ( ) Keep Right: Jon Smith                    | |
| +----------------------------------------------+ |
|                                                   |
| +--- Field Comparison --------------------------+ |
| | Card: Basic Information                        | |
| |                                                | |
| | Field        | Left (John)  | Right (Jon)     | |
| |--------------|--------------|-----------------|  |
| | First Name   | (o) John     | ( ) Jon         | |
| | Last Name    | (o) Smith    | (o) Smith       | |
| | Email        | (o) john@    | ( ) jon@        | |
| | Phone        | (o) 555-1234 | ( ) —           | |
| | Status       | (o) Donor    | ( ) Prospect    | |
| +----------------------------------------------+ |
|                                                   |
| +--- Related Records ---------------------------+ |
| | Card: Records to Migrate                       | |
| |                                                | |
| | From "Jon Smith" to "John Smith":              | |
| |   • 3 Gifts ($1,200 total)                    | |
| |   • 1 Recurring Gift                          | |
| |   • 2 Tasks                                   | |
| |   • 1 Journal Entry                           | |
| +----------------------------------------------+ |
|                                                   |
| [Cancel]                     [Merge Contacts]     |
+--------------------------------------------------+
```

**Survivor selection:** RadioGroup at top — picking a side pre-populates all field radios to that side's values.
**Field rows:** Table rows with RadioGroup per field. Pre-selected to survivor's values. Fields with identical values show a single value with no radio (no conflict).
**Related records summary:** Read-only Card showing FK counts that will be reassigned.
**Merge CTA:** AlertDialog confirmation before executing.

### 3. Creation-Time Warning Dialog

**Trigger:** ContactForm handleSubmit intercepts before save, calls duplicate check API.
**Component:** Dialog (not AlertDialog — this is informational, not destructive).

```
+------------------------------------------+
| Dialog                                    |
|                                           |
| h2: Possible Duplicates Found             |
| p: We found contacts that may be          |
|    duplicates of the one you're creating. |
|                                           |
| +--- Match Card 1 ---------[High]------+ |
| | John Smith                            | |
| | john@example.com | 555-1234           | |
| | [View Contact]                        | |
| +--------------------------------------+ |
|                                           |
| +--- Match Card 2 ---------[Medium]----+ |
| | Jon Smith                             | |
| | — | 555-5678                          | |
| | [View Contact]                        | |
| +--------------------------------------+ |
|                                           |
| [Create Anyway]         [Cancel]          |
+------------------------------------------+
```

**Max matches shown:** 3 (CONTEXT.md locked decision).
**Match card:** Card with contact name, email, phone, confidence badge.
**"View Contact":** Opens contact detail in new tab.
**"Create Anyway":** Bypasses check, proceeds with save (CONTEXT.md: no forced merge at creation time).
**"Cancel":** Closes dialog, returns to form.

### 4. Sidebar Navigation Update

Add "Duplicates" as a sub-item under or near the "Contacts" nav link. Pattern: direct NavLink (not collapsible), same styling as other nav items.

| Property | Value |
|----------|-------|
| Label | "Duplicates" |
| Icon | `GitMerge` from lucide-react (16x16, h-4 w-4 to match sub-nav pattern) |
| Route | `/contacts/duplicates` |
| Position | After "Contacts" in navItems array |
| Visibility | All roles (scoped to owner's data server-side) |

---

## Interaction States

### Loading States

| Context | Treatment |
|---------|-----------|
| Duplicates page initial load | DataTable built-in loading (skeleton rows) |
| Scan for Duplicates button | Button disabled + "Scanning..." text while API runs |
| Merge View field comparison | Skeleton placeholder cards (h-96 bg-muted rounded animate-pulse) matching ContactForm loading pattern |
| Creation-time duplicate check | Button disabled + "Checking..." text, form inputs remain visible |

### Empty States

| Context | Heading | Body |
|---------|---------|------|
| Duplicates page — no duplicates found | No duplicates found | Your contact list looks clean. Run a scan to check for new duplicates. |
| Duplicates page — all dismissed | All duplicates reviewed | You have reviewed all detected duplicate pairs. Run a new scan to check again. |
| Merge View — no conflicting fields | No conflicts | Both contacts have identical information. Choose which record to keep. |

### Error States

| Context | Message | Recovery |
|---------|---------|----------|
| Scan fails | Unable to scan for duplicates. Please try again. | Retry button in toast (sonner) |
| Merge fails | Failed to merge contacts. No changes were made. | Retry button in toast, form state preserved |
| Duplicate check on create fails | Unable to check for duplicates. You can still create the contact. | Toast warning, "Create Anyway" remains available |

### Disabled States

| Element | When Disabled | Visual |
|---------|---------------|--------|
| "Merge Contacts" button | No survivor selected OR merge in progress | `disabled:pointer-events-none disabled:opacity-50` (Button default) |
| "Scan for Duplicates" button | Scan in progress | disabled + "Scanning..." label |
| Field radio buttons | Merge in progress | `disabled:opacity-50` |
| All mutation controls | isViewingAs === true | Hidden (consistent with existing pattern — ContactList hides "Add Contact" when isViewingAs) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (duplicates page) | Scan for Duplicates |
| Primary CTA (merge view) | Merge Contacts |
| Bypass CTA (creation dialog) | Create Anyway |
| Cancel CTA (creation dialog) | Cancel |
| Empty state heading (no duplicates) | No duplicates found |
| Empty state body (no duplicates) | Your contact list looks clean. Run a scan to check for new duplicates. |
| Empty state heading (all reviewed) | All duplicates reviewed |
| Empty state body (all reviewed) | You have reviewed all detected duplicate pairs. Run a new scan to check again. |
| Error state (scan failure) | Unable to scan for duplicates. Please try again. |
| Error state (merge failure) | Failed to merge contacts. No changes were made. |
| Error state (check on create) | Unable to check for duplicates. You can still create the contact. |
| Destructive confirmation heading | Merge these contacts? |
| Destructive confirmation body | This will merge "{loser_name}" into "{survivor_name}". All related records (gifts, tasks, journal entries) will be moved to the surviving contact. This action cannot be undone. |
| Destructive confirmation CTA | Yes, Merge Contacts |
| Destructive cancel | Cancel |
| Dismiss pair label | Not Duplicates |
| Dismiss success toast | Pair dismissed. They won't be flagged again. |
| Merge success toast | Contacts merged successfully. |
| Scan complete toast (N found) | Found {N} potential duplicate pairs. |
| Scan complete toast (0 found) | No new duplicates found. |
| Page title (duplicates list) | Potential Duplicates |
| Page subtitle (duplicates list) | Review and resolve duplicate contacts |
| Page title (merge view) | Merge Contacts |
| Page subtitle (merge view) | Compare and merge duplicate records |
| Dialog title (creation warning) | Possible Duplicates Found |
| Dialog body (creation warning) | We found contacts that may be duplicates of the one you're creating. |
| Survivor selection label | Choose which contact to keep |
| Field comparison heading | Field Comparison |
| Related records heading | Records to Migrate |
| Related records body template | From "{loser_name}" to "{survivor_name}": |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | radio-group, alert-dialog | not required |

No third-party registries declared.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|---------------|
| Radio group keyboard nav | RadioGroup from Radix handles arrow key navigation natively |
| Merge confirmation focus trap | AlertDialog from Radix traps focus within confirmation dialog |
| Confidence badge semantics | Badge text includes tier label ("High", "Medium", "Low") — not color-only |
| Touch targets | All radio buttons and action buttons minimum 44px tap area |
| Screen reader for merge | RadioGroup items use aria-label: "{field_name}: {value}" |
| Dialog announcement | DialogTitle used for all dialogs (Radix announces automatically) |
| Table semantics | Field comparison uses Table with proper TableHead scope="col" |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
