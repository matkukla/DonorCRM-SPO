---
phase: 49-goal-page-data-model-backend
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/core/tests/__init__.py
  - apps/core/tests/test_fiscal_year.py
  - apps/users/tests/test_goal_services.py
  - apps/users/tests/test_views_goals.py
  - apps/users/tests/factories.py
autonomous: true
requirements: [FISC-01, FISC-02, GOAL-02, GOAL-03, GOAL-04]

must_haves:
  truths:
    - "pytest apps/core/tests/test_fiscal_year.py runs (initially FAILING — RED)"
    - "pytest apps/users/tests/test_goal_services.py runs (initially FAILING — RED)"
    - "pytest apps/users/tests/test_views_goals.py runs (initially FAILING — RED)"
    - "UserFactory no longer references monthly_goal; uses monthly_support_goal_cents"
  artifacts:
    - path: "apps/core/tests/test_fiscal_year.py"
      provides: "Failing test stubs for FISC-01, FISC-02 (boundary cases)"
    - path: "apps/users/tests/test_goal_services.py"
      provides: "Failing test stubs for GOAL-04 (effective monthly support calculation)"
    - path: "apps/users/tests/test_views_goals.py"
      provides: "Failing integration test stubs for GOAL-02, GOAL-03"
    - path: "apps/users/tests/factories.py"
      provides: "Factory with renamed field monthly_support_goal_cents"
  key_links:
    - from: "apps/core/tests/test_fiscal_year.py"
      to: "apps/core/fiscal_year.py"
      via: "import from apps.core.fiscal_year"
      pattern: "from apps.core.fiscal_year import"
    - from: "apps/users/tests/test_views_goals.py"
      to: "/api/v1/goals/me/"
      via: "self.client.get('/api/v1/goals/me/')"
      pattern: "goals/me"
---

<objective>
Create the Wave 0 test scaffolds required by the Nyquist rule before any implementation begins. All tests in this plan start as FAILING (RED) — they define the contract that Wave 1–3 tasks must satisfy.

Purpose: Ensure every implementation task in plans 02–04 has an automated verify command that can produce a meaningful result.
Output: Three new test files and one updated factory file. No production code.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-goal-page-data-model-backend/49-CONTEXT.md

<interfaces>
<!-- Existing factories.py field being renamed -->
<!-- apps/users/tests/factories.py line 23 -->
monthly_goal = factory.LazyFunction(lambda: fake.pydecimal(min_value=1000, max_value=10000, right_digits=2))
<!-- After rename → monthly_support_goal_cents = factory.LazyFunction(lambda: fake.randint(100000, 1000000)) -->

<!-- Test framework: pytest + pytest-django, config in pyproject.toml -->
<!-- Pattern: use APIClient from rest_framework.test -->
<!-- Pattern: use UserFactory from apps.users.tests.factories -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create apps/core/tests/ package and test_fiscal_year.py stubs</name>
  <files>apps/core/tests/__init__.py, apps/core/tests/test_fiscal_year.py</files>
  <behavior>
    - Test 1 (FISC-01 — August): fiscal_year_start(date(2025, 8, 15)) == date(2025, 7, 1)
    - Test 2 (FISC-01 — March): fiscal_year_start(date(2026, 3, 12)) == date(2025, 7, 1)
    - Test 3 (FISC-01 — July 1 exact): fiscal_year_start(date(2025, 7, 1)) == date(2025, 7, 1)
    - Test 4 (FISC-01 — June 30): fiscal_year_start(date(2025, 6, 30)) == date(2024, 7, 1)
    - Test 5 (FISC-02 — normal month): months_remaining(date(2025, 8, 15)) == 10 (Aug to June = 10 months)
    - Test 6 (FISC-02 — June minimum): months_remaining(date(2026, 6, 1)) == 1 (min guard)
    - Test 7 (FISC-02 — June 30 minimum): months_remaining(date(2026, 6, 30)) == 1 (min guard, not 0)
    - Test 8 (FISC-01 end): fiscal_year_end(date(2025, 8, 1)) == date(2026, 6, 30)
  </behavior>
  <action>
    Create `apps/core/tests/__init__.py` (empty).

    Create `apps/core/tests/test_fiscal_year.py` with imports:
    ```python
    import pytest
    from datetime import date
    from apps.core.fiscal_year import fiscal_year_start, fiscal_year_end, months_remaining
    ```

    Write all 8 test functions using the behaviors listed. These tests will FAIL (ImportError on `apps.core.fiscal_year`) — that is correct RED state. Do NOT create the fiscal_year.py module yet.

    Mark all tests with `@pytest.mark.django_db` only if they need DB — fiscal year functions are pure Python, no DB needed.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && pytest apps/core/tests/test_fiscal_year.py --no-cov 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects 8 tests, all FAIL with ModuleNotFoundError or ImportError. No syntax errors in the test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_goal_services.py and test_views_goals.py stubs</name>
  <files>apps/users/tests/test_goal_services.py, apps/users/tests/test_views_goals.py</files>
  <behavior>
    test_goal_services.py behaviors:
    - Test 1 (GOAL-04 — no journals): get_goal_progress(user) returns effective_monthly_support == 0.0 when no GoalJournalSelections exist
    - Test 2 (GOAL-04 — recurring only): get_goal_progress(user) returns correct recurring_monthly when journal has contacts with active RecurringGifts; one_time_monthly == 0
    - Test 3 (GOAL-04 — one-time only): get_goal_progress(user) returns correct one_time_monthly (fy_gifts / months_remaining); recurring_monthly == 0
    - Test 4 (GOAL-04 — scoping): get_goal_progress(user) does NOT include gifts from contacts NOT in selected journals

    test_views_goals.py behaviors:
    - Test 1 (GOAL-02 — PATCH saves goal): PATCH /api/v1/goals/me/ with {monthly_support_goal_cents: 350000, goal_weeks: 52} returns 200, user.monthly_support_goal_cents == 350000
    - Test 2 (GOAL-03 — PATCH saves journals): PATCH /api/v1/goals/me/ with {journal_ids: [id1, id2]} replaces selections; GET returns selected_journal_ids == [id1, id2]
    - Test 3 (GOAL-03 — replace-all): PATCH with {journal_ids: [id1]} after previous selections of [id1, id2] leaves only [id1]
    - Test 4 — GET returns effective_monthly_support field
    - Test 5 — unauthenticated GET returns 401
  </behavior>
  <action>
    Create `apps/users/tests/test_goal_services.py`:
    ```python
    import pytest
    from apps.users.goal_services import get_goal_progress
    from apps.users.tests.factories import UserFactory
    ```
    Write test stubs for all 4 behaviors. Use `@pytest.mark.django_db`. Tests will FAIL with ImportError on goal_services.

    Create `apps/users/tests/test_views_goals.py`:
    ```python
    import pytest
    from rest_framework.test import APIClient
    from apps.users.tests.factories import UserFactory
    ```
    Write test stubs for all 5 behaviors. Use `@pytest.mark.django_db`. Tests will FAIL with connection errors to /api/v1/goals/me/ (endpoint does not exist yet).

    For journal-related tests, import Journal factory from apps.journals — if no factory exists, create minimal journal instance directly using `Journal.objects.create(name="Test", owner=user)`.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && pytest apps/users/tests/test_goal_services.py apps/users/tests/test_views_goals.py --no-cov 2>&1 | tail -10</automated>
  </verify>
  <done>pytest collects 9 tests total across both files. All FAIL (ImportError or 404). No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update UserFactory to use monthly_support_goal_cents</name>
  <files>apps/users/tests/factories.py</files>
  <action>
    In `apps/users/tests/factories.py`, rename the `monthly_goal` field to `monthly_support_goal_cents` and change the value generator from Decimal to integer cents:

    Old:
    ```python
    monthly_goal = factory.LazyFunction(lambda: fake.pydecimal(min_value=1000, max_value=10000, right_digits=2))
    ```

    New:
    ```python
    monthly_support_goal_cents = factory.LazyFunction(lambda: fake.random_int(min=100000, max=1000000))
    goal_weeks = 52
    ```

    Also add `goal_weeks = 52` as a default field in `UserFactory`.

    NOTE: This change will cause existing factory-based tests to fail if the migration has not yet run (the DB column is still `monthly_goal`). That is expected at Wave 0 — this factory update is staged here so it is ready the moment the migration runs in Plan 03. If existing tests break, that is acceptable — they will be fixed by Plan 03's migration.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python -c "from apps.users.tests.factories import UserFactory; print('Factory import OK')"</automated>
  </verify>
  <done>Factory module imports without error. monthly_support_goal_cents and goal_weeks fields are present in UserFactory definition.</done>
</task>

</tasks>

<verification>
Run full Wave 0 collect to confirm all test stubs are parseable:
```
cd /home/matkukla/projects/DonorCRM && pytest apps/core/tests/test_fiscal_year.py apps/users/tests/test_goal_services.py apps/users/tests/test_views_goals.py --collect-only --no-cov 2>&1 | tail -15
```
Expected: 17 test items collected, 0 errors.
</verification>

<success_criteria>
- apps/core/tests/__init__.py exists
- apps/core/tests/test_fiscal_year.py has 8 test functions covering FISC-01 and FISC-02 boundary cases
- apps/users/tests/test_goal_services.py has 4 test functions covering GOAL-04
- apps/users/tests/test_views_goals.py has 5 test functions covering GOAL-02, GOAL-03
- apps/users/tests/factories.py uses monthly_support_goal_cents (integer cents) and goal_weeks
- All 17 test items are collected by pytest with no syntax errors
- Tests fail for the right reason (missing modules/endpoints), not syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/49-goal-page-data-model-backend/49-01-SUMMARY.md`
</output>
