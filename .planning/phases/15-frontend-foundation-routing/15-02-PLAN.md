---
phase: 15-frontend-foundation-routing
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
  - frontend/src/pages/admin/analytics/StalledContacts.tsx
  - frontend/src/pages/admin/analytics/UserDetail.tsx
autonomous: true

must_haves:
  truths:
    - "Admin analytics dashboard page renders real data from the dashboard overview endpoint"
    - "Stalled contacts page renders real data from the stalled contacts endpoint"
    - "User detail page renders real data from user performance endpoint for a specific user"
    - "All three pages show loading states while data is fetching"
    - "All three pages show error states when API calls fail"
    - "All three pages include the admin sub-navigation tabs (Users, Import Center, Analytics)"
  artifacts:
    - path: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      provides: "Dashboard overview page with summary cards showing real API data"
      min_lines: 60
    - path: "frontend/src/pages/admin/analytics/StalledContacts.tsx"
      provides: "Stalled contacts list page with real API data"
      min_lines: 60
    - path: "frontend/src/pages/admin/analytics/UserDetail.tsx"
      provides: "User detail page with performance metrics from real API data"
      min_lines: 60
  key_links:
    - from: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      to: "@/hooks/useInsights"
      via: "useAdminDashboardOverview hook"
      pattern: "useAdminDashboardOverview"
    - from: "frontend/src/pages/admin/analytics/StalledContacts.tsx"
      to: "@/hooks/useInsights"
      via: "useAdminStalledContacts hook"
      pattern: "useAdminStalledContacts"
    - from: "frontend/src/pages/admin/analytics/UserDetail.tsx"
      to: "@/hooks/useInsights"
      via: "useAdminUserPerformance hook"
      pattern: "useAdminUserPerformance"
    - from: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      to: "@/api/insights"
      via: "typed response rendering"
      pattern: "data\\.total_contacts|data\\.active_journals|data\\.stalled_contacts"
---

<objective>
Replace the 3 stub admin analytics page components with functional pages that fetch and render real API data using the React Query hooks from Plan 01.

Purpose: Complete the frontend foundation so that admin users can navigate to analytics pages and see real data from the backend. These pages serve as the rendering foundation for the rich visualization work in Phases 16-17.
Output: 3 functional page components with loading states, error states, admin sub-navigation, and real data rendering.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-frontend-foundation-routing/15-RESEARCH.md
@.planning/phases/15-frontend-foundation-routing/15-01-SUMMARY.md

@frontend/src/hooks/useInsights.ts
@frontend/src/api/insights.ts
@frontend/src/pages/admin/AdminUsers.tsx
@frontend/src/pages/admin/ImportCenter.tsx
@apps/insights/serializers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AdminAnalyticsDashboard page with real data</name>
  <files>frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx</files>
  <action>
Replace the stub with a full page component that:

1. **Imports:** Use `useAdminDashboardOverview` from `@/hooks/useInsights`, `useAdminConversionFunnel` from `@/hooks/useInsights`, Section/Container from layout, Card/CardContent/CardHeader/CardTitle from ui/card, NavLink from react-router-dom, cn from `@/lib/utils`.

2. **Admin Sub-Navigation:** Include the same admin sub-nav pattern from AdminUsers.tsx with 3 tabs: Users (`/admin` with `end`), Import Center (`/admin/imports`), Analytics (`/admin/analytics`). Copy the exact className pattern from AdminUsers.tsx.

3. **Loading State:** Show skeleton placeholders (animate-pulse divs) matching the AdminUsers.tsx loading pattern when `isLoading` is true.

4. **Error State:** Show a red error message when `error` is truthy: "Failed to load dashboard data. Please try again."

5. **Data Display:** When data is available, render:
   - **Header:** h1 "Analytics Dashboard" with description "Organization-wide fundraising analytics"
   - **Summary Cards Row:** 4 Card components in a responsive grid (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`):
     - "Total Contacts" showing `data.total_contacts`
     - "Active Journals" showing `data.active_journals`
     - "Stalled Contacts" showing `data.stalled_contacts`
     - "Conversion Rate" showing `data.conversion_rate` formatted as percentage (e.g., `(data.conversion_rate).toFixed(1) + '%'`)
   - **Donations Card:** Below the grid, a Card showing "Donations (12 Months)" with `data.donations_12m.total_amount` formatted as currency (divide by 100 for cents->dollars, use `toLocaleString('en-US', { style: 'currency', currency: 'USD' })`) and `data.donations_12m.total_count` donation count.
   - **Conversion Funnel placeholder:** A Card with title "Conversion Funnel" showing a simple table of funnel stages (stage label, count, percentage) using `useAdminConversionFunnel` data. Use a basic HTML table with Tailwind classes -- Phase 16 will replace this with Recharts visualization.

6. **Card styling:** Each summary card should have:
   - CardHeader with CardTitle showing the metric name (text-sm font-medium text-muted-foreground)
   - CardContent with the value displayed large (text-2xl font-bold)

Do NOT use Recharts or any visualization library yet -- this is a data-rendering foundation. Phase 16 adds charts.

Format money values by dividing cents by 100 and using `toLocaleString`. Do not import any new dependencies beyond what's already in the project.
  </action>
  <verify>
Run `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit` -- TypeScript compiles with no errors.
  </verify>
  <done>
AdminAnalyticsDashboard renders real data from `useAdminDashboardOverview` and `useAdminConversionFunnel` hooks. Shows 4 summary cards, donations card, funnel table. Has loading state, error state, and admin sub-navigation. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build StalledContacts and UserDetail pages with real data</name>
  <files>
    frontend/src/pages/admin/analytics/StalledContacts.tsx
    frontend/src/pages/admin/analytics/UserDetail.tsx
  </files>
  <action>
**StalledContacts.tsx:**

Replace the stub with a full page component that:

1. **Imports:** `useAdminStalledContacts` from `@/hooks/useInsights`, `type StalledContactsParams` from `@/api/insights`, `useState` from react, layout components, Card, Table components (Table, TableBody, TableCell, TableHead, TableHeader, TableRow from `@/components/ui/table`), NavLink, cn, Badge from `@/components/ui/badge`, Link from react-router-dom.

2. **State:** `useState` for params: `{ limit: 50, offset: 0, sort_by: 'days_stalled', sort_dir: 'desc' }` typed as `StalledContactsParams`.

3. **Data fetching:** `useAdminStalledContacts(params)`.

4. **Admin Sub-Navigation:** Same 3-tab pattern (Users, Import Center, Analytics).

5. **Loading/Error states:** Same patterns as AdminAnalyticsDashboard.

6. **Data Display:**
   - **Header:** h1 "Stalled Contacts" with description showing total count: `${data.total_count} contacts with no activity in 14+ days`
   - **Table:** Inside a Card, render a Table with columns: Contact Name, Owner, Last Activity, Days Stalled, Status.
     - Contact Name: `item.full_name` (plain text for now, no link needed)
     - Owner: `item.owner_name`
     - Last Activity: `item.last_activity_date` formatted as readable date, or "Never" if null
     - Days Stalled: `item.days_stalled` with a Badge -- use `variant="destructive"` for >30 days, `variant="warning"` for >14 days, `variant="secondary"` otherwise. Handle null days_stalled by showing "N/A".
     - Status: `item.status` as a Badge
   - **Pagination info:** Below the table show `Showing ${data.stalled_contacts.length} of ${data.total_count} contacts`
   - Do NOT implement pagination controls yet (Phase 17 will add full pagination). Just display the first page.

**UserDetail.tsx:**

Replace the stub with a full page component that:

1. **Imports:** `useAdminUserPerformance` from `@/hooks/useInsights`, `useParams` from react-router-dom, layout components, Card, NavLink, cn, Badge, Link.

2. **Route param:** Extract `:id` from URL using `useParams()`.

3. **Data fetching:** `useAdminUserPerformance()` -- this returns ALL users. Find the specific user by matching `user.id === id` from the returned `data.users` array. (The backend endpoint returns all users in one response, not a per-user endpoint. The UserDetail page filters client-side.)

4. **Admin Sub-Navigation:** Same 3-tab pattern.

5. **Loading/Error states:** Same patterns. Also handle case where user is not found in the data: show "User not found" message with a link back to the dashboard.

6. **Data Display:**
   - **Header:** h1 showing user name (`user.name`) with their email and role as Badge below
   - **Metrics Grid:** 6 cards in a `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`:
     - "Total Contacts" -> `user.total_contacts`
     - "Active Journals" -> `user.active_journals`
     - "Decisions Logged" -> `user.decisions_logged`
     - "Conversion Rate" -> `user.conversion_rate.toFixed(1) + '%'`
     - "Total Donations" -> format `user.total_donations` as currency (cents to dollars)
     - "Donation Count" -> `user.donation_count`
   - **Back link:** A "Back to Dashboard" link (`<Link to="/admin/analytics/dashboard">`) at the top of the page.

Format money values by dividing cents by 100 and using `toLocaleString('en-US', { style: 'currency', currency: 'USD' })`. Format dates using `new Date(dateStr).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`.
  </action>
  <verify>
1. Run `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit` -- TypeScript compiles with no errors.
2. Run `cd /home/matkukla/projects/DonorCRM/frontend && npx vite build` -- Build succeeds with no errors.
  </verify>
  <done>
StalledContacts page renders stalled contacts table from real API data with sorting defaults, loading/error states, and admin sub-navigation. UserDetail page renders user-specific metrics from the user performance API data, handles missing user gracefully, and includes back navigation. Both pages compile and build without errors.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit` -- No TypeScript errors
2. `cd /home/matkukla/projects/DonorCRM/frontend && npx vite build` -- Build succeeds
3. All 3 page components import and use React Query hooks (not raw fetch calls)
4. All 3 page components include admin sub-navigation tabs
5. All 3 page components handle loading and error states
6. AdminAnalyticsDashboard renders summary cards with real field names from DashboardOverviewResponse
7. StalledContacts renders a Table with columns matching StalledContactItem fields
8. UserDetail finds user by :id param and renders UserPerformanceItem fields
</verification>

<success_criteria>
- AdminAnalyticsDashboard shows 4 summary cards + donations card + funnel table using real API data
- StalledContacts shows a table of stalled contacts with days_stalled badges
- UserDetail shows 6 metric cards for a specific user selected by URL :id param
- All pages have loading skeletons, error messages, and admin sub-navigation
- TypeScript compiles and Vite builds without errors
- Pages are ready for Phase 16-17 to enhance with Recharts visualizations and full interactivity
</success_criteria>

<output>
After completion, create `.planning/phases/15-frontend-foundation-routing/15-02-SUMMARY.md`
</output>
