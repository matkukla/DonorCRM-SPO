---
phase: 16-dashboard-overview-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx
  - frontend/src/pages/admin/analytics/components/AlertsPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Team Activity Table renders recent team events with date, user, event type, and description columns"
    - "Team Activity Table supports client-side sorting by date and user columns via TanStack Table"
    - "Alerts Panel displays rule-based coaching prompts with severity-colored boxes (red/amber/blue)"
    - "Alerts Panel computes alerts from dashboard overview and user performance data"
  artifacts:
    - path: "frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx"
      provides: "Sortable team activity table widget"
      contains: "TeamActivityTable"
      min_lines: 40
    - path: "frontend/src/pages/admin/analytics/components/AlertsPanel.tsx"
      provides: "Coaching alerts panel widget"
      contains: "AlertsPanel"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx"
      to: "frontend/src/hooks/useInsights.ts"
      via: "useAdminTeamActivity hook"
      pattern: "useAdminTeamActivity"
    - from: "frontend/src/pages/admin/analytics/components/AlertsPanel.tsx"
      to: "frontend/src/hooks/useInsights.ts"
      via: "useAdminDashboardOverview and useAdminUserPerformance hooks"
      pattern: "useAdminDashboardOverview|useAdminUserPerformance"
---

<objective>
Create the Team Activity Table and Alerts Panel widget components for the dashboard overview page.

Purpose: DASH-02 requires a sortable Team Activity Table showing recent missionary actions. DASH-03 requires an Alerts Panel with rule-based coaching prompts. These are standalone widget components that consume existing React Query hooks and will be integrated into the dashboard page in Plan 03.

Output: Two new widget components -- TeamActivityTable.tsx and AlertsPanel.tsx -- in the analytics components directory, each self-contained with loading states, empty states, and proper data fetching.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing hooks and types to consume
@frontend/src/hooks/useInsights.ts
@frontend/src/api/insights.ts

# Pattern references for widgets
@frontend/src/pages/journals/components/ReportCharts.tsx
@frontend/src/components/dashboard/NeedsAttention.tsx
@frontend/src/components/shared/DataTable.tsx

# Research findings on widget patterns
@.planning/phases/16-dashboard-overview-page/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TeamActivityTable widget with TanStack Table client-side sorting</name>
  <files>frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx</files>
  <action>
    Create `frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx`:

    Build a sortable table widget that shows recent team activity (DASH-02).

    Implementation details:
    - Import useAdminTeamActivity hook from @/hooks/useInsights
    - Import TanStack Table: useReactTable, getCoreRowModel, getSortedRowModel, createColumnHelper, SortingState
    - Import UI components: Card, CardContent, CardHeader, CardTitle from @/components/ui/card
    - Import Table components from @/components/ui/table
    - Import format from date-fns for date formatting
    - Import ArrowUpDown from lucide-react for sort indicators

    Define columns using createColumnHelper<TeamActivityItem>():
    1. `created_at` - Header: "Date", cell: format(new Date(value), "MMM d, yyyy h:mm a"), sortable
    2. `user_name` - Header: "User", sortable
    3. `event_type` - Header: "Event", cell: display with Badge component, use variant mapping (e.g., info->secondary, warning->warning)
    4. `title` - Header: "Description", no sorting

    Component structure:
    - Fetch data: `const { data, isLoading } = useAdminTeamActivity({ limit: 50 })`
    - Manage sort state: `const [sorting, setSorting] = useState<SortingState>([{ id: 'created_at', desc: true }])`
    - Create table instance with useReactTable using getCoreRowModel() AND getSortedRowModel() -- this enables CLIENT-SIDE sorting (no API calls on sort, per research pitfall #4)
    - Loading state: Show skeleton rows (5 rows of animated pulse divs) inside Card, matching DataTable.tsx skeleton pattern
    - Empty state: Show "No recent activity" centered text
    - Render: Card > CardHeader (title "Recent Team Activity") > CardContent > Table with sortable column headers
    - Column headers that are sortable should show ArrowUpDown icon and have cursor-pointer + onClick to toggle sorting

    Do NOT use the existing DataTable shared component because it only supports manual pagination (server-side), not client-side sorting. Build inline with useReactTable directly, following the pattern from DataTable.tsx for rendering but adding getSortedRowModel.

    Export as named export: `export function TeamActivityTable()`
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit`
    TypeScript compiles without errors. Verify file exists and exports TeamActivityTable.
  </verify>
  <done>
    TeamActivityTable component exists at frontend/src/pages/admin/analytics/components/TeamActivityTable.tsx. It renders a sortable table with 4 columns (Date, User, Event, Description). Client-side sorting enabled via TanStack Table getSortedRowModel. Loading and empty states handled. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AlertsPanel widget with rule-based coaching prompts</name>
  <files>frontend/src/pages/admin/analytics/components/AlertsPanel.tsx</files>
  <action>
    Create `frontend/src/pages/admin/analytics/components/AlertsPanel.tsx`:

    Build a coaching alerts panel that computes alerts from existing data (DASH-03).

    Implementation details:
    - Import useAdminDashboardOverview and useAdminUserPerformance from @/hooks/useInsights
    - Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card
    - Import Button from @/components/ui/button
    - Import Link from react-router-dom
    - Import AlertTriangle from lucide-react
    - Import cn from @/lib/utils

    Define a CoachingAlert interface:
    ```typescript
    interface CoachingAlert {
      id: string
      message: string
      severity: "high" | "medium" | "low"
      actionLink?: string
    }
    ```

    Extract alert computation to a standalone `computeAlerts()` function (NOT inline in render -- per research pitfall #3):
    ```typescript
    function computeAlerts(
      overview?: DashboardOverviewResponse,
      users?: UserPerformanceItem[]
    ): CoachingAlert[]
    ```

    Alert rules to implement:
    1. HIGH severity: If stalled_contacts > 20, show "{N} contacts stalled >14 days across team" with link to /admin/analytics/stalled-contacts
    2. MEDIUM severity: For each user with conversion_rate < 10 AND total_contacts >= 5, show "{name} has {rate}% conversion rate" with link to /admin/analytics/users/{id}
    3. MEDIUM severity: If any user has 0 active_journals AND total_contacts > 0, show "{name} has {N} contacts but no active journals" with link to /admin/analytics/users/{id}
    4. LOW severity: If team conversion_rate < 15, show "Team conversion rate at {rate}% (below 15% threshold)"
    5. LOW severity: If active_journals == 0, show "No active journals across team"

    Component structure:
    - Fetch data: `const { data: overview } = useAdminDashboardOverview()` and `const { data: users } = useAdminUserPerformance()`
    - Compute alerts: `const alerts = useMemo(() => computeAlerts(overview, users?.users), [overview, users])`
    - Loading state: Show 3 animated pulse divs in Card (matching NeedsAttention.tsx loading pattern)
    - Empty state: Show "All clear! No coaching alerts at this time." centered
    - Render: Card with AlertTriangle icon in header, title "Coaching Alerts"
    - Each alert renders as a colored box following NeedsAttention.tsx pattern:
      - high: bg-red-50 border-red-100
      - medium: bg-amber-50 border-amber-100
      - low: bg-blue-50 border-blue-100
    - If alert has actionLink, show "View details" link button (Button variant="link" as Link)

    Export as named export: `export function AlertsPanel()`
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit`
    TypeScript compiles without errors. Verify file exists and exports AlertsPanel.
  </verify>
  <done>
    AlertsPanel component exists at frontend/src/pages/admin/analytics/components/AlertsPanel.tsx. It computes coaching alerts from dashboard overview and user performance data using extracted computeAlerts() function. Displays severity-colored alert boxes (red/amber/blue) with action links. Loading and empty states handled. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. Frontend: `cd frontend && npx tsc --noEmit` -- compiles cleanly
2. TeamActivityTable.tsx exports TeamActivityTable component
3. AlertsPanel.tsx exports AlertsPanel component
4. Both components import from existing hooks (no new dependencies)
5. No modifications to existing files (only new files created)
</verification>

<success_criteria>
- TeamActivityTable renders sortable table with Date, User, Event, Description columns
- Sorting is client-side via TanStack Table getSortedRowModel (no API calls on sort click)
- AlertsPanel computes alerts from existing dashboard/user data with computeAlerts() utility
- Alert severity colors match NeedsAttention.tsx pattern (red/amber/blue)
- Both components handle loading and empty states
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-overview-page/16-02-SUMMARY.md`
</output>
