---
phase: 16-dashboard-overview-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/insights/services.py
  - apps/insights/serializers.py
  - apps/insights/views.py
  - apps/insights/urls.py
  - apps/insights/tests/test_team_trends.py
  - frontend/src/api/insights.ts
  - frontend/src/hooks/useInsights.ts
autonomous: true

must_haves:
  truths:
    - "Backend returns 12 weekly data points with decisions_logged, donations_received, and stage_progressions counts"
    - "Admin-only access enforced on team trends endpoint (non-admin gets 403)"
    - "Frontend hook fetches trend data and returns typed TrendDataPoint array"
  artifacts:
    - path: "apps/insights/services.py"
      provides: "get_team_trends() service function"
      contains: "def get_team_trends"
    - path: "apps/insights/views.py"
      provides: "TeamTrendsView API view"
      contains: "class TeamTrendsView"
    - path: "apps/insights/serializers.py"
      provides: "TeamTrendsResponseSerializer"
      contains: "class TeamTrendsResponseSerializer"
    - path: "apps/insights/urls.py"
      provides: "admin/team-trends/ URL pattern"
      contains: "team-trends"
    - path: "apps/insights/tests/test_team_trends.py"
      provides: "Tests for team trends endpoint"
      contains: "class TestTeamTrendsView"
    - path: "frontend/src/api/insights.ts"
      provides: "TeamTrends TypeScript types and API client function"
      contains: "getAdminTeamTrends"
    - path: "frontend/src/hooks/useInsights.ts"
      provides: "useAdminTeamTrends React Query hook"
      contains: "useAdminTeamTrends"
  key_links:
    - from: "apps/insights/views.py"
      to: "apps/insights/services.py"
      via: "TeamTrendsView calls get_team_trends()"
      pattern: "get_team_trends"
    - from: "frontend/src/hooks/useInsights.ts"
      to: "frontend/src/api/insights.ts"
      via: "useAdminTeamTrends calls getAdminTeamTrends"
      pattern: "getAdminTeamTrends"
    - from: "frontend/src/api/insights.ts"
      to: "/insights/admin/team-trends/"
      via: "apiClient.get"
      pattern: "admin/team-trends"
---

<objective>
Create the backend team trends endpoint and frontend data layer for 12-week trend chart visualization.

Purpose: DASH-04 requires a trend chart showing team metrics over 12 weeks. No existing endpoint provides time-series data -- all current endpoints return current snapshots. This plan adds the missing `get_team_trends()` service function, DRF view, serializer, URL, tests, and the corresponding frontend TypeScript types and React Query hook.

Output: A working `/api/v1/insights/admin/team-trends/` endpoint returning 12 weekly data points, plus frontend types and hook ready for the TrendCharts component in Plan 03.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Backend service patterns (add new function following existing conventions)
@apps/insights/services.py
@apps/insights/views.py
@apps/insights/serializers.py
@apps/insights/urls.py

# Frontend data layer (add new types/hooks following existing patterns)
@frontend/src/api/insights.ts
@frontend/src/hooks/useInsights.ts

# Research findings on trend endpoint design
@.planning/phases/16-dashboard-overview-page/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create team trends backend endpoint with service, serializer, view, URL, and tests</name>
  <files>
    apps/insights/services.py
    apps/insights/serializers.py
    apps/insights/views.py
    apps/insights/urls.py
    apps/insights/tests/test_team_trends.py
  </files>
  <action>
    Add `get_team_trends(weeks=12)` service function to `apps/insights/services.py`:
    - Returns weekly aggregated metrics for the past N weeks (default 12)
    - For each week (ISO week starting Monday), compute:
      - `week_start`: ISO date string of Monday (e.g., "2026-02-03")
      - `week_label`: Human-readable label (e.g., "Feb 3")
      - `decisions_logged`: Count of Decision objects created that week (use TruncWeek on Decision.created_at, or filter by date range per week)
      - `donations_received`: Count of Donation objects with date in that week
      - `stage_progressions`: Count of JournalStageEvent objects created that week
    - Use database-level aggregation (annotate/aggregate) following the established pattern in get_dashboard_overview()
    - Cross-user aggregation (no user parameter) matching the admin service function pattern from 13-02
    - Target: <10 queries total
    - Build complete 12-week list filling gaps with zeros (follow get_donations_by_month pattern)

    Add serializers to `apps/insights/serializers.py`:
    - `TrendDataPointSerializer`: week_start (CharField), week_label (CharField), decisions_logged (IntegerField), donations_received (IntegerField), stage_progressions (IntegerField)
    - `TeamTrendsResponseSerializer`: trends (TrendDataPointSerializer many=True), weeks (IntegerField)

    Add `TeamTrendsView` to `apps/insights/views.py`:
    - APIView with permission_classes = [permissions.IsAuthenticated, IsAdmin]
    - GET method calls get_team_trends(), serializes with TeamTrendsResponseSerializer
    - Accept optional `weeks` query param (default 12, min 1, max 52) using get_safe_int_param
    - Add @extend_schema decorator following existing endpoint patterns

    Add URL pattern to `apps/insights/urls.py`:
    - `path('admin/team-trends/', TeamTrendsView.as_view(), name='admin-team-trends')`

    Create tests in `apps/insights/tests/test_team_trends.py`:
    - Test admin user gets 200 with 12 trend data points
    - Test non-admin user gets 403
    - Test data structure has correct fields (week_start, week_label, decisions_logged, donations_received, stage_progressions)
    - Test custom weeks param works (e.g., weeks=4 returns 4 data points)
    - Test empty database returns 12 zero-filled weeks
    - Use existing test patterns: APIClient, factory-created users with admin/staff roles
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM && python -m pytest apps/insights/tests/test_team_trends.py -v`
    All tests pass. Also verify no regressions: `python -m pytest apps/insights/ -v --timeout=30`
  </verify>
  <done>
    Team trends endpoint returns 200 for admin with 12 weekly data points containing decisions_logged, donations_received, stage_progressions. Non-admin gets 403. All existing insight tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add frontend TypeScript types and React Query hook for team trends</name>
  <files>
    frontend/src/api/insights.ts
    frontend/src/hooks/useInsights.ts
  </files>
  <action>
    Add to `frontend/src/api/insights.ts` (after the existing admin analytics types):
    - `TrendDataPoint` interface: { week_start: string, week_label: string, decisions_logged: number, donations_received: number, stage_progressions: number }
    - `TeamTrendsResponse` interface: { trends: TrendDataPoint[], weeks: number }
    - `TeamTrendsParams` interface: { weeks?: number }
    - `getAdminTeamTrends(params?: TeamTrendsParams)` async function that calls apiClient.get("/insights/admin/team-trends/", { params })

    Add to `frontend/src/hooks/useInsights.ts`:
    - Import `getAdminTeamTrends` and `TeamTrendsParams` from insights API
    - `useAdminTeamTrends(params?: TeamTrendsParams)` hook using useQuery with key ["insights", "admin", "team-trends", params] and staleTime: STALE_TIME

    Follow the exact same patterns used for useAdminTeamActivity (params-accepting hook) and useAdminDashboardOverview (admin analytics hook).
  </action>
  <verify>
    Run: `cd /home/matkukla/projects/DonorCRM/frontend && npx tsc --noEmit`
    TypeScript compiles without errors. Verify the new types and hook are exported correctly.
  </verify>
  <done>
    TrendDataPoint, TeamTrendsResponse, TeamTrendsParams types exist in insights.ts. getAdminTeamTrends function exists. useAdminTeamTrends hook exists in useInsights.ts. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. Backend: `python -m pytest apps/insights/tests/test_team_trends.py -v` -- all tests pass
2. Backend: `python -m pytest apps/insights/ -v --timeout=30` -- no regressions
3. Frontend: `cd frontend && npx tsc --noEmit` -- compiles cleanly
4. URL registered: grep for "team-trends" in urls.py confirms endpoint wired
</verification>

<success_criteria>
- GET /api/v1/insights/admin/team-trends/ returns 200 for admin with 12-element trends array
- Each trend data point has: week_start, week_label, decisions_logged, donations_received, stage_progressions
- Non-admin user receives 403
- Frontend TypeScript types match backend response shape
- React Query hook available for Plan 03 TrendCharts component
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-overview-page/16-01-SUMMARY.md`
</output>
