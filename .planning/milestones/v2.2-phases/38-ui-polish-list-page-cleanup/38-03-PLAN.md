---
phase: 38-ui-polish-list-page-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/insights/ReviewQueue.tsx
  - frontend/src/pages/insights/index.ts
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/App.tsx
  - frontend/src/hooks/useInsights.ts
  - frontend/src/api/insights.ts
  - apps/insights/views.py
  - apps/insights/urls.py
  - apps/insights/services.py
  - apps/insights/serializers.py
  - frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
  - frontend/src/pages/admin/analytics/components/ActivityHeatmap.tsx
  - frontend/package.json
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02

must_haves:
  truths:
    - "Review Queue section is not visible anywhere in the application"
    - "Review Queue sidebar nav link is removed"
    - "Navigating to /insights/review-queue redirects to /admin/analytics/dashboard"
    - "Activity heat map is not visible on the analytics dashboard"
    - "@uiw/react-heat-map package is removed from dependencies"
    - "No backend endpoints exist for review-queue or activity-heatmap"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Sidebar without Review Queue link"
    - path: "frontend/src/App.tsx"
      provides: "Review Queue route redirects to analytics dashboard"
      contains: "Navigate"
    - path: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      provides: "Dashboard without ActivityHeatmap component"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "/admin/analytics/dashboard"
      via: "Review Queue route redirects"
      pattern: "review-queue.*Navigate"
    - from: "frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx"
      to: "ActivityHeatmap removal"
      via: "Import and JSX removed"
      pattern: "ActivityHeatmap"
---

<objective>
Fully remove the Review Queue feature and Activity Heatmap from both backend and frontend. Delete component files, backend views/services, remove routes (with redirect for Review Queue), and uninstall the @uiw/react-heat-map package.

Purpose: Remove unused/unwanted analytics features per user decision, cleaning up code and reducing bundle size.
Output: Clean removal of Review Queue and Activity Heatmap with no broken references.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui-polish-list-page-cleanup/38-CONTEXT.md

<interfaces>
<!-- Files to delete or modify for removal -->

Review Queue removal chain:
1. Backend: apps/insights/views.py (ReviewQueueView class), apps/insights/urls.py (review-queue path), apps/insights/services.py (get_review_queue function), apps/insights/serializers.py (ReviewQueue serializers if any)
2. Frontend API: frontend/src/api/insights.ts (getReviewQueue function + types)
3. Frontend hook: frontend/src/hooks/useInsights.ts (useReviewQueue hook)
4. Frontend page: frontend/src/pages/insights/ReviewQueue.tsx (DELETE file)
5. Frontend barrel: frontend/src/pages/insights/index.ts (remove ReviewQueue export)
6. Router: frontend/src/App.tsx (replace ReviewQueue route with redirect to /admin/analytics/dashboard)
7. Sidebar: frontend/src/components/layout/Sidebar.tsx (remove Review Queue nav item)

Activity Heatmap removal chain:
1. Backend: apps/insights/views.py (ActivityHeatmapView class), apps/insights/urls.py (activity-heatmap path), apps/insights/services.py (get_activity_heatmap function), apps/insights/serializers.py (ActivityHeatmapDaySerializer, ActivityHeatmapResponseSerializer)
2. Frontend API: frontend/src/api/insights.ts (getAdminActivityHeatmap function + types)
3. Frontend hook: frontend/src/hooks/useInsights.ts (useAdminActivityHeatmap hook)
4. Frontend component: frontend/src/pages/admin/analytics/components/ActivityHeatmap.tsx (DELETE file)
5. Dashboard: frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx (remove import + JSX)
6. Package: frontend/package.json (remove @uiw/react-heat-map dependency)

Key backend structure (from apps/insights/urls.py):
```python
path('review-queue/', ReviewQueueView.as_view(), name='review-queue'),
path('admin/activity-heatmap/', ActivityHeatmapView.as_view(), name='admin-activity-heatmap'),
```

Key backend imports (from apps/insights/views.py):
```python
from apps.insights.services import get_review_queue, get_activity_heatmap
```

Key frontend imports (from AdminAnalyticsDashboard.tsx):
```typescript
import { ActivityHeatmap } from "./components/ActivityHeatmap"
// JSX: <ActivityHeatmap dateParams={dateParams} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Review Queue — backend endpoints, frontend page, sidebar link, and route redirect</name>
  <files>
    apps/insights/views.py
    apps/insights/urls.py
    apps/insights/services.py
    apps/insights/serializers.py
    frontend/src/api/insights.ts
    frontend/src/hooks/useInsights.ts
    frontend/src/pages/insights/ReviewQueue.tsx
    frontend/src/pages/insights/index.ts
    frontend/src/components/layout/Sidebar.tsx
    frontend/src/App.tsx
  </files>
  <action>
**Backend removal:**

1. In `apps/insights/views.py`:
   - Delete the entire `ReviewQueueView` class (class definition + all methods)
   - Remove `get_review_queue` from the imports from `apps.insights.services`
   - Remove any Review Queue related serializer imports

2. In `apps/insights/urls.py`:
   - Remove the `path('review-queue/', ReviewQueueView.as_view(), name='review-queue')` line
   - Remove `ReviewQueueView` from the imports

3. In `apps/insights/services.py`:
   - Delete the entire `get_review_queue` function

4. In `apps/insights/serializers.py`:
   - Check for and remove any ReviewQueue-related serializer classes (search for "ReviewQueue" or "review_queue")

**Frontend removal:**

5. In `frontend/src/api/insights.ts`:
   - Remove the `getReviewQueue` function
   - Remove associated TypeScript types/interfaces for Review Queue data

6. In `frontend/src/hooks/useInsights.ts`:
   - Remove the `useReviewQueue` hook function
   - Remove `getReviewQueue` from the import from `@/api/insights`

7. **Delete** `frontend/src/pages/insights/ReviewQueue.tsx` entirely (use `rm` command)

8. In `frontend/src/pages/insights/index.ts`:
   - Remove the `export { default as ReviewQueue } from "./ReviewQueue"` line

9. In `frontend/src/components/layout/Sidebar.tsx`:
   - Remove the Review Queue item from `insightsItems` array: `{ label: "Review Queue", href: "/insights/review-queue", icon: <ClipboardCheck ...>, requiredRole: "admin" }`
   - Remove the `ClipboardCheck` import from lucide-react if it's no longer used elsewhere

10. In `frontend/src/App.tsx`:
    - Remove the `ReviewQueue` lazy import: `const ReviewQueue = React.lazy(() => import("@/pages/insights/ReviewQueue"))`
    - Replace the ReviewQueue route with a redirect:
      ```tsx
      <Route path="/insights/review-queue" element={<Navigate to="/admin/analytics/dashboard" replace />} />
      ```
      This ensures existing bookmarks/links don't 404 (per user decision).

**Check for other consumers:** Before deleting, grep for "ReviewQueue", "review-queue", "review_queue", and "useReviewQueue" to ensure no other files reference these. Remove any remaining references found.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && grep -rn "ReviewQueue\|review.queue\|review_queue\|useReviewQueue" frontend/src/ apps/ --include="*.tsx" --include="*.ts" --include="*.py" | grep -v "node_modules\|__pycache__\|test_\|\.pyc" | grep -v "PLAN\|CONTEXT\|SUMMARY" 2>&1 | head -20</automated>
  </verify>
  <done>
    - ReviewQueueView class deleted from views.py
    - review-queue URL path removed from urls.py
    - get_review_queue function deleted from services.py
    - ReviewQueue.tsx file deleted
    - ReviewQueue export removed from insights/index.ts
    - Review Queue nav link removed from Sidebar
    - /insights/review-queue route redirects to /admin/analytics/dashboard
    - No remaining references to ReviewQueue in non-test source files
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Activity Heatmap — backend endpoints, frontend component, dashboard reference, and npm package</name>
  <files>
    apps/insights/views.py
    apps/insights/urls.py
    apps/insights/services.py
    apps/insights/serializers.py
    frontend/src/api/insights.ts
    frontend/src/hooks/useInsights.ts
    frontend/src/pages/admin/analytics/components/ActivityHeatmap.tsx
    frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx
    frontend/package.json
  </files>
  <action>
**Backend removal:**

1. In `apps/insights/views.py`:
   - Delete the entire `ActivityHeatmapView` class
   - Remove `get_activity_heatmap` from the imports from `apps.insights.services`
   - Remove `ActivityHeatmapResponseSerializer` from the serializer imports

2. In `apps/insights/urls.py`:
   - Remove the `path('admin/activity-heatmap/', ActivityHeatmapView.as_view(), name='admin-activity-heatmap')` line
   - Remove `ActivityHeatmapView` from the imports

3. In `apps/insights/services.py`:
   - Delete the entire `get_activity_heatmap` function

4. In `apps/insights/serializers.py`:
   - Delete the `ActivityHeatmapDaySerializer` class
   - Delete the `ActivityHeatmapResponseSerializer` class

**Frontend removal:**

5. In `frontend/src/api/insights.ts`:
   - Remove the `getAdminActivityHeatmap` function
   - Remove the `ActivityHeatmapParams` type/interface
   - Remove any other Activity Heatmap related types

6. In `frontend/src/hooks/useInsights.ts`:
   - Remove the `useAdminActivityHeatmap` hook function
   - Remove `getAdminActivityHeatmap` and `ActivityHeatmapParams` from the import from `@/api/insights`

7. **Delete** `frontend/src/pages/admin/analytics/components/ActivityHeatmap.tsx` entirely (use `rm` command)

8. In `frontend/src/pages/admin/analytics/AdminAnalyticsDashboard.tsx`:
   - Remove the `import { ActivityHeatmap } from "./components/ActivityHeatmap"` line
   - Remove the JSX: `{/* Heatmap - Full Width */}` comment and `<ActivityHeatmap dateParams={dateParams} />` element

**Package removal:**

9. Run `cd frontend && npm uninstall @uiw/react-heat-map` to remove the package from package.json and package-lock.json.
   - First verify no other files import from `@uiw/react-heat-map`: `grep -rn "@uiw/react-heat-map" frontend/src/` should return nothing after ActivityHeatmap.tsx is deleted.

**Check for other consumers:** Grep for "ActivityHeatmap", "activity.heatmap", "activity_heatmap", "useAdminActivityHeatmap", "@uiw/react-heat-map" to ensure clean removal.
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && grep -rn "ActivityHeatmap\|activity.heatmap\|activity_heatmap\|useAdminActivityHeatmap\|@uiw/react-heat-map" frontend/src/ apps/ --include="*.tsx" --include="*.ts" --include="*.py" | grep -v "node_modules\|__pycache__\|test_\|\.pyc" | grep -v "PLAN\|CONTEXT\|SUMMARY" 2>&1 | head -20</automated>
  </verify>
  <done>
    - ActivityHeatmapView class deleted from views.py
    - activity-heatmap URL path removed from urls.py
    - get_activity_heatmap function deleted from services.py
    - ActivityHeatmap serializer classes deleted
    - ActivityHeatmap.tsx component file deleted
    - ActivityHeatmap import and JSX removed from AdminAnalyticsDashboard.tsx
    - @uiw/react-heat-map uninstalled from package.json
    - No remaining references to ActivityHeatmap in non-test source files
  </done>
</task>

</tasks>

<verification>
1. `python manage.py check` passes
2. `npx tsc --noEmit` passes
3. `grep -rn "ReviewQueue" frontend/src/ apps/ --include="*.tsx" --include="*.ts" --include="*.py" | grep -v test_ | grep -v PLAN` returns no matches (except route redirect)
4. `grep -rn "ActivityHeatmap" frontend/src/ apps/ --include="*.tsx" --include="*.ts" --include="*.py" | grep -v test_ | grep -v PLAN` returns no matches
5. `grep "@uiw/react-heat-map" frontend/package.json` returns no match
6. Review Queue route redirects to analytics dashboard (not 404)
7. Analytics dashboard renders without heatmap section
</verification>

<success_criteria>
- Review Queue page, backend endpoint, sidebar link, and hook are fully removed
- /insights/review-queue redirects to /admin/analytics/dashboard
- Activity Heatmap component, backend endpoint, and @uiw/react-heat-map package are fully removed
- Analytics dashboard renders cleanly without heatmap section
- All TypeScript and Python checks pass
- No orphaned imports or dead references remain
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui-polish-list-page-cleanup/38-03-SUMMARY.md`
</output>
