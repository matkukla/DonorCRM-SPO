---
phase: 25-smartsheet-import-frontend
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/mpd.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Upload history section in MPDImportTile renders recent uploads after a successful MPD upload"
    - "getMPDUploadHistory() returns a flat MPDUploadHistoryItem[] array, not a wrapped object"
  artifacts:
    - path: "frontend/src/api/mpd.ts"
      provides: "getMPDUploadHistory with unwrapped response"
      contains: "response.data.uploads"
  key_links:
    - from: "frontend/src/api/mpd.ts"
      to: "apps/imports/views.py (MPDUploadHistoryView)"
      via: "GET /api/v1/imports/mpd/uploads/ returns { uploads: [...] }"
      pattern: "response\\.data\\.uploads"
    - from: "frontend/src/components/imports/MPDImportTile.tsx"
      to: "frontend/src/api/mpd.ts"
      via: "useMPDUploadHistory hook consuming getMPDUploadHistory"
      pattern: "uploadHistory\\?\\.slice"
---

<objective>
Fix the upload history wiring bug: the "Recent Uploads" section in the MPD Import Tile never renders because the backend returns `{ uploads: [...] }` but the frontend expects a flat array.

Purpose: Close Gap 1 from VERIFICATION.md — SC#1 partial due to response shape mismatch causing silent failure of the upload history display.
Output: Fixed `getMPDUploadHistory()` that unwraps the backend envelope so `useMPDUploadHistory` returns `MPDUploadHistoryItem[]` as typed.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-smartsheet-import-frontend/25-VERIFICATION.md
@frontend/src/api/mpd.ts
@frontend/src/components/imports/MPDImportTile.tsx
@frontend/src/hooks/useMPD.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getMPDUploadHistory response unwrapping</name>
  <files>frontend/src/api/mpd.ts</files>
  <action>
In `getMPDUploadHistory()` (line 94-97), the function currently returns `response.data` which is the full `{ uploads: MPDUploadHistoryItem[] }` envelope from the backend `MPDUploadHistoryView`.

Change the return statement from:
```typescript
return response.data
```
to:
```typescript
return response.data.uploads
```

This is Option A from the VERIFICATION.md gap analysis — unwrap the envelope on the frontend side. The backend `MPDUploadHistoryView.get()` returns `Response({'uploads': data})`, so `response.data` is `{ uploads: [...] }`. By returning `response.data.uploads`, the function correctly provides the `MPDUploadHistoryItem[]` that its return type promises.

Do NOT change the backend view (Option B) — the backend envelope pattern is intentional and consistent with other views that may add pagination metadata later.

Do NOT change MPDImportTile.tsx — the `uploadHistory?.slice(0, 5)` call is correct once `uploadHistory` is actually an array as typed.

Do NOT change the TypeScript types — `Promise<MPDUploadHistoryItem[]>` is already the correct return type for what consumers expect.
  </action>
  <verify>
1. Run `cd frontend && npx tsc --noEmit` — zero TypeScript errors (the return type already matches the corrected data shape).
2. Read the updated `getMPDUploadHistory()` and confirm it returns `response.data.uploads`.
3. Grep for `response.data.uploads` in `frontend/src/api/mpd.ts` to confirm the fix is in place.
  </verify>
  <done>
`getMPDUploadHistory()` returns `response.data.uploads` (a flat `MPDUploadHistoryItem[]`), which means `useMPDUploadHistory` provides an array to MPDImportTile, and `uploadHistory?.slice(0, 5)` correctly produces the 5 most recent uploads for the "Recent Uploads" section to render.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `cd frontend && npx tsc --noEmit` — 0 errors
2. The fix is a single-line change in `frontend/src/api/mpd.ts` — `response.data` becomes `response.data.uploads`
3. No other files need modification — the type signature, hook, and consumer component are already correct for the expected array shape
</verification>

<success_criteria>
- getMPDUploadHistory() returns response.data.uploads (unwrapped array)
- TypeScript compiles with zero errors
- Upload history section in MPDImportTile will render when upload history data exists (full runtime verification deferred to human testing)
</success_criteria>

<output>
After completion, create `.planning/phases/25-smartsheet-import-frontend/25-05-SUMMARY.md`
</output>
