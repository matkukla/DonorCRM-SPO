---
phase: quick
plan: gc8
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DATA-AUDIT]
must_haves:
  truths:
    - "All 5 test_data CSV files parse and import without errors"
    - "Every CSV column either maps to a model field or is intentionally ignored"
    - "Existing fixture mapping tests pass"
  artifacts: []
  key_links: []
---

<objective>
Verify that all 5 CSV files in test_data/ (test_constituents.csv, test_gifts.csv, test_recurring_gifts.csv, test_smartsheet.csv, test_solicitors.csv) will map correctly through the import pipeline to the application models.

Purpose: Confirm data integrity of test fixtures — every column maps to the right model field, no data gets silently dropped, and no import errors occur.
Output: Test run confirming all fixture mapping tests pass, plus analysis report of any unmapped columns or edge cases.
</objective>

<execution_context>
@/home/matkukla/.claude/get-shit-done/workflows/execute-plan.md
@/home/matkukla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@test_data/test_constituents.csv
@test_data/test_gifts.csv
@test_data/test_recurring_gifts.csv
@test_data/test_smartsheet.csv
@test_data/test_solicitors.csv
@apps/imports/re_services.py
@apps/imports/spo_services.py
@apps/imports/mpd_services.py
@apps/imports/tests/test_spo_csv_fixture_mapping.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing fixture mapping tests and analyze results</name>
  <files></files>
  <action>
Run the existing fixture mapping test suite that exercises all 4 SPO CSV files against the real import pipeline:

```bash
cd /home/matkukla/projects/DonorCRM
python manage.py test apps.imports.tests.test_spo_csv_fixture_mapping -v 2
```

This test file covers:
- test_solicitors.csv -> reconcile_missionaries() (20 missionaries, Solicitor records)
- test_gifts.csv -> import_spo_gifts() (Fund Split Amount alias, anonymous donors, prayer descriptions)
- test_recurring_gifts.csv -> import_re_recurring_gifts() (100 rows, type-label skip, contact+solicitor lookup)
- test_constituents.csv -> import_re_constituents() (400 contacts, type-label skip)

If any tests fail, investigate the root cause by examining:
1. Whether CSV headers match the alias maps (case-insensitive)
2. Whether data values parse correctly (dates, amounts, frequencies, statuses)
3. Whether cross-file references resolve (constituent_id from gifts -> contacts, solicitor names -> users)

Also verify test_smartsheet.csv mapping by checking that every column header in the file has a corresponding entry in SMARTSHEET_COLUMN_MAP (in mpd_services.py). Do this programmatically:

Read the first row of test_smartsheet.csv, lowercase each header, and check against the SMARTSHEET_COLUMN_MAP keys. Report any unrecognized columns.

Produce a mapping analysis covering all 5 files:

For each file, document:
- File name
- Number of data rows (excluding type-label and header rows)
- Which import function processes it
- Header-to-model-field mapping (which CSV column -> which canonical field -> which model field)
- Any columns that are intentionally ignored (not in alias map) and WHY (e.g., "Gift Date Last Changed" is metadata not needed)
- Any edge cases in the data (empty values, special formats like "$500.00", date formats like "03/15/26")
- Whether the data parses correctly through the import pipeline

Pay special attention to these potential issues identified during analysis:

1. **test_gifts.csv**: The "Fund Split Amount" column contains dollar-formatted values like "$500.00". Verify _parse_amount_to_cents handles the dollar sign and comma formatting. The "Gift Date Last Changed" and "Gift Type" and "Soft Credit Recipient ID" columns are NOT in any alias map — confirm these are intentionally ignored (metadata columns).

2. **test_recurring_gifts.csv**: The "Solicitor Amount" header maps to `amount` via the RECURRING_GIFT_HEADER_ALIASES (alias `solicitor amount` -> canonical `amount`). This means the recurring gift amount comes from the Solicitor Amount column, not a separate amount column. Verify the dollar-formatted values like "$50.00" parse correctly. Also verify: "Gift First Installment Due", "Last Installment/Payment Date Due", "Number of Installments Scheduled", "Gift First Installment Due_1", "Gift Is Anonymous", "Gift Date Last Changed", "Gift Type", "Gift Status Date", "Soft Credit Recipient ID" are all intentionally unmapped.

3. **test_constituents.csv**: The "Address Line 2" column is NOT in CONSTITUENT_HEADER_ALIASES — verify this is intentional (the Contact model may not have an address_line_2 field). The "Constituent Date Last Changed" column is also unmapped — metadata.

4. **test_smartsheet.csv**: The "% Standard to Max" column contains decimal values like "2.919822" (not percentage-formatted like "291%"). Verify parse_percentage() handles this correctly — if abs(value) <= 10, it multiplies by 100, which would give 292 (rounded). Confirm this is the intended behavior.

5. **test_solicitors.csv**: Simple "Name" header. Verify it maps to `raw_name` via SOLICITOR_HEADER_ALIASES (alias `name` -> `raw_name`). All 20 names are "First Last" format — verify normalize_solicitor_name() correctly reverses to "last, first".
  </action>
  <verify>
    <automated>cd /home/matkukla/projects/DonorCRM && python manage.py test apps.imports.tests.test_spo_csv_fixture_mapping -v 2 2>&1 | tail -20</automated>
  </verify>
  <done>All existing fixture mapping tests pass. A complete mapping analysis report is produced covering all 5 files, confirming every column either maps correctly or is intentionally ignored, with no silent data loss.</done>
</task>

</tasks>

<verification>
- All tests in test_spo_csv_fixture_mapping.py pass (0 failures, 0 errors)
- Every CSV header in all 5 files is accounted for (mapped or documented as intentionally ignored)
- Dollar-formatted amounts parse correctly through _parse_amount_to_cents / parse_currency
- Date formats (MM/DD/YY) parse correctly through _parse_date
- Frequency values ("Monthly") map correctly through FREQUENCY_MAP
- Status values ("Active") map correctly through STATUS_MAP
- Smartsheet percentage and currency values parse correctly
</verification>

<success_criteria>
All 5 test data files are confirmed to map correctly to the application:
1. test_solicitors.csv: 20 missionaries created via reconcile_missionaries, "Name" column resolves
2. test_constituents.csv: 400 contacts created via import_re_constituents, all address/phone/email fields map
3. test_gifts.csv: All gifts import via import_spo_gifts, Fund Split Amount parses, prayers created
4. test_recurring_gifts.csv: 100 recurring gifts import via import_re_recurring_gifts, frequency/status/amount parse
5. test_smartsheet.csv: All 22 data columns recognized by SMARTSHEET_COLUMN_MAP, currency/boolean/percentage parse
</success_criteria>

<output>
After completion, create `.planning/quick/260318-gc8-check-all-the-files-in-the-test-data-fol/260318-gc8-SUMMARY.md`
</output>
