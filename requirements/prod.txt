-r base.txt

# Production server
# >=23.0 required: 22.x is affected by HTTP Request Smuggling
# (SNYK-PYTHON-GUNICORN-9510910 / High), fixed in 23.0.0.
gunicorn>=26.0.0,<27.0

# Static files
whitenoise>=6.12.0,<7.0

# Database URL parsing
dj-database-url>=2.0,<3.0

# Error tracking — v2.x is the supported series; v1.x is EOL.
sentry-sdk>=2.62.0,<3.0
